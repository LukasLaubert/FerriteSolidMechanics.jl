var documenterSearchIndex = {"docs":
[{"category":"section","location":"models/j2_plasticity/#J2-Plasticity","page":"J2 Plasticity","text":"J2Plasticity implements small-strain von Mises (J2) plasticity with linear isotropic hardening (src/models/J2Plasticity.jl). The stress is integrated by radial return mapping, with a consistent algorithmic tangent [1, 2].","title":"J2 Plasticity"},{"category":"section","location":"models/j2_plasticity/#Mathematical-formulation","page":"J2 Plasticity","text":"The model is defined by the yield function Phi(boldsymbolsigma k) as\n\nPhi(boldsymbolsigma k) = sigma_mathrmeq - leftsigma_0 + H kright le 0 text\n\nwhere boldsymbolsigma is the Cauchy stress, sigma_mathrmeq = sqrtfrac32 boldsymbols  boldsymbols the von Mises equivalent stress, boldsymbols = operatornamedev(boldsymbolsigma) the deviatoric stress, sigma_0 the initial yield stress, H the linear isotropic hardening modulus, and k the accumulated plastic strain.\n\nThe superscript mathrmtr denotes a trial value, and the subscript n denotes the committed start-of-step state. Here, boldsymbolD^mathrme is the elastic stiffness tensor, boldsymbolepsilon the total small-strain tensor, boldsymbolepsilon^mathrmp_n the start-of-step plastic strain, G the shear modulus, and boldsymbolD the returned algorithmic tangent. Using this notation, the return mapping algorithm is evaluated as follows:\n\nTrial elastic state\nboldsymbolsigma^mathrmtr = boldsymbolD^mathrme  leftboldsymbolepsilon - boldsymbolepsilon^mathrmp_nright quad Phi^mathrmtr = sigma^mathrmtr_mathrmeq - leftsigma_0 + H k_nright\nwith boldsymbols^mathrmtr=operatornamedev(boldsymbolsigma^mathrmtr) and sigma^mathrmtr_mathrmeq = sqrtfrac32boldsymbols^mathrmtrboldsymbols^mathrmtr.\nIf Phi^mathrmtr le 0 (elastic step)\nboldsymbolsigma = boldsymbolsigma^mathrmtr quad boldsymbolepsilon^mathrmp = boldsymbolepsilon^mathrmp_n quad k = k_n quad boldsymbolD = boldsymbolD^mathrme\nIf Phi^mathrmtr  0 (plastic step) The plastic multiplier increment mu is defined as\nmu = fracPhi^mathrmtrH + 3G text\nit scales the trial deviatoric stress by\nc_1 = 1 - frac3Gmusigma^mathrmtr_mathrmeq text\nqquad\nboldsymbols = c_1 boldsymbols^mathrmtr text\nqquad\nboldsymbolsigma = boldsymbols + mathrmvol(boldsymbolsigma^mathrmtr) text\nHere, c_1 is the scalar return mapping scale factor and mathrmvol(cdot) extracts the spherical stress part. The updated yield stress is sigma_mathrmeq = sigma_0 + Hleftk_n + muright, and the state update follows\nboldsymbolepsilon^mathrmp =\nboldsymbolepsilon^mathrmp_n\n+ frac32fracmusigma_mathrmeqboldsymbols text\nqquad\nk = k_n + mu text\nThe plastic tangent returned by compute_stress_tangent is\nboldsymbolD\n=\nboldsymbolD^mathrme\n- 2GbboldsymbolQ\n- frac9G^2hsigma_mathrmeq^2boldsymbolsotimesboldsymbols text\nwith\nh = H + 3Gqquad\nb = frac3Gmusigma_mathrmeq1 + 3Gmusigma_mathrmeq\nand\nQ_ijkl\n=\nI^mathrmdev_mathrmsymijkl\n- frac32sigma_mathrmeq^2s_ijs_kl text","title":"Mathematical formulation"},{"category":"section","location":"models/j2_plasticity/#Implementation-details","page":"J2 Plasticity","text":"J2PlasticityState holds the plastic strain ϵᵖ, Cauchy stress σ, and hardening variable k in a trial/commit pair. In the elastic regime, the analytical tangent returns Dᵉ; in the plastic regime, it returns the algorithmic tangent from the return mapping update. Hardening is further linear isotropic: σʸ = σ₀ + H k.\n\nThe generic 2D wrappers use the model's compute_PK1_3D and update_state_from_3D! hooks. update_state_from_3D! writes the current trial plastic state from the embedded 3D strain.","title":"Implementation details"},{"category":"section","location":"models/j2_plasticity/#Constructor","page":"J2 Plasticity","text":"J2Plasticity(E, ν, σ₀, H)","title":"Constructor"},{"category":"section","location":"models/j2_plasticity/#2D-usage","page":"J2 Plasticity","text":"material = PlaneStress(J2Plasticity(E, ν, σ₀, H))   # plane stress\nmaterial = PlaneStrain(J2Plasticity(E, ν, σ₀, H))   # plane strain\n\nFor wrapper mechanics, see Wrappers.","title":"2D usage"},{"category":"section","location":"models/j2_plasticity/#Model-parameters","page":"J2 Plasticity","text":"Constructor argument Symbol Description\nE E Young's modulus\nν nu Poisson's ratio\nσ₀ sigma_0 Initial yield stress\nH H Linear isotropic hardening modulus","title":"Model parameters"},{"category":"section","location":"models/j2_plasticity/#Provenance","page":"J2 Plasticity","text":"J2Plasticity, its constitutive driver implementation, and related documentation are adapted from the Ferrite.jl von Mises plasticity tutorial [3]. The upstream copyright/license notice is retained in the repository LICENSE. The Developer guide walks through the implementation as a worked example for new material authors.","title":"Provenance"},{"category":"section","location":"models/j2_plasticity/#References","page":"J2 Plasticity","text":"[1] de Souza Neto, E. A., Perić, D., & Owen, D. R. J. (2008). Computational Methods for Plasticity: Theory and Applications. Wiley.\n\n[2] Simo, J. C., & Hughes, T. J. R. (1998). Computational Inelasticity. Springer.\n\n[3] Ferrite.jl documentation. Von Mises plasticity tutorial. https://ferrite-fem.github.io/Ferrite.jl/stable/tutorials/plasticity/","title":"References"},{"category":"section","location":"models/vevp_zhao2021/#VEVP-Zhao-2021","page":"VEVP Zhao 2021","text":"VEVP_Zhao2021_AD and VEVP_Zhao2021_AT are two finite-strain viscoelastic–viscoplastic implementations after Zhao et al. [1]. The implementations are available in src/models/VEVP_Zhao2021_AD.jl and src/models/VEVP_Zhao2021_AT.jl. They use a generalized Maxwell framework with one elastic module and multiple inelastic modules in parallel, with yield-threshold softening and an evolving inelastic shear modulus. Figure 1 sketches the parallel elastic, viscoelastic, and elasto-viscoplastic module layout used by the model.\n\n(Image: Rheological representation of the Zhao VEVP model)\n\nFigure 1. Rheological representation of the VEVP Zhao 2021 model with one elastic module, one viscoelastic module, and elasto-viscoplastic modules in parallel.\n\nThe two implementations share the same 14 positional constructor arguments plus the optional dt_scale keyword. VEVP_Zhao2021_AD obtains the algorithmic tangent by automatic differentiation and supports the generic PlaneStrain / PlaneStress wrapper path. VEVP_Zhao2021_AT uses a pre-derived analytical tangent in the 3D assemble-time path, with per-QP branch tensors stored as Tensors.jl SymmetricTensor{2,3,Float64,6} values. They are not interchangeable: VEVP_Zhao2021_AD assembles the final PK2 stress from the converged updated branch state, while VEVP_Zhao2021_AT follows the analytical-tangent reference path and assembles it from trial branch stresses. Switching variants for the same load case therefore changes the PK2 stress output.","title":"VEVP Zhao 2021"},{"category":"section","location":"models/vevp_zhao2021/#Mathematical-formulation","page":"VEVP Zhao 2021","text":"Zhao et al. [1] use a generalized Maxwell framework for finite viscoelasticity [2] with one elastic module and N_mathrmV inelastic modules in parallel. With the common setting τcut₁ = 0, the first inelastic module is viscoelastic as its initial effective yield stress is zero; positive τcut₁ gives that module an initial yield threshold. The remaining modules are elasto-viscoplastic. Softening works through a decrease of the effective yield stress with the maximum deformation reached so far.\n\nThe stress below is written in second Piola-Kirchhoff (PK2) form, using the deformation gradient boldsymbolF, the right Cauchy-Green tensor boldsymbolC=boldsymbolF^mathrmTboldsymbolF, the volume ratio J=detboldsymbolF, and one inelastic right Cauchy-Green tensor boldsymbolC_mathrmij for each module. Here, mathrmi labels the inelastic tensor, j=1ldotsN_mathrmV labels the inelastic module, and n is the time step index. Throughout this page, operatornamedev(cdot) denotes the deviatoric part and cdot_mathrmF denotes the Frobenius norm.","title":"Mathematical formulation"},{"category":"section","location":"models/vevp_zhao2021/#1.-Stress-evaluation","page":"VEVP Zhao 2021","text":"Using boldsymbolC = boldsymbolF^mathrmTboldsymbolF text J = detboldsymbolF text and boldsymbolC^* = J^-23boldsymbolC text the implemented PK2 stress reads as\n\nboldsymbolS\n=\nkappa JleftJ-1rightboldsymbolC^-1\n+ mu^mathrmE boldsymbolC^-1operatornamedev(boldsymbolC^*)\n+ sum_j=1^N_mathrmV\nmu^mathrmV_j boldsymbolC^-1\noperatornamedev(boldsymbolC^*boldsymbolC_mathrmij^-1) text\n\nHere, mu^mathrmE is the elastic shear modulus, kappa is the total bulk modulus, mu^mathrmV is the current total inelastic shear modulus, and mu^mathrmV_j is the part assigned to module j. While Zhao et al. [1] use the uniform assignment mu^mathrmV_j=mu^mathrmVN_mathrmV, this implementation also supports a linear module distribution through the constructor argument μVN, which sets the module-N_mathrmV endpoint mu^mathrmV_N_mathrmV. When μVN is close to zero, the uniform assignment mu^mathrmVN_mathrmV is used; otherwise the module moduli are linearly distributed and still sum to mu^mathrmV.","title":"1. Stress evaluation"},{"category":"section","location":"models/vevp_zhao2021/#2.-Viscoplastic-flow","page":"VEVP Zhao 2021","text":"The module flow is driven by the deviatoric norm of the pushed-forward trial module stress\n\nboldsymboltau^mathrmtr_j\n=\nboldsymbolFboldsymbolS^mathrmtr_jboldsymbolF^mathrmT text\n\nHere, boldsymbolS^mathrmtr_j is the trial PK2 stress contribution of inelastic module j, and boldsymboltau^mathrmtr_j is its pushed-forward trial stress.\n\nThe history variable stored as strain_maxk is the maximum principal stretch ratio measure of Zhao et al. [1]\n\nv_max(t)\n=\nmax_0 le s le t\nleftfraclambda_max(s)lambda_min(s) - 1 right text\n\nHere, lambda_max and lambda_min are the largest and smallest principal stretches reached at the history time s.\n\nUsing the ramp function langle xrangle = leftx + xright2, the effective yield stress in module j is\n\nr_mathrmyj(v_max)\n=\nleftlangle r_mathrmy0j - beta v_max rightrangle text\n\nwith initial module threshold r_mathrmy0j and softening coefficient beta.\n\nThe effective flow measure evaluated by the implementation is\n\ndotgamma_mathrmDj\n=\nleftlangle\nfracoperatornamedevboldsymboltau^mathrmtr_j_mathrmFhattau_j- r_mathrmyj(v_max) rightrangle^m_j text\n\nThe constructor arguments τ̂₁ and τ̂N set the endpoint values for the hattau_j appearing in the denominator of the power-flow law. The endpoint arguments m₁ and mN set the stress exponent m_j for each module. The constructor arguments τcut₁ and τcutN set the endpoint values for r_mathrmy0j in the effective yield stress r_mathrmyj. Setting τcut₁ = 0 gives r_mathrmy01=0; positive values give the first module an initial yield threshold.","title":"2. Viscoplastic flow"},{"category":"section","location":"models/vevp_zhao2021/#3.-Inelastic-state-update","page":"VEVP Zhao 2021","text":"For the stable AD and AT variants, the inelastic tensor is updated over the time increment Delta t with the semi-implicit, volume-preserving update [3] as\n\nboldsymbolC^mathrmnd_mathrmijn+1\n=\nboldsymbolC_mathrmijn\n+ Delta t\nfracdotgamma_mathrmDjoperatornamedevboldsymboltau^mathrmtr_j_mathrmF\nmu^mathrmV_j boldsymbolC^* text\n\nThe superscript mathrmnd marks the intermediate inelastic tensor before determinant normalization, while the normalized end-of-step tensor is\n\nboldsymbolC_mathrmijn+1\n=\ndet(boldsymbolC^mathrmnd_mathrmijn+1)^-13\nboldsymbolC^mathrmnd_mathrmijn+1text\n\nIf the trial stress norm is zero, the prefactor dotgamma_mathrmDjoperatornamedevboldsymboltau^mathrmtr_j_mathrmF is taken as zero. The determinant normalization enforces the isochoric-inelastic assumption used in Zhao et al. [1]. The AD implementation additionally floors detboldsymbolC, principal stretch eigenvalues, and the boldsymbolC_mathrmijn+1 determinant normalizer for differentiability and numerical guards; the AT implementation does not apply those AD-only caps.","title":"3. Inelastic state update"},{"category":"section","location":"models/vevp_zhao2021/#4.-Additional-shear-modulus-evolution-in-this-implementation","page":"VEVP Zhao 2021","text":"In addition to the yield stress softening criterion of Zhao et al. [1], the implementation evolves the total inelastic shear modulus as\n\nmu^mathrmV_n+1\n=\nmu^mathrmV_n\n+ alphaleftmu^mathrmVe - mu^mathrmV_nrightdotgamma_mathrmD1Delta t text\n\nHere, alpha controls the evolution rate, mu^mathrmVe is the target total inelastic shear modulus, and dotgamma_mathrmD1 is the flow measure of the first inelastic module. The updated total mu^mathrmV_n+1 is stored for the next step. Critically, VEVP_Zhao2021_AD redistributes the updated total modulus before final stress assembly, while VEVP_Zhao2021_AT follows the analytical-tangent path and evaluates final stress from the trial branch stresses.","title":"4. Additional shear modulus evolution in this implementation"},{"category":"section","location":"models/vevp_zhao2021/#Implementation-details","page":"VEVP Zhao 2021","text":"","title":"Implementation details"},{"category":"section","location":"models/vevp_zhao2021/#State-variables","page":"VEVP Zhao 2021","text":"Both Zhao variants track the maximum principal stretch v_max, the total inelastic shear modulus mu^mathrmV, and one inelastic right Cauchy-Green tensor boldsymbolC_mathrmij per module j in a trial/commit state pair.\n\nThe AD variant uses VEVP_Zhao2021_ADState as the trial/commit state wrapper. The internal state type VEVP_Zhao2021_ADStateInternal stores the forward per-module tensors Cik as SymmetricTensor{2,3,Float64,6} and recomputes their inverses as needed. This stores left6 NV + 2right  2 Float64 values per quadrature point.\n\nThe AT variant uses VEVP_Zhao2021_ATState as the trial/commit state wrapper. The internal state type VEVP_Zhao2021_ATStateInternal additionally caches the inverse tensors Cik_inv and the unnormalized next-step tensors Cikp1_nd to support the analytical tangent. This stores left18 NV + 2right  2 Float64 values per quadrature point.","title":"State variables"},{"category":"section","location":"models/vevp_zhao2021/#Tangent-and-stress-formulations","page":"VEVP Zhao 2021","text":"Two formulations are provided:\n\nVEVP_Zhao2021_AD: Evaluates the stress at the converged updated state.\n\nThe tangent is obtained via automatic differentiation (Tensors.gradient) over the local update compute_PK1_3D.\n\nVEVP_Zhao2021_AT: Evaluates the stress using the trial state.\n\nThe tangent is an analytical stiffness matrix from [4], assembled from nodal shape-gradient blocks in a custom _assemble_element! method. It is the package's primary example of a model that implements the element-level assembly hook (_assemble_element!) directly, as its node-block tangent structure cannot be expressed as the quadrature point ∂P/∂F which the generic element routine exercises.\n\nwarning: Non-interchangeable formulations\nSince VEVP_Zhao2021_AD uses the updated branch state and VEVP_Zhao2021_AT uses the trial branch state, they return different stresses for the same deformation step. The analytical tangent is finite-difference consistent while flow is inactive, but not once flow is active, so VEVP_Zhao2021_AT needs more Newton iterations per load step and tolerates a smaller load step than VEVP_Zhao2021_AD.\n\nwarning: 2D wrapper limitation\nVEVP_Zhao2021_AT supports PlaneStrain through a separate analytical tangent assembly path, but does not support PlaneStress or the generic wrapper AD path. VEVP_Zhao2021_AD also supports plane stress analyses.","title":"Tangent and stress formulations"},{"category":"section","location":"models/vevp_zhao2021/#Local-integration","page":"VEVP Zhao 2021","text":"solve_local_vevp_zhao_exact performs a semi-implicit update:\n\nKinematics: boldsymbolC=boldsymbolF^mathrmTboldsymbolF and boldsymbolC^*=leftdetboldsymbolCright^-13boldsymbolC.\nCompute the trial deviatoric stress in each inelastic module.\nEvaluate the ramped power-flow measure gamma_mathrmDj.\nUpdate the volume-preserving per-module Cik tensors via a closed-form push-forward (no local Newton–Raphson iteration).\nUpdate the total inelastic shear modulus mu^mathrmV_n+1.\nAssemble the stress from the updated branch state for AD, or from the trial branch stresses for AT.\n\nIn the AT stiffness assembly, the internal helper gamma_D stores a normalized flow prefactor after the local update. This is an implementation detail of the analytical tangent and should not be interpreted as the raw power-flow measure dotgamma_mathrmDj used in the equations above.","title":"Local integration"},{"category":"section","location":"models/vevp_zhao2021/#Constructor","page":"VEVP Zhao 2021","text":"VEVP_Zhao2021_AD(μE, κ, NV, β, α, μVe, μVN, τ̂₁, τ̂N, m₁, mN, τcut₁, τcutN, μV; dt_scale=1.0)\nVEVP_Zhao2021_AT(μE, κ, NV, β, α, μVe, μVN, τ̂₁, τ̂N, m₁, mN, τcut₁, τcutN, μV; dt_scale=1.0)\n\nBoth constructors require NV >= 2. Each per-module parameter (τ̂_j, m_j, r_y0,j, μV_j) is interpolated between its module-1 and module-N_V endpoint values; for μV_j the interpolation is uniform when μVN is near zero and linear otherwise.\n\ninfo: Time-unit scaling (`dt_scale` kwarg)\ndt_scale multiplies dt inside the constitutive update so the time step is always expressed in the unit used during parameter calibration. Default 1.0: leave it alone when the driver's dt already uses that unit. When the calibration unit and the driver unit differ, set it to the conversion factor – e.g. parameters calibrated in seconds but the driver passes dt in nanoseconds → dt_scale = 1e-9.","title":"Constructor"},{"category":"section","location":"models/vevp_zhao2021/#2D-usage","page":"VEVP Zhao 2021","text":"The two variants differ in wrapper support. VEVP_Zhao2021_AD supports both wrappers through its own compute_PK1_3D and update_state_from_3D! methods, which the generic wrapper path differentiates:\n\nmaterial = PlaneStress(VEVP_Zhao2021_AD(μE, κ, NV, β, α, μVe, μVN, τ̂₁, τ̂N, m₁, mN, τcut₁, τcutN, μV))\nmaterial = PlaneStrain(VEVP_Zhao2021_AD(μE, κ, NV, β, α, μVe, μVN, τ̂₁, τ̂N, m₁, mN, τcut₁, τcutN, μV))\n\nVEVP_Zhao2021_AT supports plane strain only, through a custom _assemble_element! method that embeds the in-plane deformation gradient with F33 = 1 and assembles the analytical tangent from embedded shape gradients:\n\nmaterial = PlaneStrain(VEVP_Zhao2021_AT(μE, κ, NV, β, α, μVe, μVN, τ̂₁, τ̂N, m₁, mN, τcut₁, τcutN, μV))\n\nUse VEVP_Zhao2021_AD for plane stress analyses. PlaneStress(VEVP_Zhao2021_AT(...)) throws an ArgumentError at assembly because the analytical tangent implementation is Float64/matrix-based and is not transparent to the AD used by the generic wrapper path.\n\nFor wrapper mechanics, see Wrappers.","title":"2D usage"},{"category":"section","location":"models/vevp_zhao2021/#Model-parameters","page":"VEVP Zhao 2021","text":"Group Constructor argument Symbol Description\nElasticity μE mu^mathrmE Elastic branch shear modulus\nElasticity κ kappa Bulk modulus\nModule layout NV N_mathrmV Number of inelastic modules (requires N_mathrmV geq 2)\nYield softening β beta Yield-threshold softening coefficient; larger values drop the threshold sooner\nShear modulus evolution α alpha Inelastic shear modulus evolution rate; higher values approach μVe faster\nShear modulus evolution μVe mu^mathrmVe Target total inelastic shear modulus approached by the μV evolution\nModule modulus distribution μVN mu^mathrmV_N_mathrmV Module-N_mathrmV shear modulus endpoint; near zero gives uniform modules\nModule interpolation τ̂₁, τ̂N hattau_1, hattau_N_mathrmV Flow-resistance stress-scale endpoints in the power-flow law; larger values reduce the viscoplastic flow rate\nModule interpolation m₁, mN m_1, m_N_mathrmV Stress exponents in the power-flow law; larger values increase the nonlinearity of the viscoplastic rate\nModule interpolation τcut₁, τcutN r_mathrmy01, r_mathrmy0N_mathrmV Initial yield thresholds for the modules; subtracted from the trial-stress ratio before the power-law ramp\nInitial state μV mu^mathrmV_0 Initial total inelastic shear modulus","title":"Model parameters"},{"category":"section","location":"models/vevp_zhao2021/#Provenance-and-acknowledgements","page":"VEVP Zhao 2021","text":"The code-level reference is the CAPRICCIO/VEVP implementation [4]. Wuyang Zhao confirmed that the Julia translation/adaptation may be distributed as part of FerriteSolidMechanics.jl under the MIT license, and that he can grant this permission for the relevant CAPRICCIO/VEVP model rights holders.\n\nThe implementation is maintained by Lukas Laubert. Wuyang Zhao is gratefully acknowledged for providing the reference implementation and rheological layout figure, for proofreading this page, and for clarifying details about the model formulation and parameterization.","title":"Provenance and acknowledgements"},{"category":"section","location":"models/vevp_zhao2021/#Main-differences-from-the-reference","page":"VEVP Zhao 2021","text":"The CAPRICCIO/Zhao code-level reference [4] provides a hand-derived analytical stiffness matrix and evaluates the final PK2 stress from the trial branch state SV_trial{j}. The VEVP_Zhao2021_AT variant preserves this trial-state analytical-tangent formulation, but stores its branch tensors in Tensors.jl SymmetricTensor{2,3,Float64,6} form. Consequently, products such as boldsymbolC^*boldsymbolC_textrmij^-1 are symmetrized at the storage boundary. However, the archived Matrix-storage version VEVP_Zhao2021_AT_Matlab and the original MATLAB routines keep the full intermediate matrix. For coaxial or identity branch states this difference is round-off level. For non-coaxial active-flow samples, single-quadrature point checks showed changes up to the second decimal place in off-diagonal PK2 components. Besides, the VEVP_Zhao2021_AD variant differs in three implementation choices:\n\nThe tangent is obtained via automatic differentiation (Tensors.gradient over the PK1 wrapper).\nThe final PK2 stress is evaluated at the converged branch state boldsymbolC_mathrmijn+1.\nSmall determinant and principal stretch floors are applied in the AD path.\n\nThe two variants return different PK2 stresses for the same input; they are not interchangeable.","title":"Main differences from the reference"},{"category":"section","location":"models/vevp_zhao2021/#See-also","page":"VEVP Zhao 2021","text":"Experimental variants, including an archived Matrix-storage AT port: VEVP_Zhao2021_AD_Simplified, VEVP_Zhao2021_AT_Matlab (see Experimental models).","title":"See also"},{"category":"section","location":"models/vevp_zhao2021/#References","page":"VEVP Zhao 2021","text":"[1] Zhao, W., Ries, M., Steinmann, P., & Pfaller, S. (2021). A viscoelastic–viscoplastic constitutive model for glassy polymers informed by molecular dynamics simulations. International Journal of Solids and Structures, 226–227, 111071. doi:10.1016/j.ijsolstr.2021.111071\n\n[2] Reese, S., & Govindjee, S. (1998). A theory of finite viscoelasticity and numerical aspects. International Journal of Solids and Structures, 35(26–27), 3455-3482. doi:10.1016/S0020-7683(97)00217-5\n\n[3] Shutov, A., Landgraf, R., & Ihlemann, J. (2013). An explicit solution for implicit time stepping in multiplicative finite-strain viscoelasticity. Computer Methods in Applied Mechanics and Engineering, 265, 213-225. doi:10.1016/j.cma.2013.07.004\n\n[4] Pfaller, S., Ries, M., Zhao, W., Bauer, C., Weber, F., & Laubert, L. (2024). CAPRICCIO - Tool to run concurrent Finite Element-Molecular Dynamics Simulations (Version 2.0.1) [Software]. Zenodo. doi:10.5281/zenodo.12606758.","title":"References"},{"category":"section","location":"tutorials/adaptive_time_stepping/#Adaptive-time-stepping","page":"Adaptive time stepping","text":"A plane strain quarter plate with a hole, solved with an adaptive outer loop that rejects and retries load steps when a local material update fails. The runnable script is examples/adaptive_time_stepping.jl and can be executed from the repository root with:\n\njulia --project=. examples/adaptive_time_stepping.jl","title":"Adaptive time stepping"},{"category":"section","location":"tutorials/adaptive_time_stepping/#Problem-setup","page":"Adaptive time stepping","text":"The geometry and boundary conditions follow the 2D plate with a hole tutorial. The right edge is displaced outward with the load factor t, while the bottom and left edges enforce symmetry.\n\nThis tutorial focuses on time step-controlling: Instead of prescribing a fixed step size for the whole run, it attempts a step, checks whether both the local material updates and the global Newton loop converges, and, if not succeeding, retries with a smaller dt.","title":"Problem setup"},{"category":"section","location":"tutorials/adaptive_time_stepping/#Grid,-interpolation,-constraints","page":"Adaptive time stepping","text":"The mesh, interpolation, displacement field, and boundary conditions are the same as in the 2D plate with a hole tutorial. This tutorial reuses that setup and changes only the material model and the outer load step loop. For the full source, see Plain program.","title":"Grid, interpolation, constraints"},{"category":"section","location":"tutorials/adaptive_time_stepping/#Material","page":"Adaptive time stepping","text":"We apply VEPD_Detrez2010 through a plane strain wrapper:\n\ndetrez_parameters = (\n    167.0, 0.27, 3.24, 28.6, 20.8,\n    0.59, 41.7, 4.5, 3.58,\n    [101.0, 21.0], [10.0, 100.0],\n)\nmaterial = PlaneStrain(VEPD_Detrez2010(detrez_parameters...))\n\nVEPD_Detrez2010's local plastic return can raise VEPD_Detrez2010ConvergenceError, which subtypes LocalAssemblyFailure. The adaptive time stepping strategy uses this to recover from local failures.","title":"Material"},{"category":"section","location":"tutorials/adaptive_time_stepping/#Assembler","page":"Adaptive time stepping","text":"The assembler is created in the same way as in the fixed-step tutorials:\n\nassembler = create_assembler(material, dh, ch; quadrature_order=2)","title":"Assembler"},{"category":"section","location":"tutorials/adaptive_time_stepping/#Rolling-back-state","page":"Adaptive time stepping","text":"Assembly is stateful: While stiffness_matrix and try_stiffness_matrix evaluate the constitutive response at quadrature points, they store the resulting trial material states in the assembler (at the new dt provided). If a trial step is rejected, those tentative states must be discarded before the same step is retried. To this end, call FerriteSolidMechanics' revert_states! function to restore the assembler's material states to their last committed values. This call works with both serial and MPI assemblers by default:\n\nrevert_states!(assembler)","title":"Rolling back state"},{"category":"section","location":"tutorials/adaptive_time_stepping/#Adaptive-time-stepping-and-Newton-loop","page":"Adaptive time stepping","text":"The outer loop advances from t = 0 to t_end. Each trial step starts from the last accepted displacement u_start and time t_start:\n\nt = 0.0                                           # last accepted time\ndt = 0.25                                         # trial step size\ndt_min = 1e-4                                     # smallest allowed trial step\ndt_max = 0.25                                     # largest step after accepted steps\ndt_shrink = 0.5                                   # factor after rejected steps\ndt_grow = 2.0                                     # factor after accepted steps\ncontroller = TimeStepController(; dt_min, dt_max, shrink=dt_shrink, grow=dt_grow, max_rejections=20) # dt shrink/grow policy after accepted/rejected steps\n\nwhile t < t_end - 1e-12                           # continue until the final time is accepted\n    accepted = false\n    u_start = copy(u)                             # displacement to restore on retry\n    t_start = t                                   # time to restore on retry\n\n    while !accepted                               # retry until accepted\n        t_trial = min(t_start + dt, t_end)        # trial end time, clipped at t_end\n        dt_step = t_trial - t_start               # material time increment\n        update!(ch, t_trial)                      # Dirichlet data at t_trial\n        u .= u_start                              # restore displacement at t_start\n        apply!(u, ch)                             # impose Dirichlet values at t_trial\n\n        newton_converged = false\n        step_rejected = false\n\n        for iter in 1:max_newton                  # Newton iterations for this trial step\n            result = try_stiffness_matrix(assembler, u; dt=dt_step) # returns converged=false on failure\n            if !result.converged                  # recover local material if failed\n                revert_states!(assembler)         # discard trial material updates\n                u .= u_start                      # restore displacement at t_start\n                dt = reject_step!(controller, dt_step) # shrink the attempted step for the retry\n                step_rejected = true\n                break\n            end\n\n            K, r = result.K, result.r             # convergence successful: get tangent/residual\n            apply_zero!(K, r, ch)                 # apply zero-displacement constraints\n            if norm(r) <= newton_tol              # converged global Newton solve\n                newton_converged = true\n                break\n            end\n            u .-= K \\ r                           # Newton correction\n        end\n\n        if newton_converged\n            update_states!(assembler)             # commit trial material states\n            t = t_trial                           # accept the trial end time\n            accepted = true\n            dt = accept_step!(controller, dt_step) # next trial step size\n        elseif !step_rejected\n            revert_states!(assembler)             # discard failed Newton trial states\n            u .= u_start                          # restore displacement at t_start\n            dt = reject_step!(controller, dt_step) # shrink the attempted step for the retry\n        end\n    end\nend\n\nThe user-owned outer loop decides whether a step is accepted, retried, or rejected. TimeStepController supplies a basic scalar grow/shrink policy and rejection budget using accept_step! and reject_step!.\n\nTwo failure modes lead to the same retry path:\n\nA local material failure makes try_stiffness_matrix return converged = false.\nA global Newton failure occurs when the Newton loop reaches max_newton without satisfying newton_tol.\n\nIn both cases, the loop restores the material state, restores u, reduces the attempted dt_step through reject_step!, and retries from the same t_start. Only an accepted step calls update_states!(assembler). The TimeStepController uses both the lower bound dt_min and max_rejections as exhaustion guards. Setting the default value of dt_min = NaN or dt_max = NaN disables the lower- or upper-bound guard, respectively. The max_rejections default is unbounded as well.\n\nFor why this loop calls try_stiffness_matrix instead of stiffness_matrix, see Adaptive time stepping and recoverable local failures.","title":"Adaptive time stepping and Newton loop"},{"category":"section","location":"tutorials/adaptive_time_stepping/#Postprocessing","page":"Adaptive time stepping","text":"After the final accepted step, the example evaluates the stresses:\n\nstresses = compute_stresses(assembler, u)\n\nThe named tuple returned by run_adaptive_time_stepping also stores the accepted times, accepted step sizes, final residuals, iteration counts, rejected-step count, and external counters for how often dt actually decreased or increased. The script further prints a short summary:\n\nSolved adaptive time stepping quarter plate\n  cells: 48\n  dofs: 130\n  accepted steps: 8\n  rejected/retried steps: 7\n  dt decreases: 7\n  dt increases: 5\n  peak displacement: 0.12\n  dt range over accepted steps: [0.015625, 0.25]\n\nThe accepted dt values show which step sizes were eventually accepted. The dt_decreases and dt_increases counters are tracked by the tutorial's code, not by TimeStepController. dt_increases counts only accepted steps where accept_step! returns a larger dt; a step already capped at dt_max is not counted as an increase. Similarly, dt_decreases counts only successful calls to reject_step!; if the next step would fall below dt_min, reject_step! throws before returning.","title":"Postprocessing"},{"category":"section","location":"tutorials/adaptive_time_stepping/#Swapping-the-material-model","page":"Adaptive time stepping","text":"The adaptive loop can drive any material model that implements the AbstractMaterial interface. To swap the material, change only the material constructor; the assembler, rollback, and adaptive loop stay the same:\n\nmaterial = PlaneStrain(VEPD_Detrez2010(detrez_parameters...))\nmaterial = PlaneStrain(J2Plasticity(70e3, 0.3, 250.0, 1e3))\nmaterial = PlaneStress(VEVP_Zhao2021_AD(5.0, 20.0, 6, 0.0, 0.0, 283.0, 18.51, 0.1421, 0.6152, 3.0, 3.0, 0.0, 0.0, 283.0))\n\nMaterials with robust local updates may never reject a step. The same driver still works; upon reaching dt_max, it simply behaves like a fixed-step loop unless a local update or global Newton iteration fails.","title":"Swapping the material model"},{"category":"section","location":"tutorials/adaptive_time_stepping/#Plain-program","page":"Adaptive time stepping","text":"The full runnable source is rendered from examples/adaptive_time_stepping.jl:\n\nusing Markdown\nimport FerriteSolidMechanics\npath = joinpath(pkgdir(FerriteSolidMechanics), \"examples\", \"adaptive_time_stepping.jl\")\nMarkdown.parse(\"```julia\\n\" * read(path, String) * \"\\n```\")","title":"Plain program"},{"category":"section","location":"concepts/#Concepts","page":"Concepts","text":"This page explains the structure of the assembler, the behavior of public functions, and the interface design. We recommend reading this page before implementing a custom material.","title":"Concepts"},{"category":"section","location":"concepts/#The-GenericMaterialAssembler","page":"Concepts","text":"The core architecture is implemented in src/GenericMaterialAssembler.jl. The function create_assembler returns a GenericMaterialAssembler struct, which stores and preallocates core buffers to minimize allocations during the Newton solve:\n\nField (selected) Purpose\nK_linear Preassembled global linear stiffness matrix (only nonzero if any material has is_linear == true)\nK_tangent Preallocated buffer for the global tangent stiffness matrix (refilled every call to stiffness_matrix)\nr Preallocated buffer for the global internal residual vector\nmaterials, cell_to_mat_idx The list of materials and the mapping cellid → material index\nlinear_cells, nonlinear_cells Sorted partitions of cell ids by is_linear(material)\nowned_nonlinear_cells The subset of nonlinear_cells assigned to this MPI rank (mod(cellid-1, nranks) == rank)\nstates One quadrature point state vector per owned nonlinear cell (entries for linear or non-owned cells are unassigned)\ndh, ch, ah The closed Ferrite DofHandler, ConstraintHandler, and optional custom alpha-value source\nquadrature_orders Per-sub-dof-handler quadrature order\nthickness Scalar multiplier applied to the local element stiffness ke and residual re in the element loop\n_sdh_* Per-sub-dof-handler metadata: cell-to-sdh index, CellValues, nquadpoints, ndofs_per_cell, owned-cell indices\n_cell_ke, _cell_re Per-owned-cell caches for the local element stiffness matrices and residual vectors, used by the two-phase assembly\n_stresses_cache, _stresses_mpi_buffer Reusable output buffers for compute_stresses (callers should copy the returned stress field before the next evaluation)","title":"The GenericMaterialAssembler"},{"category":"section","location":"concepts/#Task-local-workspaces","page":"Concepts","text":"The sparse matrices, per-cell state, stress cache, task-owned workspaces, and per-cell result buffers are created in create_assembler. The parallel loops hand each spawned task a reference to one of these preallocated workspaces for element-local scratch storage. Individual materials can allocate inside their material_response (or custom _assemble_element!) methods; allocate_material_cache provides a per-element scratch object to reuse buffers across quadrature points. This is particularly relevant when using automatic differentiation or temporary tensor objects.","title":"Task-local workspaces"},{"category":"section","location":"concepts/#Multi-material-cellset-coverage","page":"Concepts","text":"When create_assembler is called with a dictionary of materials (Dict{String, <:AbstractMaterial}), the string keys must correspond to named cellsets in the underlying grid. These cellsets must collectively cover all active cells of the DofHandler exactly once. Inactive grid cells within those cellsets are simply ignored. However, active cells that are missing entirely, or assigned to multiple material cellsets, will trigger an error during assembler construction.\n\nFor example:\n\n# The grid has cellsets defining the two domains\naddcellset!(grid, \"steel\", Set(1:100))\naddcellset!(grid, \"rubber\", Set(101:200))\n\n# The dictionary keys match the cellset names exactly\nmaterials = Dict(\n    \"steel\"  => PlaneStrain(J2Plasticity(200e3, 0.3, 250.0, 1e3)),\n    \"rubber\" => PlaneStrain(MooneyRivlin(1e3, 0.2e3, 10e3))\n)\n\nassembler = create_assembler(materials, dh, ch)","title":"Multi-material cellset coverage"},{"category":"section","location":"concepts/#Linear-and-nonlinear-cells","page":"Concepts","text":"Each material reports is_linear(material). Linear materials have a displacement-independent tangent. Their cell IDs are collected in the assembler's linear_cells vector, and the element stiffness is precomputed once during create_assembler. On every subsequent stiffness_matrix call, the linear part is added to the tangent without recomputation.\n\nK_tangent = K_nonlinear(u) + K_linear\nr         = r_nonlinear(u) + K_linear * u\n\nNonlinear materials include any model with internal state, history dependence, or displacement-dependent stiffness. The cells assigned to nonlinear materials are stored in nonlinear_cells, while the corresponding element tangent and residual are recomputed during every Newton iteration. The quadrature point state is owned and updated by the rank hosting the cell.\n\nEvery stiffness_matrix call on a nonlinear cell runs _assemble_element!. Inside the element routine, the material model is evaluated at every quadrature point. If this involves a return mapping, local Newton solve, or AD tangent, this cost is paid on each global Newton iteration. The values written during this call are trial values; they are committed only later through update_states!.\n\nMaterials normally do not implement _assemble_element! themselves: they implement the quadrature point hook material_response(material, ε_or_F, state, dt, cache) and declare their strain measure via kinematics(material) (SmallStrain() or FiniteStrain()). A generic element routine then owns the quadrature loop, the integration weights (including alpha scaling), the weak-form contractions into ke (element stiffness) and re (element residual), and the trial-state bookkeeping (set_trial!). _assemble_element! remains the stable element-level extension hook for formulations whose element structure is material-specific – the bundled VEVP_Zhao2021_AT assembles its hand-derived tangent from nodal shape-gradient blocks and is the canonical example. Material-specific kinematics, state updates, wrapper condensation, and local solves stay in the material's material_response, while all repeated finite element algebra is shared.","title":"Linear and nonlinear cells"},{"category":"section","location":"concepts/#The-element-loop-in-two-phases","page":"Concepts","text":"The element assembly orchestrated by stiffness_matrix (and internally _assemble_stiffness_local!) consists of two phases to parallelize the element computations while keeping the global matrix assembly sequential:\n\nPhase 1 (_stiffness_sdh_batch!): each task computes (elementwise) ke and re into a task-local workspace, then copies them into _cell_ke[idx] / _cell_re[idx].\nPhase 2: a single sequential loop scatters every _cell_ke[idx], _cell_re[idx] into the global matrix and vector via assemble!.\n\nPhase 1 is parallelized across threads using OhMyThreads.@tasks, and the element workload is distributed across MPI ranks. See the Threading and MPI section below for implementation details.","title":"The element loop in two phases"},{"category":"section","location":"concepts/#State-management","page":"Concepts","text":"Material models with internal history implement a trial/commit pattern on their state object.\n\n            [Start Newton Iteration] ◄──────────────┐\n                        │                           │\n                        │ (try_)stiffness_matrix    │\n                        │  ↳ material_response      │\n                        ▼                           │\n             [Trial / Current State]                │\n                 ¦             ▲                    │\n   update_state! ¦             ¦ revert_state!      │\n(step converged) ¦     OR      ¦ (step failed) ─────┤ (retry step with smaller dt)\n                 ▼             ¦                    │\n          [Committed / Previous State] ─────────────┘ (next load step)\n\ncreate_state(mat) returns a new quadrature point state. For history-dependent materials it initializes trial and committed history to the same values.\nDuring a Newton step, assembly (called by (try_)stiffness_matrix) writes trial values into the current fields at every quadrature point – the generic element routine stores the trial returned by material_response via set_trial!; custom _assemble_element! methods write it directly. The committed state is never modified by assembly, so it is always safe to revert from if a step fails.\nIf the step converges, the outer loop calls update_states!(assembler), which dispatches to update_state! on every quadrature point state of every owned nonlinear cell, and with that commits the trial state. The assembler inspects the return value: if the material model's update_state! returns nothing (the standard for mutable struct states), it assumes the state was updated in-place. If it returns a new AbstractMaterialState (required for immutable struct states), the assembler overwrites the old state in the state vector with the new one.\nIf the step must be rejected, revert_states!(assembler) restores the last committed history for all owned states. This wipes out all values left in the trial state from the failed iterations, ensuring the next attempt (e.g. with a smaller dt) starts from the previously committed state.\ncompute_stresses is postprocessing. Stateful materials read from their current/trial state fields to calculate the final stress, without mutating or advancing the history.\n\nThe shape of the trial and committed history is up to the material. Some models use current/previous structs (e.g. J2PlasticityState, VEVP_Zhao2021_ATState, VEVP_Zhao2021_ADState); others use named fields such as current_Fp/previous_Fp (VEPD_Detrez2010) or current_Fvp/previous_Fvp (VEVP_MOAMMM). update_state!/revert_state! implementations typically delegate to copy_state!(dest, src) for the actual copy (which evaluates to a value assignment for immutable types or a deep copy for arrays). This bundled FerriteSolidMechanics.copy_state! is a default fallback that handles scalars, immutable types, and arrays recursively.","title":"State management"},{"category":"section","location":"concepts/#Threading-and-MPI","page":"Concepts","text":"Threading. OhMyThreads.@tasks with a :greedy scheduler is used to parallelize the stiffness and stress element loops. OhMyThreads.@local binds each spawned task to a unique preallocated AssemblyWorkspace, so task migration in Julia 1.10/1.11 cannot make two tasks share element buffers, and the Newton loop does not reallocate those buffers. The number of spawned assembly tasks is capped to the number of preallocated workspaces; if that invariant is ever violated, an error is thrown instead of reusing a workspace. update_states! uses Threads.@threads because each cell state is owned by exactly one loop iteration.\nMPI. Cells are partitioned by mod(cellid-1, nranks) == rank for linear, nonlinear, and stress-output work. After local assembly, the per-rank K_tangent.nzval, K_linear.nzval, residual vector, and stress cache are summed across ranks. stiffness_matrix and compute_stresses are therefore collective calls that every rank must reach, including on failure: a local failure on one rank is made collective before it propagates, so peer ranks throw instead of blocking in the reduction. All mpi_allreduce! calls are no-ops when nranks == 1. The current MPI path replicates the global sparse matrices, residual vector, and stress cache on every rank; MPI distributes the cell-level constitutive work but not the global linear algebra storage or the sparse linear solve.\n\nThreading scales automatically with the Julia thread count. MPI support is enabled automatically whenever MPI.Init() has been called in the user's script.","title":"Threading and MPI"},{"category":"section","location":"concepts/#Dimensionality-wrappers","page":"Concepts","text":"The generic PlaneStrain and PlaneStress wrappers embed the 2D deformation gradient into 3D, call the 3D constitutive update, and extract the in-plane parts. Wrappable 3D materials expose compute_PK1_3D(material, F, dt, state) and update_state_from_3D!(state, material, F, dt). The second hook writes the current/trial state for the converged embedded 3D deformation; the final commit still happens through update_states!. See the Wrappers page for the exact embedding, static-condensation, and local-Newton math. Some materials provide custom wrapper routes: Ogden follows the same public wrapper API but uses an exact-tangent wrapper assembly, while VEVP_Zhao2021_AT supports PlaneStrain through an analytical tangent path but not PlaneStress (this would require derivation and implementation of another matching 2D tangent).","title":"Dimensionality wrappers"},{"category":"section","location":"concepts/#Material-constructor-conventions","page":"Concepts","text":"Every exported material constructor follows these conventions:\n\nMaterial parameters are passed as positional arguments.\nBehavior options – integration modes, time-unit scalings, tangent types – are passed as keyword arguments with sensible defaults. Changing a keyword argument changes how the model evaluates its response, not what material it represents.\n\n# VEPD_Detrez2010: material parameters positional, integration modes are kwargs\nmat1 = VEPD_Detrez2010(E, ν, R0, Q, b, α, β, n_ab, μ_ab, G, τ;\n                 plastic_update=:end_step, maxwell_update=:closed_form_cv)\n\n# VEVP_Zhao2021_AD / VEVP_Zhao2021_AT: material parameters positional, dt_scale is a kwarg\nmat2 = VEVP_Zhao2021_AD(μE, κ, NV, β, α, μVe, μVN, τ̂₁, τ̂N, m₁, mN, τcut₁, τcutN, μV;\n                 dt_scale=1.0)\n\n# MooneyRivlin: material parameters positional, tangent evaluation type is a kwarg\nmat3 = MooneyRivlin(C10, C01, κ; tangent=:AD)\n\n# PlaneStress wrapper: mat1 is the base model, local-Newton controls are kwargs\nmat4 = PlaneStress(mat1; tol=1e-10, maxiter=20)\n\nMaterial model authors are encouraged to follow these conventions as outlined in the Developer guide.","title":"Material constructor conventions"},{"category":"section","location":"concepts/#The-dt-keyword","page":"Concepts","text":"The public assembly functions (stiffness_matrix, try_stiffness_matrix, compute_forces) and the postprocessing function (compute_stresses) all accept dt as a keyword argument. However, only the assembly functions are allowed to advance material history.\n\nRate-independent materials ignore dt (see the Stable models table).\nViscoelastic / viscoplastic materials use dt to evolve internal variables (viscous stretches, plastic flow, cumulative strain, etc.). Passing dt=0.0, however, does not lead to obtaining a rate-independent model behavior; it only freezes the (viscous) state evolution.\n\nstiffness_matrix and compute_forces pass dt into the element update and write trial state. compute_stresses is a stress-output pass: for the bundled stateful materials it evaluates stresses using the state currently held in the assembler (whether that is an uncommitted trial state during debugging, or the committed state during standard postprocessing) without taking another constitutive time step. Typical postprocessing calls compute_stresses(assembler, u) and uses the default dt = 0.0. Postprocessing accepts dt so that custom rate-dependent materials have access to the time increment if their stress formula requires it, but their stress routines must still remain non-evolving and not commit state.\n\ninfo: Time units (`dt_scale` kwarg)\nSome models take a dt_scale keyword that multiplies dt before the constitutive update runs. Set it to a value that matches the time unit used during parameter calibration (1.0 if dt already uses that unit, 1e-9 when the driver passes nanoseconds but the calibration used seconds, etc.):mat = VEVP_Zhao2021_AD(μE, κ, NV, β, α, μVe, μVN, τ̂₁, τ̂N, m₁, mN, τcut₁, τcutN, μV;\n                 dt_scale = 1e-9)Check the individual material model pages to see if this keyword is allowed, e.g., the Zhao model page.","title":"The dt keyword"},{"category":"section","location":"concepts/#Adaptive-time-stepping-and-recoverable-local-failures","page":"Concepts","text":"Some nonlinear local updates can fail before the global Newton loop recovers. Examples include the plastic return in VEPD_Detrez2010, the Perzyna solve in VEVP_MOAMMM, and the local plane stress condensation in PlaneStress. These exceptions are thrown as subtypes of LocalAssemblyFailure. Calling try_stiffness_matrix instead of stiffness_matrix turns those failures into a result object that an outer time step controller can inspect (see Adaptive time stepping):\n\nresult = try_stiffness_matrix(assembler, u_trial; dt=dt_step)\nif !result.converged\n    # Reject this load/time step and retry with a smaller dt_step.\n    @info \"Reducing time step after local assembly failure\" error=result.error\nend\n\ntry_stiffness_matrix catches only recoverable LocalAssemblyFailure exceptions. Other errors are still rethrown to expose bugs. In multithreaded assembly, try_stiffness_matrix can safely recover from thread errors, provided they are caused exclusively by LocalAssemblyFailure exceptions. On failure, the assembler's matrix and residual buffers hold partial results, but they are re-zeroed by the next assembly call and need no manual reset. Inspect result.error to see what went wrong, and call revert_states! before retrying the step to roll back the trial material history written by the failed attempt. In MPI runs, failure states are synchronized across all ranks to prevent deadlocks. If one rank fails, every rank receives converged = false; consequently, ranks without the original local exception also show a RemoteAssemblyFailure in result.error.\n\nA typical adaptive skeleton looks as follows:\n\nt = 0.0\nu = zeros(ndofs(dh))\ncontroller = TimeStepController(; dt_min, dt_max, max_rejections=20)\ndt = 0.25\n\nwhile t < t_end\n    accepted = false\n    u_start = copy(u)\n    t_start = t\n\n    while !accepted\n        t_trial = min(t_start + dt, t_end)\n        dt_step = t_trial - t_start\n        update!(ch, t_trial)\n        u .= u_start\n        apply!(u, ch)\n        # Construct specific time-dependent external load variables for t_trial here.\n\n        newton_converged = false\n        step_rejected = false\n        for _ in 1:max_newton\n            result = try_stiffness_matrix(assembler, u; dt=dt_step)\n            if !result.converged\n                revert_states!(assembler)\n                u .= u_start\n                dt = reject_step!(controller, dt_step)\n                step_rejected = true\n                break\n            end\n\n            K, r = result.K, result.r\n            apply_zero!(K, r, ch)\n            if norm(r) < newton_tol\n                newton_converged = true\n                break\n            end\n            u .-= K \\ r\n        end\n\n        if newton_converged\n            update_states!(assembler)\n            t = t_trial\n            accepted = true\n            dt = accept_step!(controller, dt_step)\n        elseif !step_rejected\n            revert_states!(assembler)\n            u .= u_start\n            dt = reject_step!(controller, dt_step)\n        end\n    end\nend\n\nCall update_states! only after an accepted, converged outer step. If an assembly attempt or Newton step is rejected, restore the quadrature states with revert_states!(assembler) before retrying from the last accepted displacement and time. A failed local update may already have written trial state before throwing; rollback is therefore mandatory even though no global step was accepted. Keep t_start and u_start fixed while retrying a rejected step; only dt, t_trial, boundary conditions, and time-dependent loads should change. Pass dt_step, the actually attempted increment after final-time clipping, to accept_step! and reject_step!. TimeStepController supplies the shrink/grow policy, optional dt_min/dt_max bounds, and a maximum consecutive rejection count. Use dt_min together with max_rejections when a permanently failing step must not retry forever; dt_min = NaN disables only the lower-bound guard. When the controller cannot shrink further, reject_step! throws TimeStepControllerExhausted. In MPI, every rank must participate in the same sequence of try_stiffness_matrix calls and reject the step collectively when converged == false.","title":"Adaptive time stepping and recoverable local failures"},{"category":"section","location":"concepts/#What-the-package-does-*not*-do-on-its-own","page":"Concepts","text":"It does not set up DofHandler or ConstraintHandler – that is the user's job using Ferrite.jl syntax.\nIt does not run a Newton loop – stiffness_matrix or try_stiffness_matrix takes one (u, dt) and returns one (K, r), i.e. the tangent and the internal force; the user tests the convergence.\nIt does not integrate in time – the user owns the time loop, the time step size, and any accepted/rejected-step policy.\nIt does not apply Dirichlet boundary conditions – the user calls Ferrite's apply!(u, ch) and apply_zero!(K, r, ch) around the solve.\nIt does not form the residual – the user subtracts the external force from the internal one: residual .= f_int .- f_ext.\n\nFor complete loops, see the fixed-step Newton solve in 2D plate with a hole and the adaptive time stepping driver in Adaptive time stepping. For assembling f_ext, see External loads.","title":"What the package does not do on its own"},{"category":"section","location":"performance/#Performance-and-parallel-execution","page":"Performance and parallel execution","text":"This page covers the settings that decide how fast a simulation runs: which linear solver to use, how many MPI ranks and Julia threads to launch, and how many BLAS threads to use for solving.","title":"Performance and parallel execution"},{"category":"section","location":"performance/#Recommendations","page":"Performance and parallel execution","text":"recommended_solve_settings returns the recommended solver configuration for a given grid and node:\n\ns = recommended_solve_settings(dh; cores_per_node=72, memory_per_node_gb=256, mumps_available=true)\nLinearAlgebra.BLAS.set_num_threads(s.blas_threads)\n\nWe recommend to verify critical thresholds on your hardware using the procedure in Reproducing these on your cluster. The following summarized recommendations are based on the measurements in the bottom part of this page. The numbers below are for linear (Q1) elements; see How measurements were taken for scope.\n\nSolver: Use distributed_solve for 3D problems (faster at every size measured, up to 10.2× at 273k DOFs). Without MUMPS, K \\ residual on a single rank remains viable in 3D. Use K \\ residual for 2D problems, where it led at every size measured up to 4.0M DOFs; above ~500k its lead is 1.14× to 1.31× and available memory determines the choice. See Choosing the linear solver.\nRank and thread layout: For distributed_solve, use 18–36 MPI ranks with 2–4 Julia threads per rank. For K \\ residual in 3D, use a single rank with all cores as Julia threads. In 2D with K \\ residual, use 4–36 ranks for cheap materials, where all layouts are within 8% of each other, and 18–36 ranks for expensive materials, where the spread reaches 1.5×. See Rank and thread layout.\nBLAS threads: Set to 1 in all configurations except single-rank 3D K \\ residual, where BLAS threading pays (measured at 36 threads on a 72-core node). Multi-threaded BLAS under distributed_solve caused a 6.3× slowdown. In 2D, BLAS threading degrades performance at every thread count tested. See BLAS threads.\nMemory: Under K \\ residual, multiply estimated_replicated_memory_gb by the ranks per node and compare against node RAM, or read the fits field of recommended_solve_settings, which does exactly that. Both are scaled to NeoHooke over three load steps; hence fits budgets 2.6× for a more expensive material on a longer run. An expensive model (VEVP_Zhao2021_AT measured 9×) or a long run (NeoHooke measured 2.5× over 48 load steps) reaches or exceeds that budget on its own, so measure yours before committing to a large job. See Memory.\nLarge meshes: In 2D, 16M DOFs solve on one node in 154 s at 95 GB, with the rank count lowered so the replicated copies fit. In 3D, the factorization outgrows the memory of 8 nodes somewhere above 1M DOFs. See Large meshes.\nMulti-node scaling: Stay on a single node while memory permits; the best single-node layout beat every multi-node layout measured, at 273k and at 985k DOFs in 3D. Expand only when factorization memory exceeds single-node capacity. See Scaling across multiple nodes.\nLinear elastic models: Hooke and Hooke2D are purely solve-bound (element matrices are assembled once). Use a single rank with BLAS multithreading. See Assembly cost across material models.\n\nThe sections below present the measurements behind these recommendations.","title":"Recommendations"},{"category":"section","location":"performance/#How-measurements-were-taken","page":"Performance and parallel execution","text":"All benchmarks were measured on FerriteSolidMechanics.jl v0.1.0 on the NHR@FAU Fritz cluster (72 cores and 256 GB RAM per node, Open MPI 5.0.8, Julia 1.12): NeoHooke was used for 3D solver benchmarks, while VEVP_Zhao2021_AD was used for 2D benchmarks and assembly-cost comparisons. Every grid uses linear (Q1) elements at quadrature_order=2: hexahedra in 3D, quadrilaterals in 2D. Higher-order elements were not measured; at equal DOF count they change the sparsity of K, so timings and memory fits need recalibration (see Reproducing these on your cluster). Runs cover three load steps unless stated otherwise.\n\nEach figure is the median of its repeats, each submitted as a separate job and timed after a full warm-up Newton solve. The solver tables below list each row's repeats in the repeats column, K \\ residual first and distributed_solve second (split by a /); the remaining tables cover two or three repeats, and the 16M and 985k meshes ran once. Most runs above 20 s varied by up to 8% across repeats; the small-grid measurements in the tables below varied by 18–45%, and more when one repeat was an outlier. Hence, read those as indicative. The multi-node layouts in Scaling across multiple nodes are the exception among the long runs; their spread is reported within the table.\n\nWhich solver is faster follows from solver architecture, and that ordering held across every problem size and material model measured, so the general rules likely apply to other clusters as well. However, the exact problem size (DOF or element count) where one solver overtakes the other – and the exact memory required per rank – depend on your network hardware, BLAS build, and element type. See Reproducing these on your cluster to measure it for your setup.","title":"How measurements were taken"},{"category":"section","location":"performance/#Choosing-the-linear-solver","page":"Performance and parallel execution","text":"Every rank assembles its own cells, then the tangent is reduced so that all ranks hold the full K. The two solvers differ in how the linear solve is performed:\n\nK \\ residual: every rank solves the whole system independently, allocating memory for a complete matrix factorization.\ndistributed_solve (MUMPS): the ranks split one solve between them, so each stores only a fraction of the factorization. Requires import MUMPS, and communicates between ranks during factorization.\n\nIn 3D, distributed_solve was faster at every size measured when comparing each solver in its optimal combination of MPI ranks and threads:\n\n3D grid K \\ residual (1 rank × 72 threads, BLAS 36) distributed_solve (18 ranks × 4 threads, BLAS 1) distributed_solve faster by repeats\n2.2k DOFs 1.4 s 1.2 s 1.11× 8 / 8\n15k DOFs 9.8 s 5.8 s 1.7× 8 / 8\n47k DOFs 37.7 s 11.8 s 3.2× 4 / 13\n108k DOFs 145 s 25.0 s 5.8× 5 / 4\n273k DOFs 634 s 62.3 s 10.2× 2 / 5\n\nThese are whole-run wall times, covering element assembly and every Newton solve. Per solve, the 3D performance gain is larger (3.2× at 47k DOFs and 3.5× at 73k DOFs). At 2.2k DOFs, K \\ residual is faster per solve (0.107 s vs 0.131 s), but the multi-rank MPI layout completes element assembly faster than the single-rank threaded layout, making distributed_solve faster overall. That 2.2k row covers eight repeats per solver whose individual runs overlap, 1.22 to 1.53 s replicated against 1.08 to 1.60 s distributed, so treat the ordering there as weak. The 273k-DOF case ran fastest at 36 ranks × 2 threads, ahead of 18 ranks × 4 threads, which measured 81 to 105 s over three repeats.\n\nPer solve, the two solvers cross at approximately 3k DOFs in 3D. Below that threshold, execution times are nearly identical, making K \\ residual preferable due to its simpler setup without external dependencies. In 2D, K \\ residual was faster at every size measured:\n\n2D grid K \\ residual distributed_solve K \\ residual faster by repeats\n8k DOFs 2.0 s 4.6 s 2.3× 9 / 9\n291k DOFs 51.6 s 61.6 s 1.19× 9 / 5\n516k DOFs 96.0 s 112 s 1.16× 7 / 5\n1.0M DOFs (Hooke, one load step) 7.4 s 8.8 s 1.19× 2 / 2\n2.0M DOFs (Hooke) 52.6 s 59.7 s 1.14× 5 / 5\n4.0M DOFs (Hooke, one load step) 30.2 s 39.5 s 1.31× 2 / 2\n\nThe gap narrows with problem size, from 2.3× at 8k DOFs to 1.14–1.19× between 516k and 2.0M, and it stays in favour of K \\ residual throughout. The Hooke rows use the linear elastic model, whose element matrices are assembled once, so those runs spend almost all their time on the linear solve. They also run at the rank count that keeps the total memory of the replicated copies within the node's RAM: 9 ranks at 1.0M DOFs and 4 at both 2.0M and 4.0M. The 1.0M and 4.0M rows ran one load step and the 2.0M row the default three, so these rows are not a scaling series: 4.0M only looks faster than 2.0M because it ran a third of the steps. The warm-up matters most at 4.0M: K \\ residual reuses its symbolic factorization across Newton iterations and ran 30.2 s with the warm-up against 50.8 s without it, while distributed_solve re-analyses on every call and measured 39.3 s with the warm-up against 39.6 s without. Above roughly 500k DOFs the difference is lower than a third either way, so memory consumption becomes the primary selection criterion.\n\nRule of thumb: use distributed_solve for 3D problems above ~4k..20k DOFs, and K \\ residual for 2D problems, which led at every size measured up to 4.0M DOFs and was the only solver to complete at 16M (see Large meshes).","title":"Choosing the linear solver"},{"category":"section","location":"performance/#Memory","page":"Performance and parallel execution","text":"Available RAM is a strict execution limit; jobs exceeding node memory are terminated by the operating system.\n\nWhen using K \\ residual, each rank constructs its own complete factorization of K, which requires significantly more memory than K itself. Accumulated across ranks, these duplicate factorizations are the primary driver of node memory usage.\n\nestimated_replicated_memory_gb predicts the peak memory required for a single factorization copy. Factorization memory grows superlinearly. Fitted to these measurements, it scales as mathcalO(textdofs^143) in 3D and mathcalO(textdofs^109) in 2D. A sparse direct factorization on a regular grid is expected to scale as mathcalO(textdofs^43) in 3D and mathcalO(textdofs log textdofs) in 2D, so the exponents should transfer to other clusters even where the prefactors do not. Doubling the DOFs of a 3D mesh increases memory requirements by approximately 2.7×, meaning a mesh refinement can easily exceed available node RAM:\n\n3D DOFs 47k 108k 273k 556k\nK \\ residual, per rank 7.2 GB 20.7 GB 62.5 GB 179 GB\ndistributed_solve, per rank 1.5 GB 2.1 GB 3.4 GB –\n\nThe predicted peak memory stays within 15% of every measured point, over 2.2k to 556k DOFs in 3D and 8k to 2.0M in 2D. Memory still grows as mathcalO(textdofs^109) an order of magnitude beyond that range, as Large meshes shows.\n\nThis leads to two practical implications for job planning.\n\nRunning multiple ranks multiplies total memory usage under K \\ residual. J2Plasticity at 47k DOFs measured ~10 GB per rank, so 36 ranks would require ~360 GB and therefore not survive on a 256 GB node.\n\nestimated_replicated_memory_gb is scaled to NeoHooke over three load steps. Two factors raise the peak memory of a rank above this estimate:\n\nThe material, at the same number of load steps: measurements showed 0.7× (Hooke) to 1.5× (VEVP_MOAMMM) of NeoHooke. They differ in history state per quadrature point and in Newton iterations per load step.\nThe number of load steps: NeoHooke at 47k DOFs measured 7.1 GB over three load steps, 9.3 GB over six (1.3×) and 11.0 GB over twelve (1.55×), then 13.4 and 16.3 GB in two repeats at 24 steps and 14.0 and 18.7 GB at 48. Peak memory keeps growing with the run, and repeats of the same run differ by up to 1.3×, because the recorded peak depends on when the garbage collector happens to run.\n\nTogether the two factors reach 2.6× in VEVP_MOAMMM over twelve load steps, which measured 17.1 GB against the 6.6 GB estimated, and recommended_solve_settings allows exactly this 2.6× in its fits field. Either factor reaches that budget on its own: NeoHooke measured 2.5× over 48 load steps, and VEVP_Zhao2021_AT 9× over three, at 59.8 GB against the 6.6 GB estimated. Based on the tests conducted, the two also stop compounding once the run is longer: at six and twelve load steps VEVP_MOAMMM measured 1.5× above NeoHooke, but at 24 load steps only 15.2 GB against 14.9 GB. The budget is deliberately pessimistic: one estimate per rank marked configurations as fitting that were then killed for running out of memory. Because of the variations found, measure the memory of a long run, or of a model with many Newton iterations per load step, directly.\n\nFor 2D systems, memory limits become relevant before 1M DOFs. At 804k DOFs on 18 ranks, K \\ residual exceeded 256 GB node RAM, whereas distributed_solve completed using 3.3 GB per rank.\n\nAbove ~500k DOFs in 2D, total memory capacity determines solver selection. At that scale K \\ residual measured at most 1.31× faster, so choose based on which solver fits in node RAM.\n\nThere are two ways to keep the memory within node RAM, and which one is better changes with problem size:\n\nK \\ residual: every rank allocates its own full factorization copy (e.g., 18 ranks store 18 copies on the node), so lowering the rank count lowers the node's total memory in proportion.\ndistributed_solve: splits one factorization across the ranks, so each rank's memory stays nearly constant as ranks are added, though the node's total memory still grows because each rank also keeps its own copy of K and its own MUMPS workspace: at 985k DOFs in 3D it measured 8.6 GB per rank on 36 ranks (2 nodes), 8.1 GB on 72 (4 nodes) and 7.9 GB on 144 (8 nodes), which is 310 GB, 585 GB and 1138 GB across the nodes.\n\nThat per-rank copy of K sets the ceiling in very large problems, where it is no longer negligible next to the factorization: at 16M 2D DOFs it is 4.3 GB per rank, so replicating it across 18 ranks already exceeds a node's RAM, while a single replicated rank completes the same problem (see Large meshes). Below a few million DOFs the factorization dominates, so distributed_solve is the memory-efficient choice; above that, reducing the rank count saves more memory.\n\nThe 2D recommendation is therefore to use K \\ residual while all rank copies fit in node RAM, lowering the rank count as the mesh grows, and to switch to distributed_solve when the factorization no longer fits. To verify memory fit, evaluate estimated_replicated_memory_gb for your grid, multiply by the ranks per node, and check against total node RAM. recommended_solve_settings performs this check automatically via the fits field, for the K \\ residual path, and at the fixed cores ÷ 8 rank count only – it does not try the other layouts measured above.","title":"Memory"},{"category":"section","location":"performance/#Rank-and-thread-layout","page":"Performance and parallel execution","text":"Given a fixed core count, MPI ranks parallelize element assembly significantly better than Julia threads. Total element assembly time over a 2D run at 291k DOFs across a 72-core node illustrates this trend (a layout like 18 × 4 reads as 18 MPI ranks with 4 Julia threads each) (assembly is solver-independent, so these hold for both solvers):\n\nLayout 1 × 72 4 × 18 9 × 8 18 × 4 36 × 2\nAssembly, NeoHooke 22.1 s 2.09 s 0.65 s 0.44 s 0.45 s\nAssembly, VEVP_Zhao2021_AD 213 s 43.8 s 22.6 s 12.0 s 7.3 s\n\nEach MPI rank manages its own heap and garbage collection independently, whereas Julia threads within a process share a single heap and GC.\n\nConversely, K \\ residual scales worse with more ranks because every rank repeats the full linear solve. In 3D, where the linear solve dominates total runtime, this effect is critical (J2Plasticity at 47k DOFs, BLAS 1 throughout):\n\nLayout 1 × 72 4 × 18 9 × 8 18 × 4 36 × 2\nK \\ residual 166 s 148 s 167 s 197 s out of memory\ndistributed_solve 88.9 s 43.9 s 36.9 s 31.7 s 27.1 s\n\nBecause the two solvers exhibit opposite scaling behavior, optimal rank-to-thread allocation depends on whether assembly or solving dominates:\n\nSituation Layout on a 72-core node\ndistributed_solve 18–36 ranks × 2–4 threads, BLAS 1\nK \\ residual, 3D 1 rank × 72 threads, BLAS 36–72\nK \\ residual, 2D, cheap material 4–36 ranks × 2–18 threads, BLAS 1\nK \\ residual, 2D, expensive material 18–36 ranks × 2–4 threads, BLAS 1\n\nThe 2D wall times at 291k DOFs across the same 72-core layouts (K \\ residual) were:\n\nLayout 1 × 72 4 × 18 9 × 8 18 × 4 36 × 2\nNeoHooke 34.4 17.9 17.5 16.8 16.6\nVEVP_Zhao2021_AD 251 82.3 65.2 51.6 42.8\n\nSolve time varies by at most 1.4× across this sweep while assembly varies by 29×, so the assembly share drives the layout selection. For a cheap material the four multi-rank layouts lie within 8% of each other, so any of them seems like a reasonable choice.\n\nEach added rank also holds its own factorization copy, and that limit lies well inside the range the table above recommends. The table below repeats the VEVP_Zhao2021_AD sweep on two further meshes, where memory becomes the limit:\n\n2D DOFs 9 × 8 18 × 4 36 × 2\n130k 30.1 s 25.1 s 21.3 s\n516k 127 s 95.6 s out of memory\n\nAt 130k DOFs the highest rank count is 1.18× ahead of 18 ranks × 4 threads, and at 516k the same layout is killed on a 256 GB node while 18 ranks run at 8.2 GB each. Check memory before choosing the highest rank count: the maximum number of ranks that fit in node memory decreases as the mesh grows.\n\nAt small problem sizes, prefer having ranks over threads: at 1.5k DOFs, 4 ranks took 0.42 s compared to 1.20 s for 1 rank, whereas adding a second thread per rank raised the four-rank time to 0.76 s due to thread overhead and load imbalance.","title":"Rank and thread layout"},{"category":"section","location":"performance/#BLAS-threads","page":"Performance and parallel execution","text":"recommended_blas_threads provides the recommended BLAS thread configuration:\n\nLinearAlgebra.BLAS.set_num_threads(recommended_blas_threads(dh))\n\nIt returns ncores for 3D grids when running with a single rank per node, and 1 otherwise (detecting ranks per node via MPI). The table below applies this rule to the three primary cases:\n\nCase Returned BLAS threads Evidence\n2D, any layout 1 2D solve at 291k DOFs: 2.61 s (1 thread), 3.31 s (9), 3.38 s (18), 3.49 s (72)\n3D, one rank per node ncores 312 s (1 BLAS thread) → 145 s (36 BLAS threads) at 108k DOFs, 2.1× speedup\n3D, multiple ranks per node 1 no gain for K \\ residual at 18 ranks, 15k DOFs (1.21 s vs 1.20 s per solve), and a large loss for distributed_solve (below)\n\nSome OpenBLAS builds carry a fixed thread limit and abort with precompiled NUM_THREADS exceeded, which happened here on distributed_solve runs launched at 72 BLAS threads, while K \\ residual at 72 completed. Cap BLAS threads at half the node core count if you encounter that error.\n\nBLAS threading only pays on a sufficiently large 3D mesh: at 15k DOFs on one rank, the solve took 1.61 s at 1 BLAS thread against 1.46 s at 36 and 1.52 s at 72, whereas the table above shows a 2.1× speedup at 108k DOFs, so the gain appears somewhere between these two mesh sizes on the architecture tested.\n\nIn 2D, no thread count measured faster than 1 thread: solve time rises from 2.61 s at 1 thread to 3.49 s at 72 threads. Julia defaults BLAS threading to Sys.CPU_THREADS ÷ 2, so 2D runs should set BLAS threads to 1 explicitly (e.g. via LinearAlgebra.BLAS.set_num_threads(recommended_blas_threads(dh))).\n\nwarning: `distributed_solve` requires single-threaded BLAS\nIn a 3D benchmark at 108k DOFs with 18 ranks, increasing BLAS threads from 1 to 4 made distributed_solve 6.3× slower (3.8 s vs 23.6 s per solve).recommended_blas_threads returns 1 whenever ranks share a node, which covers every distributed_solve layout.","title":"BLAS threads"},{"category":"section","location":"performance/#Assembly-cost-across-material-models","page":"Performance and parallel execution","text":"Assembly cost per Newton iteration spans a factor of 17 across the material models tested. The table below lists that per-iteration cost alongside the number of Newton iterations per load step, which together determine the total assembly time over a run, for a unit cube under simple shear over three load steps (47k DOFs, 18 ranks × 4 threads):\n\nMaterial Hooke NeoHooke ArrudaBoyce J2Plasticity Ogden VEVP_Zhao2021_AD VEVP_MOAMMM\nAssembly per iteration preassembled 0.031 s 0.030 s 0.027 s 0.39 s 0.34 s 0.46 s\nNewton iterations per load step 2 3 3 9 4 9 5\n\nThe applied shear is set per material, just below the load at which that model stops converging, so the inelastic models are in flow. The iteration counts therefore describe each model under its own load case rather than a comparison at equal load, and the VEVP_MOAMMM column comes from a twelve-step run.\n\nThe VEVP_Zhao2021_AT model, not listed in the table, is the most expensive of those tested: on the same grid and layout it measured 5.9 s of assembly per iteration, 17× the VEVP_Zhao2021_AD value, and 670 s over three load steps. Due to convergence issues with the AT model, the solve measurement was undertaken using a shear rate ten times lower than the AD model's. Despite the lower shear rate, it still needs 28 Newton iterations per load step against 9 for VEVP_Zhao2021_AD; see VEVP Zhao 2021 for the implementation differences.\n\nAt an equal core count, one rank with many threads assembles more slowly than many ranks with few threads, by a factor of about 10 in both of these cases: VEVP_Zhao2021_AD costs 3.3 s per iteration at 1 rank × 72 threads against 0.34 s at 18 ranks × 4 threads, and NeoHooke 0.31 s against 0.031 s. Threads in one process also share a single heap and garbage collector, which an allocation-heavy material pays for additionally: VEVP_Zhao2021_AD spent 70 s in garbage collection at 1 rank × 72 threads against 5.5 s per rank at 18 ranks × 4 threads. recommended_solve_settings covers this with assembly_bound, which adds ranks in 2D. In 3D, however, the solve still dominates, so the layout stays at one rank.\n\nLinear elastic models (Hooke and Hooke2D) set is_linear(material) = true, allowing element stiffness matrices to be assembled once during initialization and reused, so the Newton loop performs no element assembly at all. Because linear models are purely solve-bound, single-rank execution with BLAS multithreading is optimal (1 rank × 72 threads with BLAS multithreading took 17.4 s vs 35.7 s across 18 ranks at 47k DOFs in 3D).","title":"Assembly cost across material models"},{"category":"section","location":"performance/#Large-meshes","page":"Performance and parallel execution","text":"The measurements above cover 2.2k to 556k DOFs in 3D and 8k to 2.0M in 2D. Runs beyond that range, to 16M DOFs in 2D and 985k in 3D, were conducted to check whether the recommendations scale along:\n\nMesh Solve Layout Wall time Peak memory\n2D, 1.0M DOFs K \\ residual 9 × 8, BLAS 1 7.4 s 6.4 GB per rank\n2D, 4.0M DOFs K \\ residual 4 × 18, BLAS 1 30.2 s 21.9 GB per rank\n2D, 16.0M DOFs K \\ residual 1 × 72, BLAS 36 154 s 95 GB\n3D, 985k DOFs distributed_solve 36 × 4, 2 nodes 377 s 8.6 GB per rank\n\nEvery row covers one load step, using Hooke in 2D and NeoHooke in 3D. Wall time is essentially the linear solve: Hooke is preassembled, and assembly took 1.4 s of 377 s in the 3D row. The rank count decreases along the 2D rows to keep the replicated copies within node RAM, so mesh size and rank count both change between them and their wall times do not measure scaling with mesh size alone. Peak memory, by contrast, does extrapolate: the three 2D points imply an exponent of 1.083, against the 1.086 that estimated_replicated_memory_gb fits over meshes an order of magnitude smaller.\n\nFour configurations did not complete, in every case for lack of memory:\n\n2D at 16.0M DOFs with distributed_solve, at 18 ranks on one node and at 36 ranks across two. Each rank holds its own copy of K, and at that size 18 copies already exceed node RAM (see Memory).\n3D at 4.0M and 16.0M DOFs with distributed_solve on 8 nodes. The fitted memory curve estimates their factorizations at 2.9 TB and 21 TB. Both runs reached 1998 GB and 2006 GB of the 2048 GB available, and the scheduler terminated them within five minutes.\n\nIn 3D the factorization therefore exceeds the memory of eight nodes somewhere above 1M DOFs, while a 2D mesh of the same DOF-size still fits on a single node. Both solvers form an explicit factorization, so no rank or thread layout within distributed_solve overcomes this limit. Only adding more nodes provides more memory, at the solve-time cost shown in Scaling across multiple nodes.","title":"Large meshes"},{"category":"section","location":"performance/#Scaling-across-multiple-nodes","page":"Performance and parallel execution","text":"Use a single node while memory constraints allow. For problem sizes fitting on one node, adding nodes costs wall time through inter-node network communication during distributed_solve factorization. Each row is the median of three repeats, with the spread given as the ratio of the slowest repeat to the fastest:\n\n3D, 273k DOFs, distributed_solve Wall time Spread\n18 ranks × 4 threads, 1 node 102 s 1.30×\n36 ranks × 2 threads, 1 node 62 s 1.01×\n36 ranks × 4 threads, 2 nodes 123 s 1.15×\n72 ranks × 4 threads, 4 nodes 76 s 1.06×\n\nThe best single-node layout outperformed every multi-node layout measured. Furthermore, no individual run of any other layout was faster than the slowest 36 ranks × 2 threads run, at 62.3 s.\n\nThe same ordering was observed on a mesh four times larger: at 985k DOFs the run took 377 s on 2 nodes, 708 s on 4, and 821 s on 8, with the MUMPS factorization accounting for 122 s, 233 s, and 270 s per solve, respectively.\n\nOnly the linear solve grows with the node count. Element assembly at 273k DOFs decreased from 0.168 s to 0.064 s per Newton iteration between 18 and 72 ranks, and the tangent reduction increased from 2.1 s to 4.7 s when a second node was added. Multi-node execution is therefore recommended when the factorization exceeds single-node RAM, rather than as a means of shortening a run that already fits on one node.","title":"Scaling across multiple nodes"},{"category":"section","location":"performance/#reproduce","page":"Performance and parallel execution","text":"The solver crossover can be located by timing the linear solve of a single Newton step, without running the full simulation. The snippet below times that solve alone, so compare whole runs as well where assembly takes a large share of the run time.\n\nInsert the benchmark snippet below into a script that initializes dh, ch, assembler, and u (such as examples/mpi_four_point_bending.jl). The distributed_solve path requires MUMPS.jl (Unix-only); omit import MUMPS, the distributed_solve warm-up call, and the t_dist measurement to benchmark the K \\ residual path alone.\n\nusing LinearAlgebra, MPI, Ferrite, FerriteSolidMechanics\nimport MUMPS                                  # distributed solver (Unix-only)\n\nK, r = stiffness_matrix(assembler, u; dt=dt)  # executed by all ranks\napply_zero!(K, r, ch)\n\n# Warm up JIT compilation before timing\nK \\ r\ndistributed_solve(K, r, MPI.COMM_WORLD)\n\n# Timing\nt_rep  = minimum(@elapsed(K \\ r) for _ in 1:3)\nMPI.Barrier(MPI.COMM_WORLD)\nt_dist = minimum(@elapsed(distributed_solve(K, r, MPI.COMM_WORLD)) for _ in 1:3)\n\n# Output measurements\nMPI.Comm_rank(MPI.COMM_WORLD) == 0 && println(\n    ndofs(dh), \" dofs, \", MPI.Comm_size(MPI.COMM_WORLD), \" ranks: \",\n    \"replicated \", round(t_rep; digits=2), \" s; distributed \", round(t_dist; digits=2), \" s\")\n\nExecute the benchmark script in two distinct launches to evaluate each solver in its optimal layout:\n\n# Replicated path: single rank, BLAS threads enabled -> record t_rep\nmpiexec -n 1 julia --project=. --threads=72 bench.jl\n\n# Distributed path: multiple ranks, single-threaded BLAS -> record t_dist\nOPENBLAS_NUM_THREADS=1 mpiexec -n 18 julia --project=. --threads=4 bench.jl\n\nCalling BLAS.set_num_threads(recommended_blas_threads(dh)) within the script automatically sets appropriate BLAS thread counts for both execution modes (assuming our cluster benchmarks also hold on your hardware). We suggest repeating this measurement across three mesh sizes spanning an order of magnitude; the performance threshold is the mesh size where t_rep and t_dist intersect. Repeat each measurement in a separate job submission rather than several times within one, so that node-to-node differences show up in the spread. distributed_solve also pays one extra factorization whenever MUMPS retries with more workspace. In a 2D case with four retries over sixteen solves, that extra factorization cost 15% more time per solve.\n\nTo calibrate the memory model, compare estimated_replicated_memory_gb(ndofs(dh), dim) against the peak memory usage reported by your job scheduler (e.g., MaxRSS in SLURM's sacct output) for a single-rank run. Multiply the single-rank memory usage by the desired ranks per node to determine total node memory demands. Repeat this check after changing the material model, since a more expensive one raised the memory by up to 1.5× in our benchmarks. Repeat it as well after lengthening the run: over the series in Memory, NeoHooke at 47k DOFs rose from 7.1 GB over three load steps to 11.0 GB over twelve (1.55×) and to 14.0–18.7 GB over 48.\n\nThe MPI four-point bending tutorial is the 2D problem measured on this page, and it includes the full runnable script.","title":"Reproducing these on your cluster"},{"category":"section","location":"wrappers/#Plane-strain-/-plane-stress-wrappers","page":"In-plane wrappers","text":"PlaneStrain and PlaneStress are wrappers that adapt a 3D material to a 2D plane analysis. Both delegate the constitutive evaluation to the wrapped 3D model and extract the in-plane parts. The code lives in src/PlaneStrainStress.jl.\n\nThis page documents:\n\nThe embedding of the 2D deformation gradient into 3D.\nHow PlaneStrain recovers the 2D PK1 stress and consistent tangent.\nHow PlaneStress solves the out-of-plane equilibrium and statically condenses the tangent.\nThe state wrapper used by PlaneStress to track the out-of-plane stretch across Newton iterations and load steps.\n\nFor a usage example, see the 2D plate with a hole. To make a new 3D material wrappable, see Hooking into the wrappers below.","title":"Plane strain / plane stress wrappers"},{"category":"section","location":"wrappers/#Notation","page":"In-plane wrappers","text":"A 2D problem lives in the x₁–x₂ plane. The 3D embedding adds an x₃-direction that is out-of-plane. Vectors and tensors follow the conventions of Tensors.jl.\n\nF ∈ Tensor{2,2} – the 2D deformation gradient.\nC = Fᵀ F – the 2D right Cauchy–Green tensor.\nP ∈ Tensor{2,2} – the 2D first Piola–Kirchhoff stress.\nS ∈ SymmetricTensor{2,2} – the 2D second Piola–Kirchhoff stress.\nσ ∈ SymmetricTensor{2,2} – the 2D Cauchy stress.\n\nThe 3D counterparts carry an overbar: F̄, P̄, etc.","title":"Notation"},{"category":"section","location":"wrappers/#Embedding-the-2D-deformation-gradient","page":"In-plane wrappers","text":"Both wrappers start by embedding the 2D F into a 3D F̄ (using embed_F_2D_to_3D(F, F33)). The choice of F̄₃₃ distinguishes plane strain from plane stress.","title":"Embedding the 2D deformation gradient"},{"category":"section","location":"wrappers/#Plane-strain-with-F-1","page":"In-plane wrappers","text":"The constraint F̄₃₃ = 1 in\n\nbar F =\nbeginpmatrix F_11  F_12  0  F_21  F_22  0  0  0  1 endpmatrix\n\nenforces ε₃₃ = 0 in the reference configuration.","title":"Plane strain with F̄₃₃ = 1"},{"category":"section","location":"wrappers/#Plane-stress-with-unknown-F","page":"In-plane wrappers","text":"Plane stress enforces σ̄₃₃ = 0. Due to the absence of out-of-plane shear (F̄₁₃ = F̄₂₃ = F̄₃₁ = F̄₃₂ = 0) in 2D, this constraint is equivalent to P̄₃₃ = 0 (since P̄ = J σ̄ F̄⁻ᵀ and thus P̄₃₃ = J σ̄₃₃ (F̄⁻ᵀ)₃₃ = J σ̄₃₃ / F̄₃₃). The 3D constitutive update becomes an implicit equation in the scalar F̄₃₃ by defining a scalar residual function g as\n\ng(F̄_33) = bar P_33(F_11 F_12 F_21 F_22 F̄_33) = 0 text\n\nA local Newton iteration solves g(F̄₃₃) = 0 at each quadrature point per assembly call, using the previously converged value (stored on the PlaneStressStateWrapper) as the initial guess.","title":"Plane stress with unknown F̄₃₃"},{"category":"section","location":"wrappers/#PlaneStrain:-PK1,-tangent,-state","page":"In-plane wrappers","text":"PlaneStrain returns the in-plane PK1 via P = extract_P_2D(P̄) and the in-plane consistent tangent ∂P/∂F via automatic differentiation of the complete embedded 3D response in a single call:\n\ndP2D_dF2D = Tensors.gradient(F2D_ -> extract_P_2D(Tensor{2,3}(compute_PK1_3D(ps.model, embed_F_2D_to_3D(F2D_, 1.0), dt, state))), F2D)\n\nThe gradient is taken with respect to the 2D F, so the constraint F̄₃₃ = 1 is naturally propagated through the differentiation.","title":"PlaneStrain: PK1, tangent, state"},{"category":"section","location":"wrappers/#PlaneStress:-local-Newton-and-condensation","page":"In-plane wrappers","text":"PlaneStress wraps a Newton solver around the 3D material. The loop runs inside the wrapper's material_response, i.e. at every quadrature point of every assembly call. The local Newton controls are configurable via keyword arguments tol and maxiter in PlaneStress(model; tol=1e-10, maxiter=20). The core algorithm for this local solve runs as follows:\n\nF̄₃₃  ←  state.F33_previous      # initial guess from previous load step\ng(x) = compute_PK1_3D(model, embed_F_2D_to_3D(F, x), dt, state.inner)[3,3]\n\nrepeat up to maxiter:\n    residual  ←  g(F̄₃₃)         # constitutive evaluation\n    if |residual| < tol: break\n    dP̄₃₃/dF̄₃₃  ←  g'(F̄₃₃)       # constitutive evaluation (via AD)\n    F̄₃₃  ←  F̄₃₃  −  residual / (dP̄₃₃/dF̄₃₃)\nstate.F33_current ← F̄₃₃\n\nEach Newton correction performs two constitutive evaluations: one primal evaluation for the residual, and one AD evaluation for the derivative.\n\nIf the local Newton solve does not converge within maxiter, or if ∂P̄₃₃/∂F̄₃₃ drops below the threshold sqrt(eps(Float64)) ≈ 1.5e-8, the wrapper throws a PlaneStressConvergenceError (a LocalAssemblyFailure subtype) containing a reason symbol (:small_newton_derivative or :local_newton_nonconvergence). Outer load step controllers can catch this directly or call try_stiffness_matrix, which returns a named tuple with converged = false and the error object.\n\nOnce the local iteration converges, the wrapper evaluates the full 3D tangent ∂P̄/∂F̄ and extracts the in-plane PK1 stress. The consistent 2D tangent is then determined via static condensation (Schur complement) as\n\nfracpartial P_ijpartial F_kl\n=\nfracpartial bar P_ijpartial bar F_kl\n-\nfracpartial bar P_ijpartial bar F_33\nfracpartial bar P_33partial bar F_kl\nBig\nfracpartial bar P_33partial bar F_33\nquad forall ijkl in 12 text\n\nwhich is implemented by condense_tangent_2D in src/PlaneStrainStress.jl. Note that if the out-of-plane stiffness component in this full 3D tangent drops below the numerical threshold, this condensation step will independently throw a PlaneStressConvergenceError with the reason :singular_condensation.","title":"PlaneStress: local Newton & condensation"},{"category":"section","location":"wrappers/#The-PlaneStressStateWrapper","page":"In-plane wrappers","text":"Both PlaneStrain and PlaneStress update the wrapped material's trial state with a single call to update_state_from_3D!(state, model, F̄, dt). That call is the last step of the wrapper's material_response, after stress and tangent have been computed. Final state commits happen canonically via update_states!.\n\nPlaneStrain does not need a custom state wrapper because its out-of-plane stretch F̄₃₃ = 1 is fixed. In contrast, PlaneStress wraps the 3D material's state to track the variable out-of-plane stretch F̄₃₃:\n\nmutable struct PlaneStressStateWrapper{S} <: AbstractMaterialState\n    inner::S                       # the wrapped 3D material state\n    F33_current::Float64           # F̄₃₃ at the most recent trial\n    F33_previous::Float64          # F̄₃₃ at the last converged step\nend\n\nThe wrapped 3D state (inner) is updated only after the local Newton loop has successfully converged. If the outer load step is rejected, revert_state! rolls back both F33 and the wrapped 3D state. F33_previous is then used as the initial guess for the next attempt.","title":"The PlaneStressStateWrapper"},{"category":"section","location":"wrappers/#Hooking-into-the-wrappers","page":"In-plane wrappers","text":"Two methods connect a 3D material to the wrapper pipeline, beyond the standard state / element hooks:\n\ncompute_PK1_3D(material, F̄, dt, state) -> P̄\nupdate_state_from_3D!(state, material, F̄, dt)\n\nThe first returns the 3D first Piola–Kirchhoff stress. The second writes the wrapped material's current/trial variables at the converged 3D deformation. Final commit happens through the usual update_state! / update_states! path.\n\nSmall-strain models (and AbstractHyperelastic subtypes) do not need to implement either method. A generic fallback builds both from material_response: it forms ε = sym(F̄ - I), evaluates the constitutive response, and converts the resulting Cauchy stress to PK1. Finite-strain models must implement both methods explicitly, as no universal conversion from deformation gradient to stress exists for them.\n\nBoth wrappers differentiate compute_PK1_3D with ForwardDiff. The deformation gradient therefore carries ForwardDiff.Dual entries, while the committed history remains Float64. If a model constructs a state object or scratch cache that mixes both types, the call will fail with a MethodError. As this is not a LocalAssemblyFailure, try_stiffness_matrix does not catch it. A wrappable model must therefore accept both types in its internal state and scratch cache; see the Developer guide for details.\n\nCheck the Stable models table for a full list of bundled 3D materials that support the wrapper API.\n\nMost models use the generic wrapper assembly explained above. Ogden is an exception: PlaneStrain(Ogden(...)) and PlaneStress(Ogden(...)) dispatch to exact-tangent methods in src/models/Ogden.jl, avoiding AD through spectral powers (which are non-finite at repeated principal stretches).\n\nVEVP_Zhao2021_AT does not support the generic wrapper AD path: its analytic-tangent implementation is Float64/matrix-based. It provides a specialized PlaneStrain(VEVP_Zhao2021_AT(...)) assembly path, but PlaneStress is not implemented for VEVP_Zhao2021_AT. Use VEVP_Zhao2021_AD for plane stress analyses.\n\nIf you write a new 3D model, see the Developer guide – its wrapper hooks section shows the generic small-strain derivation and explains why a finite-strain model must supply both methods itself.","title":"Hooking into the wrappers"},{"category":"section","location":"models/#Material-models","page":"Overview","text":"This page helps you choose a material model based on your requirements. Each linked model page (see Stable models) provides the equations, constructor, state behavior, implementation notes, provenance, acknowledgements, and references.","title":"Material models"},{"category":"section","location":"models/#Which-model-should-I-use?","page":"Overview","text":"The table below is a non-exhaustive overview of frequent problem classes and suitable material formulations; applicability should still be checked for each case.\n\nProblem class Reach for\nLinear elasticity Hooke/Hooke2D\nSmall-strain plasticity J2Plasticity\nFinite-strain rubber / elastomer NeoHooke, MooneyRivlin, Ogden, ArrudaBoyce\nSemi-crystalline / crosslinked polymer with damage VEPD_Detrez2010\nThermoplastic / glassy polymer VEVP_MOAMMM, VEVP_Zhao2021_AD\n2D plane strain/stress for any model PlaneStrain, PlaneStress wrappers\n\nSee the Stable models table below for the full list and related variants.","title":"Which model should I use?"},{"category":"section","location":"models/#Stable-models","page":"Overview","text":"The dt column indicates whether the material is rate-dependent and actively uses the time step size dt for material state updates.\n\nConstructor Page Use when you need dt 2D support\nHooke Linear Elasticity 3D isotropic linear elasticity No PlaneStrain / PlaneStress; prefer Hooke2D\nHooke2D Linear Elasticity Direct 2D linear elasticity No Direct\nNeoHooke Neo–Hookean Compressible finite-strain hyperelasticity No PlaneStrain / PlaneStress\nArrudaBoyce Arruda–Boyce Limited-chain finite-strain hyperelasticity No PlaneStrain / PlaneStress\nMooneyRivlin Mooney–Rivlin Two-invariant finite-strain hyperelasticity No PlaneStrain / PlaneStress\nOgden Ogden Principal stretch finite-strain hyperelasticity with arbitrary term count No Specialized PlaneStrain / PlaneStress\nJ2Plasticity J2 Plasticity Small-strain von Mises plasticity No PlaneStrain / PlaneStress\nVEPD_Detrez2010 VEPD Detrez 2010 Finite-strain viscoelastic-plastic-damage response Yes PlaneStrain / PlaneStress\nVEVP_Zhao2021_AD VEVP Zhao 2021 Finite-strain viscoelastic-viscoplastic glassy-polymer response Yes PlaneStrain / PlaneStress\nVEVP_Zhao2021_AT VEVP Zhao 2021 Related Zhao formulation with trial-state stress and analytical tangent Yes 3D and PlaneStrain only\nVEVP_MOAMMM VEVP MOAMMM Finite-strain viscoelastic-viscoplastic response with Drucker-Prager plasticity Yes PlaneStrain / PlaneStress","title":"Stable models"},{"category":"section","location":"models/#Using-MaterialModelsBase.jl-models","page":"Overview","text":"FerriteSolidMechanics.jl focuses on established material models provided as a whole, but also supports modularly composed materials from the following packages by Knut Andreas Meyer (which also offer extended tooling for them, such as parameter differentiation for calibration and stress-state-driven point simulations): MaterialModelsBase.jl specifies an interface for constitutive models, and MechanicalMaterialModels.jl provides an existing library of material models (elasto-(visco)plasticity and viscoelasticity). Any material implementing this interface can be used directly through the FromMaterialModelsBase wrapper.\n\nThe wrapper is a thin bridge: FerriteSolidMechanics' own constitutive interface deliberately follows similar conventions. Trial/commit state management, threaded/MPI assembly, stress output, and try_stiffness_matrix-based adaptive stepping (local convergence failures surface as MaterialModelsBaseConvergenceError) therefore all work as for bundled material models.\n\nThe bridge targets MaterialModelsBase.jl 0.4; a different version may require an update. The extension activates when MaterialModelsBase is loaded:\n\nusing Pkg\nPkg.add(url=\"https://github.com/KnutAM/Newton.jl\") # dependency of MechanicalMaterialModels\nPkg.add(url=\"https://github.com/KnutAM/MaterialModelsBase.jl\")\nPkg.add(url=\"https://github.com/KnutAM/MechanicalMaterialModels.jl\")\n\nusing FerriteSolidMechanics\nimport MaterialModelsBase   # activates the extension; prefer `import` over `using` (both packages export `material_response`)\nusing MechanicalMaterialModels: Plastic, LinearElastic, Voce, ArmstrongFrederick, NortonOverstress\n\nmmmMaterial = Plastic(;\n    elastic    = LinearElastic(E=210.0e3, ν=0.3),\n    yield      = 100.0,\n    isotropic  = Voce(Hiso=50.0e3, κ∞=100.0),\n    kinematic  = ArmstrongFrederick(Hkin=200.0e3, β∞=200.0),\n    overstress = NortonOverstress(tstar=1.0, nexp=2.0)\n)\n\nmat = FromMaterialModelsBase(mmmMaterial)  # kinematics=SmallStrain() is the default\nfem = create_assembler(mat, dh, ch)        # then solve exactly as with bundled models\n\nFor finite-strain MaterialModelsBase materials pass FromMaterialModelsBase(mmmMaterial; kinematics=FiniteStrain()), which uses the deformation gradient F and expects the material to return (P, ∂P∂F, new_state) (first Piola–Kirchhoff stress, its tangent, and updated state). Note that FerriteSolidMechanics' PlaneStrain/PlaneStress material wrappers are unrelated to MaterialModelsBase's stress-state types of the same names. Whether a material model used through FromMaterialModelsBase can be combined with these 2D wrappers is currently untested. Hooking into the wrappers states the requirements for a wrappable material model.","title":"Using MaterialModelsBase.jl models"},{"category":"section","location":"models/#Related-pages","page":"Overview","text":"Material model API lists the constructor docstrings of all bundled models, the PlaneStrain / PlaneStress wrappers, and the wrapper extension points.\nWrappers explains how 3D material models are embedded into 2D plane strain and plane stress analyses.\nExperimental models lists comparison variants under FerriteSolidMechanics.Experimental.\nDeveloper guide explains the material interface (material_response + kinematics, trial/commit state handling, and the optional element-level assembly hook) needed to support these features in custom materials.","title":"Related pages"},{"category":"section","location":"tutorials/mpi_four_point_bending/#MPI-four-point-bending","page":"MPI four-point bending","text":"A 2D plane strain four-point-bending beam using VEVP_Zhao2021_AD, solved with FerriteSolidMechanics' threaded and MPI assembly paths. The runnable script is examples/mpi_four_point_bending.jl; the SLURM submission script is examples/submit_four_point_bending.sh.\n\nRun the one-rank script from the repository root with:\n\njulia --project=. examples/mpi_four_point_bending.jl\n\nRun the same script through MPI with:\n\nmpiexec -n 4 julia --project=. --threads=1 examples/mpi_four_point_bending.jl\n\nThe script's main() initializes MPI before calling the example function. Without mpiexec, this is a single-rank MPI run. The direct run writes PVD/VTU output and therefore requires WriteVTK.jl in the active environment. For a no-output run that avoids WriteVTK.jl, include the script and call main(; vtk=nothing):\n\njulia --project=. --threads=1 -e \"include(ARGS[1]); main(; vtk=nothing)\" examples/mpi_four_point_bending.jl","title":"MPI four-point bending"},{"category":"section","location":"tutorials/mpi_four_point_bending/#Problem-setup","page":"MPI four-point bending","text":"In its default configuration, the beam is under plane strain with aspect ratio L/H = 10. Its structured quadrilateral mesh spans L x H and contains nx = 10*density and ny = density elements with density = 4 as the default value. As shown in Figure 1, the supports and loads are applied on four boundary segments:\n\nsupport_left: bottom edge at x = 0.10L, pinned with u_x = u_y = 0,\nsupport_right: bottom edge at x = 0.90L, roller support with u_y = 0,\nload_left: top edge at x = 0.30L, downward traction,\nload_right: top edge at x = 0.70L, downward traction.\n\nThe load history ramps from zero to full load, holds the full load, unloads immediately to zero, and then keeps a zero-load recovery phase. The load is applied as a surface traction integrated over the two top boundary segments, ensuring well-posed boundary conditions across mesh refinements.\n\n(Image: Four-point bending setup) Figure 1. Four-point bending setup with pinned and roller supports, and loaded top segments.","title":"Problem setup"},{"category":"section","location":"tutorials/mpi_four_point_bending/#Grid,-interpolation,-constraints,-loads","page":"MPI four-point bending","text":"The example creates the mesh, boundary facetsets, displacement field, supports, and load integration data with standard Ferrite.jl calls (this setup is identical to a serial run; see 2D plate with a hole for a detailed walkthrough):\n\ndensity = 4                                        # mesh density parameter\nbeam_length = 10.0                                 # beam length L\nbeam_height = 1.0                                  # beam height H\nnx = 10 * density                                  # elements along the beam\nny = density                                       # elements along the height\ngrid = generate_grid(Quadrilateral, (nx, ny), Vec(0.0, 0.0), Vec(beam_length, beam_height)) # create the beam mesh\n\nhx = beam_length / nx                              # element width\nhalf_segment = max(1.01 * hx, 0.025 * beam_length) # segment half-width, at least one facet\non_bottom_segment(x, x0) = isapprox(x[2], 0.0; atol=1e-10) && abs(x[1] - x0) <= half_segment      # true on bottom edge inside the segment\non_top_segment(x, x0) = isapprox(x[2], beam_height; atol=1e-10) && abs(x[1] - x0) <= half_segment # true on top edge inside the segment\naddfacetset!(grid, \"support_left\",  x -> on_bottom_segment(x, 0.10 * beam_length)) # pinned support segment\naddfacetset!(grid, \"support_right\", x -> on_bottom_segment(x, 0.90 * beam_length)) # roller support segment\naddfacetset!(grid, \"load_left\",     x -> on_top_segment(x, 0.30 * beam_length))    # left load segment\naddfacetset!(grid, \"load_right\",    x -> on_top_segment(x, 0.70 * beam_length))    # right load segment\n\ninterpolation = Lagrange{RefQuadrilateral,1}()^2  # linear displacement shape functions in 2D\ndh = DofHandler(grid)                             # create dof handler for the grid\nadd!(dh, :u, interpolation)                       # add displacement dofs under the field name `:u`\nclose!(dh)                                        # finalize the dof numbering\n\nch = ConstraintHandler(dh)                        # collect support boundary conditions\nadd!(ch, Dirichlet(:u, getfacetset(grid, \"support_left\"),  (x, t) -> [0.0, 0.0], [1, 2])) # pinned support\nadd!(ch, Dirichlet(:u, getfacetset(grid, \"support_right\"), (x, t) -> [0.0], [2]))         # roller support\nclose!(ch)                                        # finalize the constraint data\n\n\nThe supports are enforced as displacement constraints during every iteration of the Newton loop, while the external traction is assembled once at the beginning of each load step (see Load and Newton loop).","title":"Grid, interpolation, constraints, loads"},{"category":"section","location":"tutorials/mpi_four_point_bending/#Material","page":"MPI four-point bending","text":"We employ the 3D VEVP_Zhao2021_AD model through a plane strain wrapper:\n\nmaterial = PlaneStrain(VEVP_Zhao2021_AD(5.0, 20.0, 6, 0.0, 0.0, 283.0, 18.51, 0.1421, 0.6152, 3.0, 3.0, 0.0, 0.0, 283.0))\n\nPlaneStrain embeds the 2D deformation into the 3D material model and extracts the in-plane response.","title":"Material"},{"category":"section","location":"tutorials/mpi_four_point_bending/#Assembler","page":"MPI four-point bending","text":"We call create_assembler exactly as in the serial tutorials:\n\nassembler = create_assembler(material, dh, ch; quadrature_order=2)\n\nThe same call works similarly in a non-MPI Julia session, a one-rank MPI run, and a multi-rank MPI run. When MPI has been initialized, the assembler distributes nonlinear cell work across ranks and reduces the assembled global tangent and internal force vector.","title":"Assembler"},{"category":"section","location":"tutorials/mpi_four_point_bending/#External-load","page":"MPI four-point bending","text":"The downward pressure on the two loading pads is declared on a LoadHandler, which is built and closed the way the ConstraintHandler above is:\n\nlh = LoadHandler(assembler)              # takes dh, thickness and quadrature orders from the assembler\nfor name in (\"load_left\", \"load_right\")  # both loading pads carry the same pressure\n    add!(lh, Traction(name, (x, factor) -> Vec(0.0, -load_pressure * factor))) # downward surface traction\nend\nclose!(lh)                               # resolve the facetsets and validate the load functions\n\nThe load function receives the quadrature point coordinate x and, as its second argument, the factor external_forces! is called with. This example passes a load factor rather than physical time. Through x any spatial profile can be written directly in the load function, for example Vec(0.0, -load_pressure * factor * x[1] / beam_length) for a pressure growing linearly along the beam. See the External loads page for the other load types and for the Pressure shorthand that applies a traction along the facet normal.","title":"External load"},{"category":"section","location":"tutorials/mpi_four_point_bending/#Load-and-Newton-loop","page":"MPI four-point bending","text":"The load factor is prescribed by an array of converged load steps:\n\nload_profile = vcat(\n    collect(range(1.0 / load_steps, 1.0; length=load_steps)), # ramp up\n    fill(1.0, hold_steps),                                    # hold full load\n    [0.0],                                                    # immediate unload (replace with a short ramp if too abrupt)\n    fill(0.0, recover_steps),                                 # recovery\n)\n\nAt every load step the external force vector is evaluated once, and FerriteSolidMechanics assembles the internal force and tangent inside the Newton loop via stiffness_matrix:\n\nfor (step, load_factor) in enumerate(load_profile)            # advance the load history\n    f_ext = external_forces!(lh, load_factor)                 # dead load: once per step, not per iteration\n\n    for iter in 1:max_newton                                  # Newton iterations for this load step\n        K, f_int_step = stiffness_matrix(assembler, u; dt=dt) # constitutive evaluation and tangent/internal force assembly\n        copyto!(f_int, f_int_step)                            # store internal force for output\n        residual .= f_int .- f_ext                            # residual with external traction\n        apply_zero!(K, residual, ch)                          # apply zero-displacement constraints\n\n        threshold = newton_tol * max(norm(f_ext), 1.0)        # relative residual tolerance\n        if norm(residual) <= threshold                        # stop once the residual is small enough\n            break\n        end\n        u .-= K \\ residual                                    # update the displacement vector\n    end\n    update_states!(assembler)                                 # commit material states after convergence\nend\n\nBecause the VEVP_Zhao2021_AD model is rate-dependent, the dt passed to stiffness_matrix is the physical time increment between two entries of load_profile. This example keeps dt fixed and calls stiffness_matrix directly, which is sufficient for this load case and material parameters. For simulations where the local material update may fail, replace the assembly call (stiffness_matrix) with try_stiffness_matrix and reject or retry the whole load step as shown in the Adaptive time stepping tutorial. Fail-recovery must be supported by the material model chosen; see Feature support. Since try_stiffness_matrix synchronizes local failures automatically across MPI ranks, the adaptive time stepping loop requires no MPI-specific changes: if converged = false, every rank rolls back, reduces dt, and retries the same step.","title":"Load and Newton loop"},{"category":"section","location":"tutorials/mpi_four_point_bending/#Postprocessing","page":"MPI four-point bending","text":"The example writes VTK files and a text history only on rank 0. Stress output is evaluated inside write_output_step! when VTK output is enabled:\n\nstresses = compute_stresses(assembler, u)\n\nWith the default output path, rank 0 writes examples/results/mpi_four_point_bending/four_point_bending.pvd. Pass vtk = nothing to run_mpi_four_point_bending() (or main(; vtk=nothing)) to disable PVD/VTU output and skip the stress-output pass.","title":"Postprocessing"},{"category":"section","location":"tutorials/mpi_four_point_bending/#What-MPI-changes","page":"MPI four-point bending","text":"The Newton and the outer load loops are the same in serial, threaded, and MPI runs. The MPI-specific behavior is already handled inside GenericMaterialAssembler:\n\ncreate_assembler assigns nonlinear cells to ranks by a static round-robin rule.\nstiffness_matrix evaluates only the rank-owned cells and then reduces the global tangent and internal force vector across ranks.\ncompute_stresses follows the same ownership-and-reduction pattern for stress output.\nupdate_states! commits only the quadrature states owned by the current rank.\n\nThreading is independent of MPI: Each rank uses the specified Julia threads to evaluate its owned elements (cells) in parallel during stiffness and stress assembly, while quadrature points within a cell are always evaluated sequentially by the thread processing that cell.\n\nNote that while the constitutive and element-assembly work is distributed, the sparse linear solve is not: After the reduction, every rank has the full global tangent stiffness matrix K and internal force vector f_int, constructs the residual, and solves the same system via K \\ residual. MPI can only improve runtime when there is enough nonlinear element work per rank to dominate the reduction overhead and rank-replicated solving.\n\nTwo solvers are available for that system:\n\nK \\ residual, the standard replicated solve this example uses. Here, every rank factorizes the full system, so each pays the memory of one factorization. It needs no extra packages and measured faster at every 2D grid size.\ndistributed_solve, which splits one factorization across the ranks with MUMPS. It requires import MUMPS and runs on Unix only. In 3D it measured faster at every mesh size above roughly 4k DOFs, by 5.8× at 108k DOFs and 10.2× at 273k, and it uses far less memory per rank. See Distributed solve for large 3D systems.\n\nThis example is 2D and therefore keeps K \\ residual; the BLAS tuning (BLAS.set_num_threads) applies to that solve only.\n\ntip: Speed up the replicated solve\nBLAS threading helps the replicated solve in 3D, where the linear solve dominates – but only when a rank has the node to itself. The example calls LinearAlgebra.BLAS.set_num_threads(recommended_blas_threads(dh)) once, before the load loop; see recommended_blas_threads. The helper reads the grid dimension and the ranks sharing the node, so it returns 1 for this 2D example, ncores for a 3D model on one rank per node, and 1 again as soon as ranks share a node. The 2D value is an active setting, not a default: Julia starts BLAS at Sys.CPU_THREADS ÷ 2, and on a 72-core node the 2D solve measured 2.61 s per call at 1 thread against 3.38 s at 18 and 3.49 s at 72.\n\nrecommended_solve_settings returns the solver, rank and thread counts, BLAS threads and estimated memory for a given grid and node, so these rules need not be applied by hand. For the measurements behind it, by dimension, mesh size and material, see Performance and parallel execution.","title":"What MPI changes"},{"category":"section","location":"tutorials/mpi_four_point_bending/#MPI-troubleshooting","page":"MPI four-point bending","text":"A healthy mpiexec -n 4 run starts with a single startup line from rank 0 that includes ranks=4. A broken launcher setup may instead print one startup line per process with ranks=1; in that case, the launcher is not connected to the MPI library used by MPI.jl. Before running the full example, this minimal command should print four ranks:\n\nmpiexec -n 4 julia --project=. --threads=1 -e \"using MPI; MPI.Init(); write(stdout, string(:rank, ' ', MPI.Comm_rank(MPI.COMM_WORLD), ' ', '/', ' ', MPI.Comm_size(MPI.COMM_WORLD), '\\n')); flush(stdout); MPI.Finalize()\"\n\ntip: MPI launcher / library mismatch (Linux)\nIf mpiexec works on one machine but fails on a cluster during MPI.Init(), first check that the launcher and the MPI library loaded by MPI.jl come from the same MPI implementation:which mpiexec\nmpiexec --version\njulia --project=. -e 'using MPI; println(MPI.MPI_LIBRARY); println(MPI.mpiexec())'A common failure is launching with system Open MPI while MPI.jl loads MPICH. After loading the cluster MPI module, configure MPI.jl to use that system MPI. MPIPreferences sets this on the environment you launch from, not on FerriteSolidMechanics, so add it to that environment first:julia --project=. -e 'using Pkg; Pkg.add(\"MPIPreferences\")'\njulia --project=. -e 'using MPIPreferences; MPIPreferences.use_system_binary(; mpiexec=\"mpiexec\")'\njulia --project=. -e 'using Pkg; Pkg.precompile()'Restart Julia and recheck MPI.MPI_LIBRARY. If automatic detection cannot find the MPI library, locate libmpi with your cluster module tools or ldconfig -p | grep libmpi. Then pass the full path, for example library_names=[\"/path/to/libmpi.so\"], and set abi=\"OpenMPI\" or abi=\"MPICH\" to match the loaded module.\n\ntip: MPI launcher / library mismatch (Windows)\nOn Windows, where mpiexec may list multiple MPI launchers.where mpiexec\njulia --project=. -e \"using MPI; println(MPI.MPI_LIBRARY); println(MPI.mpiexec())\"If MPI.jl reports MicrosoftMPI, C:\\Program Files\\Microsoft MPI\\Bin\\mpiexec.exe should appear either as the only entry or before other mpiexec.exe entries. If it does not, move C:\\Program Files\\Microsoft MPI\\Bin earlier in PATH (system environment variables), or launch with the full Microsoft MPI mpiexec.exe path instead:\"C:\\Program Files\\Microsoft MPI\\Bin\\mpiexec.exe\" -n 4 julia --project=. --threads=1 examples/mpi_four_point_bending.jl","title":"MPI troubleshooting"},{"category":"section","location":"tutorials/mpi_four_point_bending/#Distributed-solve-for-large-3D-systems","page":"MPI four-point bending","text":"In the replicated solve, every rank factorizes the full K into triangular factors (K = L·U), so the solve cost is independent of the node count and those factors must fit within a single node's memory. In 3D the factors have far more nonzeros than K and dominate time and memory; for a large enough mesh they exceed one node's RAM and the solve fails.\n\ndistributed_solve solves K * du = residual with the MUMPS distributed sparse direct solver, splitting the factorization across the MPI ranks. It is a drop-in replacement for the K \\ residual line in the Newton loop:\n\nimport MUMPS   # loads the FerriteSolidMechanics MUMPS extension; add MUMPS.jl to the environment\n\n# ... inside the Newton loop, after apply_zero!(K, residual, ch):\nu .-= distributed_solve(K, residual, MPI.COMM_WORLD)\n\nMUMPS is loaded through a package extension, so nothing is installed unless you import MUMPS. This is an opt-in path and Unix/cluster-only. Every rank calls it with its copy of K and residual, and all ranks receive the same du. Only the triangular factors are distributed; K itself stays replicated on every rank. The factors use far more memory than K, so distributing them is what saves memory. This, however, does not help once K itself exceeds one node's memory.","title":"Distributed solve for large 3D systems"},{"category":"section","location":"tutorials/mpi_four_point_bending/#When-it-pays-off","page":"MPI four-point bending","text":"In 3D the distributed solve was faster at every mesh size measured. Measured on NHR@FAU Fritz (72-core nodes), 3D hexahedral NeoHooke, each solver in its own best configuration:\n\nGrid Replicated K \\ residual (1 rank × 72 threads, BLAS 36) distributed_solve (BLAS 1)\n108k DOFs 145 s, 20.7 GB 25.0 s, 2.1 GB (18 ranks × 4 threads)\n273k DOFs 634 s, 62.5 GB 62.3 s, 3.4 GB (36 ranks × 2 threads)\n\nBoth took nine Newton iterations and reached the same solution. Replicated factors are held per rank, so the 62.5 GB at 273k DOFs is the cost of one rank on a 256 GB node and the total scales with the rank count. At 556k DOFs one rank needed 179 GB, or 70% of a node.\n\nIn 2D the replicated solve was faster, 52 s against 62 s at 291k DOFs, so keep K \\ residual there. These thresholds depend on element, mesh and hardware; measure on your own problem before relying on them.","title":"When it pays off"},{"category":"section","location":"tutorials/mpi_four_point_bending/#Launch-layout","page":"MPI four-point bending","text":"MUMPS wants many MPI ranks per node, each with exactly one BLAS thread, the opposite of the threaded solve (one rank per node, many threads). Its factorization is communication-heavy, so use a system MPI (for example a cluster's Open MPI module, configured as in MPI troubleshooting) rather than a bundled one.\n\nOn a 72-core node, 18–36 ranks were fastest: at 273k DOFs in 3D, 36 ranks × 2 threads took 62 s and 18 ranks × 4 threads took 102 s. Concentrating the same cores into one rank costs much more: at 47k DOFs, one rank with 72 threads took 89 s against 27 s at 36 ranks × 2 threads. Critically, two nodes measured slower than one: 123 s for 36 ranks against 102 s for 18, both at 18 ranks per node, and four nodes recovered only to 76 s. The factorization communicates across the network from the second node onwards, while element assembly keeps scaling with the ranks, from 0.168 s to 0.064 s per Newton iteration over the same 18 to 72 ranks. Therefore, only add nodes when the factors no longer fit in one node's memory.\n\nwarning: Give MUMPS one BLAS thread\nAt 18 ranks, raising the BLAS thread count from 1 to 4 made the solve 6.3× slower. recommended_blas_threads already returns 1 for this layout, since it detects the ranks sharing the node.","title":"Launch layout"},{"category":"section","location":"tutorials/mpi_four_point_bending/#Scaling-notes","page":"MPI four-point bending","text":"The default density = 4 configuration has 160 cells. For density = d, the mesh extends to 10d^2 cells. Increase density substantially before timing parallel runs, and set vtk = nothing to avoid stress-output and filesystem costs.\n\ndensity = 224, 447 and 894 give 1.0M, 4.0M and 16.0M DOFs. All three ran on one node with K \\ residual, in 7.4 s, 30 s and 154 s, at 6.4 GB, 21.9 GB and 95 GB per rank with Hooke over one load step. The rank count has to fall as the mesh grows, here to 9, 4 and 1, because every rank holds its own factorization. At 16.0M DOFs distributed_solve exceeded the node at 18 ranks instead, each of them holding a 4.3 GB copy of K before its share of the factorization. The script prints a synchronized elapsed wall time at the very end on rank 0.\n\nCompare pure threading, pure MPI, and hybrid approaches on the target machine; see What MPI changes for what does and does not get distributed.","title":"Scaling notes"},{"category":"section","location":"tutorials/mpi_four_point_bending/#Running-locally-and-on-clusters","page":"MPI four-point bending","text":"For a minimal run without mpiexec and without VTK output, include the script and call main with small settings:\n\njulia --project=. --threads=1 -e \"include(ARGS[1]); main(; density=1, load_steps=1, hold_steps=0, recover_steps=0, vtk=nothing)\" examples/mpi_four_point_bending.jl\n\nIn the following, for running/timing without VTK and stress output, add -e \"include(ARGS[1]); main(; vtk=nothing)\" directly before examples/mpi_four_point_bending.jl.\n\nOn a single shared-memory node, consider timing one Julia process with several threads, since this assembles in parallel but solves the Newton system only once instead of once per MPI rank:\n\njulia --project=. --threads=8 examples/mpi_four_point_bending.jl\n\nFor MPI, start with one Julia thread per rank (see MPI troubleshooting if you encounter issues):\n\nmpiexec -n 8 julia --project=. --threads=1 examples/mpi_four_point_bending.jl\n\nHybrid MPI/threading is supported: each MPI rank uses its Julia threads to assemble its assigned cells in parallel (see Threading and MPI for the exact ownership rule). Keep number_of_ranks * threads_per_rank within the allocated CPU count:\n\nmpiexec -n 4 julia --project=. --threads=2 examples/mpi_four_point_bending.jl","title":"Running locally and on clusters"},{"category":"section","location":"tutorials/mpi_four_point_bending/#SLURM-launch","page":"MPI four-point bending","text":"On SLURM, submit the SLURM submission script from the repository root:\n\nsbatch examples/submit_four_point_bending.sh\n\nThe submission script was tested on the NHR@FAU clusters. It loads Open MPI and hwloc, configures MPI.jl for the loaded system MPI, instantiates the project, and runs examples/mpi_four_point_bending.jl with srun. The default srun MPI plugin is pmix_v3; set SRUN_MPI_TYPE to another value from srun --mpi=list if your cluster requires it. The SLURM submission script disables VTK output in the final srun command with -e 'include(ARGS[1]); main(; vtk=nothing)'. Remove the -e ... part to write PVD/VTU output, but then ensure WriteVTK.jl is available, e.g., with julia --project=. -e 'using Pkg; Pkg.add(\"WriteVTK\")'. Adjust the #SBATCH partition, task count, wall time, CPU layout, and module load line for your SLURM cluster before running.","title":"SLURM launch"},{"category":"section","location":"tutorials/mpi_four_point_bending/#SLURM-submission-script","page":"MPI four-point bending","text":"The submission script is rendered from examples/submit_four_point_bending.sh:\n\nusing Markdown\nimport FerriteSolidMechanics\npath = joinpath(pkgdir(FerriteSolidMechanics), \"examples\", \"submit_four_point_bending.sh\")\nMarkdown.parse(\"```sh\\n\" * read(path, String) * \"\\n```\")","title":"SLURM submission script"},{"category":"section","location":"tutorials/mpi_four_point_bending/#Plain-program","page":"MPI four-point bending","text":"The full script is rendered from examples/mpi_four_point_bending.jl:\n\nusing Markdown\nimport FerriteSolidMechanics\npath = joinpath(pkgdir(FerriteSolidMechanics), \"examples\", \"mpi_four_point_bending.jl\")\nMarkdown.parse(\"```julia\\n\" * read(path, String) * \"\\n```\")","title":"Plain program"},{"category":"section","location":"api_models/#Material-model-API","page":"Material model API","text":"This page documents the constructors of the bundled material models together with the PlaneStrain and PlaneStress wrappers that embed a 3D model into a 2D analysis. The assembler, the load handler, the material interface, and the remaining public functions are on the API reference page.\n\n","title":"Material model API"},{"category":"section","location":"api_models/#Material-models","page":"Material model API","text":"Each material model is a concrete AbstractMaterial subtype whose constructor signature is documented on its dedicated page within the Material models section. The grouped autodocs below provide the associated parameter docstrings.","title":"Material models"},{"category":"section","location":"api_models/#Dimensionality-wrappers","page":"Material model API","text":"The PlaneStrain and PlaneStress wrappers turn a 3D material model into a 2D one, with the mathematical details explained on the Wrappers page. The two extension points below (compute_PK1_3D and update_state_from_3D!) are exported as part of the stable API. A FiniteStrain material must implement both methods to be wrappable, since no universal conversion from deformation gradient to stress exists for finite-strain kinematics. A SmallStrain material and any AbstractHyperelastic subtype need neither: a generic fallback derives both hooks from material_response.","title":"Dimensionality wrappers"},{"category":"type","location":"api_models/#FerriteSolidMechanics.Hooke","page":"Material model API","text":"Hooke(E, nu)\n\nThree-dimensional isotropic linear elastic material. The stress is σ = C ⊡ ε with a constant tangent C, so the assembler treats Hooke cells as linear.\n\nParameters\n\nE – Young's modulus; higher values stiffen the elastic response\nnu – Poisson's ratio; controls lateral contraction under stretch\n\nReferences\n\nFerrite.jl documentation. Linear elasticity tutorial. https://ferrite-fem.github.io/Ferrite.jl/stable/tutorials/linear_elasticity/\n\n\n\n\n\n","title":"FerriteSolidMechanics.Hooke"},{"category":"type","location":"api_models/#FerriteSolidMechanics.Hooke2D","page":"Material model API","text":"Hooke2D(E, nu; plane_stress=false)\n\nTwo-dimensional isotropic linear elastic material. The stress is σ = C ⊡ ε with a constant tangent C, so the assembler treats Hooke2D cells as linear.\n\nParameters\n\nE – Young's modulus; higher values stiffen the elastic response\nnu – Poisson's ratio; controls lateral contraction under stretch\n\nKeyword arguments\n\nplane_stress – selects the plane stress constitutive matrix (σ_zz = 0); the default false selects plane strain (ε_zz = 0)\n\n\n\n\n\n","title":"FerriteSolidMechanics.Hooke2D"},{"category":"type","location":"api_models/#FerriteSolidMechanics.NeoHooke","page":"Material model API","text":"NeoHooke(E, nu)\n\nCompressible Neo–Hookean hyperelastic material parameterized by Young's modulus E and Poisson's ratio nu. The constructor stores the equivalent Lamé parameters μ (shear modulus) and λ.\n\nReferences\n\nFerrite.jl documentation. Hyperelasticity tutorial. https://ferrite-fem.github.io/Ferrite.jl/stable/tutorials/hyperelasticity/\n\n\n\n\n\n","title":"FerriteSolidMechanics.NeoHooke"},{"category":"type","location":"api_models/#FerriteSolidMechanics.ArrudaBoyce","page":"Material model API","text":"ArrudaBoyce(μ, κ, N; tangent=:AT)\n\nCompressible Arruda–Boyce eight-chain hyperelastic material. The network shear modulus μ and the limiting chain extensibility N parameterize the isochoric network response. The bulk modulus κ parameterizes the volumetric response.\n\nKeyword arguments\n\ntangent – chooses how the model forms ∂P/∂F: :AT (default) builds it from the analytic second Piola–Kirchhoff stress and its derivative, :AD obtains it by automatic differentiation of the PK1 map\n\nReferences\n\nE. M. Arruda, M. C. Boyce. A three-dimensional constitutive model for the large stretch behavior of rubber elastic materials. Journal of the Mechanics and Physics of Solids 41(2) (1993) 389-412. https://doi.org/10.1016/0022-5096(93)90013-6\n\n\n\n\n\n","title":"FerriteSolidMechanics.ArrudaBoyce"},{"category":"type","location":"api_models/#FerriteSolidMechanics.MooneyRivlin","page":"Material model API","text":"MooneyRivlin(C10, C01, κ; tangent=:AD)\n\nCompressible Mooney–Rivlin hyperelastic material. The isochoric response is linear in the first two modified invariants, with coefficients C10 and C01. The bulk modulus κ parameterizes the volumetric response.\n\nKeyword arguments\n\ntangent – chooses how the model forms ∂P/∂F: :AD (default) obtains it by automatic differentiation of the PK1 map, :AT builds it from the analytic second Piola–Kirchhoff stress and its derivative\n\nReferences\n\nM. Mooney. A theory of large elastic deformation. Journal of Applied Physics 11(9) (1940) 582-592. https://doi.org/10.1063/1.1712836\nR. S. Rivlin. Large elastic deformations of isotropic materials. IV. further developments of the general theory. Philosophical Transactions of the Royal Society of London. Series A, Mathematical and Physical Sciences 241(835) (1948) 379-397. https://doi.org/10.1098/rsta.1948.0024\n\n\n\n\n\n","title":"FerriteSolidMechanics.MooneyRivlin"},{"category":"type","location":"api_models/#FerriteSolidMechanics.Ogden","page":"Material model API","text":"Ogden(μ, α, κ)\n\nCompressible Ogden hyperelastic material with an arbitrary number of isochoric terms. The stress and tangent are analytic, in spectral form. The vectors μ and α hold one coefficient and one exponent per term, in the μ / α Ogden convention. Scalar μ and α values are accepted as a one-term shorthand. The bulk modulus κ parameterizes the volumetric response. The combined small-strain shear modulus 0.5 * sum(μ .* α) must be positive.\n\nReferences\n\nR. W. Ogden. Large deformation isotropic elasticity – on the correlation of theory and experiment for incompressible rubberlike solids. Proceedings of the Royal Society of London. Series A, Mathematical and Physical Sciences 326(1567) (1972) 565-584. https://doi.org/10.1098/rspa.1972.0026\nR. W. Ogden. Non-Linear Elastic Deformations. Ellis Horwood, 1984.\n\n\n\n\n\n","title":"FerriteSolidMechanics.Ogden"},{"category":"type","location":"api_models/#FerriteSolidMechanics.J2Plasticity","page":"Material model API","text":"J2Plasticity(E, nu, sigma0, H)\n\nSmall-strain von Mises plasticity with isotropic hardening.\n\nParameters\n\nE – Young's modulus; higher values stiffen the elastic response\nnu – Poisson's ratio; controls lateral contraction under stretch\nsigma0 – Initial yield limit; higher values delay the onset of plasticity\nH – Linear hardening modulus; larger values raise the post-yield stiffness\n\nReferences\n\nE. A. de Souza Neto, D. Perić, D. R. J. Owen. Computational Methods for Plasticity: Theory and Applications. Wiley, 2008.\nJ. C. Simo, T. J. R. Hughes. Computational Inelasticity. Springer, 1998.\nFerrite.jl documentation. Von Mises plasticity tutorial. https://ferrite-fem.github.io/Ferrite.jl/stable/tutorials/plasticity/\n\n\n\n\n\n","title":"FerriteSolidMechanics.J2Plasticity"},{"category":"type","location":"api_models/#FerriteSolidMechanics.VEPD_Detrez2010","page":"Material model API","text":"VEPD_Detrez2010(E, ν, R0, Q, b, α, β, n_ab, μ_ab, G, τ; plastic_update=:end_step, maxwell_update=:closed_form_cv)\n\nViscoelastic-plastic-damage material model after Detrez. Combines crystalline elasticity, network hyperelasticity, selectable Maxwell-branch updates, and isotropic hardening with damage.\n\nParameters\n\nE – Young's modulus of the crystalline phase; higher values stiffen the elastic stress response\nν – Poisson's ratio of the crystalline phase; controls lateral contraction under stretch\nR0 – Initial yield stress; higher values delay the onset of plasticity\nQ – Saturation modulus; higher values raise the asymptotic hardening limit\nb – Saturation exponent; larger values cause the yield stress to plateau at smaller plastic strains\nα – Damage scaling factor; sets the asymptotic damage level (value is capped at 1)\nβ – Damage exponent; larger values accelerate damage accumulation with plastic strain\nn_ab – Number of Kuhn segments per Arruda–Boyce chain; larger values soften the network at moderate stretches and push locking to higher stretches\nμ_ab – Network shear modulus; scales the Arruda–Boyce network stress contribution\nG – Vector of per-Maxwell-branch shear moduli; higher values raise the branch elastic stiffness\nτ – Vector of per-Maxwell-branch relaxation times; larger values delay branch equilibration\n\nKeyword arguments\n\nplastic_update – :end_step applies one return map at the end of the global step; :path_substepped applies the same return map over 8 deformation-path subincrements\nmaxwell_update – :closed_form_cv updates the viscous right Cauchy-Green branch state Cv in closed form; :objective_rate advances branch Cauchy stresses with a 16-substep objective rate update\n\nReferences\n\nF. Detrez, S. Cantournet, R. Séguéla. A constitutive model for semi-crystalline polymer deformation involving lamellar fragmentation. Comptes Rendus Mécanique 338(12) (2010) 681–687. https://doi.org/10.1016/j.crme.2010.10.008\n\nThe code-level reference for this implementation is an unpublished C routine that was kindly provided by Fabrice Detrez.\n\n\n\n\n\n","title":"FerriteSolidMechanics.VEPD_Detrez2010"},{"category":"type","location":"api_models/#FerriteSolidMechanics.VEVP_Zhao2021_AD","page":"Material model API","text":"VEVP_Zhao2021_AD(μE, κ, NV, β, α, μVe, μVN, τ̂₁, τ̂N, m₁, mN, τcut₁, τcutN, μV;\n           dt_scale=1.0)\n\nViscoelastic-viscoplastic material model after Zhao with automatic differentiation for the algorithmic tangent. Uses multiple inelastic modules and a semi-implicit internal-variable update.\n\nNote: not interchangeable with VEVP_Zhao2021_AT\n\nUses the same parameterization and semi-implicit update structure as VEVP_Zhao2021_AT, but builds the final PK2 stress from a different basis. This struct uses the converged SVⱼ (new μVₖ₊₁, new Cᵢₖ₊₁), so the returned stress is colocated with the AD tangent. VEVP_Zhao2021_AT uses the trial SV_trial{j} (old μVₖ, old Cᵢₖ_inv), matching the CAPRICCIO/Zhao analytical-tangent path. The AD path also applies small determinant and principal stretch floors. The same F, dt, parameters, and history state therefore give different PK2 stresses from the two models.\n\nReferences\n\nW. Zhao, M. Ries, P. Steinmann, S. Pfaller. A viscoelastic-viscoplastic constitutive model for glassy polymers informed by molecular dynamics simulations. International Journal of Solids and Structures 226–227 (2021) 111071. https://doi.org/10.1016/j.ijsolstr.2021.111071\nW. Zhao, R. Xiao, S. Pfaller, P. Steinmann. Modeling strain hardening in glassy polymers based on the microscopic mechanisms revealed by molecular dynamic simulations. Journal of the Mechanics and Physics of Solids 206 (2026) 106384. https://doi.org/10.1016/j.jmps.2025.106384\n\nThe implementation-level reference for this code path is part of the CAPRICCIO FE–MD coupling tool. This file is a translated/adapted Julia implementation with package-specific changes and explicit permission for MIT distribution in FerriteSolidMechanics.jl: Pfaller, S., Ries, M., Zhao, W., Bauer, C., Weber, F., & Laubert, L. CAPRICCIO - Tool to run concurrent Finite Element-Molecular Dynamics Simulations (Version 2.0.1), Zenodo (2024). https://doi.org/10.5281/zenodo.12606758\n\nParameters\n\nNV must be at least 2 as branch parameters are interpolated between the branch-1 and branch-N end points.\n\nμE – Elastic branch shear modulus\nκ – Bulk modulus\nNV – Number of inelastic modules (requires NV ≥ 2; module parameters are interpolated between the module-1 and module-NV endpoints)\nβ – Yield-threshold softening coefficient; larger values drop the threshold sooner\nα – Inelastic shear modulus evolution rate; higher values approach μVe faster\nμVe – Target total inelastic shear modulus approached by the μV evolution\nμVN – Module-NV shear modulus endpoint; near zero gives uniform modules\nτ̂₁, τ̂N – Flow-resistance stress-scale endpoints in the power-flow law; larger values reduce the viscoplastic flow rate\nm₁, mN – Stress exponents in the power-flow law; larger values increase the nonlinearity of the viscoplastic rate\nτcut₁, τcutN – Initial yield thresholds for the modules; subtracted from the trial-stress ratio before the power-law ramp\nμV – Initial total inelastic shear modulus\n\nKeyword arguments\n\ndt_scale – multiplies dt inside the constitutive update so the time step is expressed in the unit used during parameter calibration. Default 1.0 leaves dt unscaled. When the calibration unit and the driver unit differ, set it to the conversion factor: parameters calibrated in seconds with a driver passing dt in nanoseconds give dt_scale = 1e-9.\n\n\n\n\n\n","title":"FerriteSolidMechanics.VEVP_Zhao2021_AD"},{"category":"type","location":"api_models/#FerriteSolidMechanics.VEVP_Zhao2021_AT","page":"Material model API","text":"VEVP_Zhao2021_AT(μE, κ, NV, β, α, μVe, μVN, τ̂₁, τ̂N, m₁, mN, τcut₁, τcutN, μV;\n           dt_scale=1.0)\n\nViscoelastic-viscoplastic material model after Zhao with an assemble-time constitutive update. Provides an analytically derived stiffness matrix for finite-strain problems.\n\nNote: not interchangeable with VEVP_Zhao2021_AD\n\nUses the same parameterization and semi-implicit update structure as VEVP_Zhao2021_AD, but builds the final PK2 stress from a different basis. This struct uses the trial SV_trial{j} (old μVₖ, old Cᵢₖ_inv) with a hand-derived analytical tangent. VEVP_Zhao2021_AD uses the converged SVⱼ (new μVₖ₊₁, new Cᵢₖ₊₁) and obtains the tangent by AD. The same F, dt, parameters, and history state therefore give different PK2 stresses from the two models.\n\nAn archived Matrix-storage translation of the CAPRICCIO/Zhao routines is preserved as FerriteSolidMechanics.Experimental.VEVP_Zhao2021_AT_Matlab.\n\nReferences\n\nW. Zhao, M. Ries, P. Steinmann, S. Pfaller. A viscoelastic-viscoplastic constitutive model for glassy polymers informed by molecular dynamics simulations. International Journal of Solids and Structures 226-227 (2021) 111071. https://doi.org/10.1016/j.ijsolstr.2021.111071\nW. Zhao, R. Xiao, S. Pfaller, P. Steinmann. Modeling strain hardening in glassy polymers based on the microscopic mechanisms revealed by molecular dynamic simulations. Journal of the Mechanics and Physics of Solids 206 (2026) 106384. https://doi.org/10.1016/j.jmps.2025.106384\n\nThe implementation-level reference for this code path is part of the CAPRICCIO FE–MD coupling tool. This file is a translated/adapted Julia implementation with package-specific changes and explicit permission for MIT distribution in FerriteSolidMechanics.jl: Pfaller, S., Ries, M., Zhao, W., Bauer, C., Weber, F., & Laubert, L. CAPRICCIO - Tool to run concurrent Finite Element-Molecular Dynamics Simulations (Version 2.0.1), Zenodo (2024). https://doi.org/10.5281/zenodo.12606758\n\nParameters\n\nNV must be at least 2 as branch parameters are interpolated between the branch-1 and branch-N end points.\n\nμE – Elastic branch shear modulus\nκ – Bulk modulus\nNV – Number of inelastic modules (requires NV ≥ 2; module parameters are interpolated between the module-1 and module-NV endpoints)\nβ – Yield-threshold softening coefficient; larger values drop the threshold sooner\nα – Inelastic shear modulus evolution rate; higher values approach μVe faster\nμVe – Target total inelastic shear modulus approached by the μV evolution\nμVN – Module-NV shear modulus endpoint; near zero gives uniform modules\nτ̂₁, τ̂N – Flow-resistance stress-scale endpoints in the power-flow law; larger values reduce the viscoplastic flow rate\nm₁, mN – Stress exponents in the power-flow law; larger values increase the nonlinearity of the viscoplastic rate\nτcut₁, τcutN – Initial yield thresholds for the modules; subtracted from the trial-stress ratio before the power-law ramp\nμV – Initial total inelastic shear modulus\n\nKeyword arguments\n\ndt_scale – multiplies dt inside the constitutive update so the time step is expressed in the unit used during parameter calibration. Default 1.0 leaves dt unscaled. When the calibration unit and the driver unit differ, set it to the conversion factor: parameters calibrated in seconds with a driver passing dt in nanoseconds give dt_scale = 1e-9.\n\n\n\n\n\n","title":"FerriteSolidMechanics.VEVP_Zhao2021_AT"},{"category":"type","location":"api_models/#FerriteSolidMechanics.VEVP_MOAMMM","page":"Material model API","text":"VEVP_MOAMMM(order, KK_inf, GG_inf, alpha, nu_p, eta, p_exp,\n              sigmac0, hc1, hc2, hcexp,\n              sigmat0, ht1, ht2, htexp,\n              hb0, hb1, hb2,\n              KK, k, GG, g)\n\nViscoelastic-viscoplastic material model from the MOAMMM project with automatic differentiation. Combines a logarithmic strain viscoelastic update, a Drucker–Prager yield surface, isotropic hardening through separate compression and tension yield stress evolution, and kinematic hardening.\n\nReferences\n\nV.-D. Nguyen, F. Lani, T. Pardoen, X. P. Morelle, L. Noels. A large strain hyperelastic viscoelastic-viscoplastic-damage constitutive model based on a multi-mechanism non-local damage continuum for amorphous glassy polymers. International Journal of Solids and Structures 96 (2016) 192–216. https://doi.org/10.1016/j.ijsolstr.2016.06.008\n\nThe code-level reference for this implementation is the Fortran UMAT umat.f from the moammmPublic code repository.\n\nnote: Note\nThe Nguyen et al. (2016) paper describes a full hyperelastic–viscoelastic–viscoplastic model with damage and softening. The translated Fortran UMAT (umat.f in the moammmPublic repository) implements only the damage-free, no-softening subset of that model. This Julia port implements the same subset.\n\nParameters\n\norder – Polynomial order for the logarithm/exponential approximations\nKK_inf – Equilibrium bulk modulus; long-term volumetric stiffness\nGG_inf – Equilibrium shear modulus; long-term deviatoric stiffness\nalpha – Drucker–Prager yield exponent; 1 → linear Drucker–Prager, 2 → quadratic\nnu_p – Plastic Poisson's ratio; controls volumetric–deviatoric coupling under plastic flow\neta – Viscoplastic viscosity; higher values delay rate-dependent flow\np_exp – Viscoplastic exponent; controls stress sensitivity of the viscoplastic rate\nsigmac0 – Initial compression yield limit; higher values delay compressive yield\nhc1 – Linear compression yield stress growth coefficient\nhc2 – Saturation compression yield stress increment\nhcexp – Compression yield stress saturation exponent; at γ = 1/h{c,exp}, the factor (1 − exp(−h{c,exp}·γ)) ≈ 0.63\nsigmat0 – Initial tension yield limit; higher values delay tensile yield\nht1 – Linear tension yield stress growth coefficient\nht2 – Saturation tension yield stress increment\nhtexp – Tension yield stress saturation exponent; at γ = 1/h{t,exp}, the factor (1 − exp(−h{t,exp}·γ)) ≈ 0.63\nhb0 – Constant term of the kinematic hardening polynomial that scales the total backstress rate\nhb1 – Linear term of the kinematic hardening polynomial; scales with plastic strain\nhb2 – Quadratic term of the kinematic hardening polynomial; scales with the square of plastic strain\nKK – Vector of per-branch bulk moduli; raise the volumetric branch stiffness\nk – Vector of per-branch volumetric relaxation times; larger values delay volumetric equilibration\nGG – Vector of per-branch shear moduli; raise the deviatoric branch stiffness\ng – Vector of per-branch deviatoric relaxation times; larger values delay deviatoric equilibration\n\n\n\n\n\n","title":"FerriteSolidMechanics.VEVP_MOAMMM"},{"category":"type","location":"api_models/#FerriteSolidMechanics.PlaneStrain","page":"Material model API","text":"PlaneStrain(model)\n\nWrap a 3D material model for a 2D plane strain analysis.\n\n\n\n\n\n","title":"FerriteSolidMechanics.PlaneStrain"},{"category":"type","location":"api_models/#FerriteSolidMechanics.PlaneStress","page":"Material model API","text":"PlaneStress(model; tol=1e-10, maxiter=20)\n\nWrap a 3D material model for a 2D plane stress analysis. A scalar Newton iteration solves the out-of-plane stretch locally at each quadrature point.\n\nKeyword arguments\n\ntol – Residual tolerance on P[3,3], relative to the stress scale max(norm(P3D), 1)\nmaxiter – Maximum number of local Newton iterations\n\n\n\n\n\n","title":"FerriteSolidMechanics.PlaneStress"},{"category":"function","location":"api_models/#FerriteSolidMechanics.compute_PK1_3D","page":"Material model API","text":"compute_PK1_3D(material, F, dt, state)\n\nReturn the 3D first Piola-Kirchhoff stress for the dimensional wrappers.\n\nExtension point. A cross-package hook. Any material that is to be wrapped by PlaneStrain or PlaneStress must provide a method.\n\n\n\n\n\n","title":"FerriteSolidMechanics.compute_PK1_3D"},{"category":"function","location":"api_models/#FerriteSolidMechanics.update_state_from_3D!","page":"Material model API","text":"update_state_from_3D!(state, material, F, dt)\n\nUpdate a wrapped material state after a 3D constitutive evaluation.\n\nExtension point. A cross-package hook. Any material that is to be wrapped by PlaneStrain or PlaneStress must provide a method. The method writes the material's current/trial variables for the converged 3D deformation gradient F, and a replacement-style state may return a new state value. The history commit still happens through update_state! and update_states!.\n\n\n\n\n\n","title":"FerriteSolidMechanics.update_state_from_3D!"},{"category":"section","location":"models/linear_elasticity/#Linear-Elasticity","page":"Linear Elasticity","text":"Standard isotropic linear elasticity (src/models/LinearElasticity.jl).\n\nTwo constructors are provided:\n\nHooke – 3D isotropic linear elasticity, parameterized by E and ν.\nHooke2D – 2D isotropic linear elasticity, with a plane_stress::Bool keyword. By default it builds a plane strain stiffness; pass plane_stress=true for the analytical plane stress matrix.","title":"Linear Elasticity"},{"category":"section","location":"models/linear_elasticity/#Mathematical-formulation","page":"Linear Elasticity","text":"The 3D Hooke model uses the fourth-order isotropic Lamé stiffness C_ijkl = lambdadelta_ijdelta_kl + muleftdelta_ikdelta_jl + delta_ildelta_jkright, where C_ijkl maps strain to stress, delta_ij is the Kronecker delta, and the indices ijkl run over the spatial dimensions. The Lamé parameters are lambda = Enu  leftleft1+nurightleft1-2nurightright and mu = E  left2left1+nurightright. Hooke2D uses the same Lamé form for plane strain and an analytical Voigt matrix for plane stress.","title":"Mathematical formulation"},{"category":"section","location":"models/linear_elasticity/#Implementation-details","page":"Linear Elasticity","text":"Hooke and Hooke2D are stateless, i.e., create_state returns NoState(). The stress given by σ = C : ε, using the constant material tangent C. The generic small-strain element routine accumulates (ε(δu_i) : C : ε(δu_j)) * α_dΩ at each quadrature point. Here, ε(δu_i) is the symmetric strain associated with basis function displacement δu_i, and α_dΩ is the quadrature weight including the alpha scaling and the independent volume measure. Hooke and Hooke2D implement the material-interface method is_linear with true, so the assembler preassembles K_linear once during create_assembler; subsequent stiffness_matrix calls only handle nonlinear cells and add K_linear * u to the residual.","title":"Implementation details"},{"category":"section","location":"models/linear_elasticity/#Constructor","page":"Linear Elasticity","text":"Hooke(E, ν)                       # 3D isotropic linear elasticity\nHooke2D(E, ν; plane_stress=false) # 2D plane strain\nHooke2D(E, ν; plane_stress=true)  # 2D plane stress","title":"Constructor"},{"category":"section","location":"models/linear_elasticity/#Hooke2D-analytical-plane-stress","page":"Linear Elasticity","text":"When Hooke2D is constructed with plane_stress=true, the stiffness matrix is built in Voigt form as\n\nC^mathrmps = fracE1-nu^2\nbeginpmatrix\n1  nu  0 \nnu  1  0 \n0  0  left1-nuright2\nendpmatrix\n\nand converted to a SymmetricTensor{4,2} via fromvoigt. Otherwise the plane strain stiffness is the standard Lamé form as\n\nC^mathrmpe_ijkl = lambdadelta_ijdelta_kl + mu\nleftdelta_ikdelta_jl + delta_ildelta_jkright\n\nwith lambda = Enu  leftleft1+nurightleft1-2nurightright and mu = E  left2left1+nurightright.","title":"Hooke2D analytical plane stress"},{"category":"section","location":"models/linear_elasticity/#Hooke2D-vs-PlaneStrain(Hooke)-/-PlaneStress(Hooke)","page":"Linear Elasticity","text":"Hooke2D is a direct analytical 2D form, while PlaneStrain(Hooke) / PlaneStress(Hooke) are the wrappers that take a 3D Hooke and reduce it to a 2D problem through 2D-to-3D embedding and, for plane stress, local Newton iteration plus static condensation (see Wrappers). For a linear elastic material the two approaches are mathematically equivalent: both use the same infinitesimal-strain stiffness, so the in-plane stresses agree to within numerical round-off (rtol ≈ 10⁻¹⁰) at any displacement level. The regression test in test/test_dimensionality_wrappers.jl verifies this on a single-element uniaxial load case.\n\nnote: Prefer `Hooke2D` for production 2D linear elasticity.\nThe generic wrappers always run through the nonlinear wrapper path: PlaneStrain(Hooke(...)) and PlaneStress(Hooke(...)) are treated as nonlinear by the assembler, even though the wrapped material is linear. This way, there's no preassembly into K_linear; the PlaneStrain / PlaneStress element routine is evaluated on every Newton iteration, and PlaneStress also solves the local out-of-plane condition. For linear 2D analyses, use Hooke2D directly unless you specifically want to exercise the generic wrapper contract.","title":"Hooke2D vs PlaneStrain(Hooke) / PlaneStress(Hooke)"},{"category":"section","location":"models/linear_elasticity/#Provenance","page":"Linear Elasticity","text":"The 3D Hooke element routine and related documentation are adapted from the Ferrite.jl linear elasticity tutorial [1]. The upstream copyright/license notice is retained in the repository LICENSE. Hooke2D is a package-local model with analytical plane strain and plane stress moduli.","title":"Provenance"},{"category":"section","location":"models/linear_elasticity/#References","page":"Linear Elasticity","text":"[1] Ferrite.jl documentation. Linear elasticity tutorial. https://ferrite-fem.github.io/Ferrite.jl/stable/tutorials/linear_elasticity/","title":"References"},{"category":"section","location":"models/ogden/#Ogden","page":"Ogden","text":"Ogden is a stateless finite-strain hyperelastic material model (src/models/Ogden.jl). It implements Ogden's isotropic hyperelastic model [1, 2, 3] with a quadratic volumetric penalty. The model accepts an arbitrary number of terms through the parameter vectors μ and α, while scalar arguments are treated as a one-term shorthand.","title":"Ogden"},{"category":"section","location":"models/ogden/#Mathematical-formulation","page":"Ogden","text":"The model uses the deformation gradient boldsymbolF, the right Cauchy-Green tensor boldsymbolC=boldsymbolF^mathrmTboldsymbolF, and the volume ratio J=detboldsymbolF=sqrtdetboldsymbolC. Further, let lambda_i denote the principal stretches, with lambda_i^2 being the eigenvalues of boldsymbolC. The implemented free energy density is defined as\n\nPsi(boldsymbolC)\n=\nsum_a=1^N_mathrmterms\nfracmu_aalpha_a\nleft\nJ^-alpha_a3\nsum_i=1^3lambda_i^alpha_a\n-3\nright\n+\nfrackappa2leftJ-1right^2 text\n\nThe first part is isochoric since J^-alpha_a3lambda_i^alpha_a=barlambda_i^alpha_a with barlambda_i=J^-13lambda_i. The second part is a quadratic volumetric penalty governed by the bulk modulus kappa. The implementation follows the mu_aalpha_a Ogden convention, so the infinitesimal shear modulus is\n\nmu_0\n=\nfrac12\nsum_a=1^N_mathrmtermsmu_aalpha_a text\n\nThe one-term case with alpha_1=2 reduces to MooneyRivlin with C_10=mu_12, C_01=0, and the same kappa.","title":"Mathematical formulation"},{"category":"section","location":"models/ogden/#Stress-evaluation","page":"Ogden","text":"For implementation, we define p_a=alpha_a2, so that lambda_i^alpha_a=left(lambda_i^2right)^p_a turns each stretch power into a power of an eigenvalue of boldsymbolC; the stretch sum becomes sum_ilambda_i^alpha_a=operatornametrleft(boldsymbolC^p_aright), evaluated without an explicit eigendecomposition into principal directions. Using spectral powers of boldsymbolC, the isochoric contribution of term a to the second Piola-Kirchhoff stress (PK2) reads\n\nboldsymbolS^mathrmiso_a\n=\nmu_a J^-alpha_a3\nleft\nboldsymbolC^p_a-1\n-\nfracoperatornametrleft(boldsymbolC^p_aright)3\nboldsymbolC^-1\nright text\n\nwhile the volumetric contribution is\n\nboldsymbolS_mathrmvol\n=\nkappa JleftJ-1rightboldsymbolC^-1 text\n\nThe second Piola-Kirchhoff stress is\n\nboldsymbolS\n=\nboldsymbolS_mathrmvol\n+\nsum_a=1^N_mathrmtermsboldsymbolS^mathrmiso_a text\n\nwhile the first Piola-Kirchhoff stress (PK1) used by the assembler is\n\nboldsymbolP=boldsymbolFboldsymbolS\n\nand the Cauchy stress is\n\nboldsymbolsigma\n=\nfrac1JboldsymbolPboldsymbolF^mathrmT text","title":"Stress evaluation"},{"category":"section","location":"models/ogden/#Implementation-details","page":"Ogden","text":"","title":"Implementation details"},{"category":"section","location":"models/ogden/#State-variables","page":"Ogden","text":"Ogden is rate-independent and stateless (create_state returns NoState()). The dt keyword is ignored.","title":"State variables"},{"category":"section","location":"models/ogden/#Tangent-formulation","page":"Ogden","text":"Ogden provides an exact analytical tangent through tangent=:AT. The tangent differentiates the spectral powers boldsymbolC^p_a through divided differences and uses the repeated-eigenvalue limit when two eigenvalues coincide. At the undeformed state boldsymbolC=boldsymbolI, and more generally when two principal stretches coincide, direct AD through the spectral powers can return non-finite derivatives. The analytical tangent avoids this by using divided differences with repeated-eigenvalue limits. No AD tangent option is therefore provided.","title":"Tangent formulation"},{"category":"section","location":"models/ogden/#Wrapper-paths","page":"Ogden","text":"PlaneStrain(Ogden(...)) and PlaneStress(Ogden(...)) are supported through Ogden-specific assembly methods. These methods call the same exact 3D PK1 stress and tangent as the direct 3D model, then extract or condense the 2D response, respectively. The public wrapper API is unchanged; however, this internal route is specific to Ogden because it avoids generic AD through spectral powers. Since the material has no history, update_state_from_3D! is a no-op.","title":"Wrapper paths"},{"category":"section","location":"models/ogden/#Constructor","page":"Ogden","text":"Ogden(μ, α, κ; tangent=:AT)\n\nμ and α can be real scalars, tuples, or vectors. They must contain the same nonzero number of finite terms. Every α entry must be nonzero, κ must be finite and positive, and the combined small-strain shear modulus 0.5 * sum(μ .* α) must be positive. Individual negative fitted terms are accepted when the combined small-strain shear modulus is positive. This constructor check does not guarantee ellipticity or stability over all finite-strains. The optional tangent keyword currently accepts only :AT; passing :AD raises an ArgumentError.","title":"Constructor"},{"category":"section","location":"models/ogden/#2D-usage","page":"Ogden","text":"material = PlaneStress(Ogden(μ, α, κ))   # plane stress\nmaterial = PlaneStrain(Ogden(μ, α, κ))   # plane strain\n\nFor wrapper mechanics, see Wrappers.","title":"2D usage"},{"category":"section","location":"models/ogden/#Model-parameters","page":"Ogden","text":"Constructor argument Symbol Description\nμ mu_a Term coefficient in the classic mu_aalpha_a Ogden convention\nα alpha_a Nonzero principal stretch exponent for each term\nκ kappa Bulk modulus\ntangent - Exact tangent route, currently :AT only","title":"Model parameters"},{"category":"section","location":"models/ogden/#Provenance-and-acknowledgements","page":"Ogden","text":"The constitutive equations follow Ogden's isotropic hyperelastic model [1]. Ogden also treated compressible rubberlike solids [2]; the decoupled form implemented here follows Flory [4] and Ogden [5]. The Julia implementation was written for FerriteSolidMechanics.jl; no external code was translated or adapted.\n\nThe implementation is maintained by Lukas Laubert. Sebastian Pfaller is gratefully acknowledged for proofreading this page.","title":"Provenance and acknowledgements"},{"category":"section","location":"models/ogden/#References","page":"Ogden","text":"[1] Ogden, R. W. (1972). Large deformation isotropic elasticity – on the correlation of theory and experiment for incompressible rubberlike solids. Proceedings of the Royal Society of London. Series A, Mathematical and Physical Sciences, 326(1567), 565-584. doi:10.1098/rspa.1972.0026\n\n[2] Ogden, R. W. (1972). Large deformation isotropic elasticity: on the correlation of theory and experiment for compressible rubberlike solids. Proceedings of the Royal Society of London. Series A, Mathematical and Physical Sciences, 328(1575), 567-583. doi:10.1098/rspa.1972.0096\n\n[3] Ogden, R. W. (1984). Non-Linear Elastic Deformations. Ellis Horwood. (Dover reprint, 1997).\n\n[4] Flory, P. J. (1961). Thermodynamic relations for high elastic materials. Transactions of the Faraday Society, 57, 829-838. doi:10.1039/TF9615700829\n\n[5] Ogden, R. W. (1978). Nearly isochoric elastic deformations: application to rubberlike solids. Journal of the Mechanics and Physics of Solids, 26(1), 37-57. doi:10.1016/0022-5096(78)90012-1","title":"References"},{"category":"section","location":"faq/#FAQ","page":"FAQ","text":"This page summarizes characteristics of the package in a Q&A format, specifically regarding the dimensionality wrappers, the alpha_value extension point, and the assembled output. If you have questions that are not addressed here, check the Concepts page or file an issue.","title":"FAQ"},{"category":"section","location":"faq/#General","page":"FAQ","text":"","title":"General"},{"category":"section","location":"faq/#Does-the-assembler-run-a-Newton-loop-for-me?","page":"FAQ","text":"No. The assembler returns (K, r) from (try_)stiffness_matrix; the user writes the convergence test and the u .-= K \\ r update. The tutorials demonstrate the typical Newton loop. The Concepts page details the exact scope of the assembler.","title":"Does the assembler run a Newton loop for me?"},{"category":"section","location":"faq/#What-does-dt-actually-do?","page":"FAQ","text":"The dt keyword is the load step time increment, using the unit convention of your material parameters. The assembler (invoked via (try_)stiffness_matrix) uses it to advance trial history. Assembly semantics are model-dependent:\n\nFor rate-independent materials (see the Stable models table), dt has no effect on the tangent. Recommendation: Pass the actual load step size anyway; if you ever swap a model for a rate-dependent one, your code does not need to change.\nFor rate-dependent materials, dt influences the local update (e.g. governing relaxation, creep, or viscoplastic flow). Setting dt = 0.0 freezes the rate-dependent state evolution; generally, this does not yield a rate-independent material response.","title":"What does dt actually do?"},{"category":"section","location":"faq/#Does-compute_stresses-advance-my-material-state?","page":"FAQ","text":"No for the bundled models. Use (try_)stiffness_matrix (or compute_forces) in the Newton loop to write trial state, call update_states! once after a converged load step, then call compute_stresses whenever you need stress output. compute_stresses reports the stress corresponding to the current assembler state (whether trial or committed) without advancing internal variables. The postprocessing hooks (material_stress, or the lower-level _compute_stress_qp) receive dt, but leave it at the default dt = 0.0: they must not advance state, and are not a substitute for (try_)stiffness_matrix or update_states!. However, passing different values for dt can be used as an advanced extension point.","title":"Does compute_stresses advance my material state?"},{"category":"section","location":"faq/#When-do-I-call-update_states!?","page":"FAQ","text":"Once per load step, after Newton has converged, before moving on to the next step. Do not call it inside the Newton loop. For stateless material models the call is a no-op.","title":"When do I call update_states!?"},{"category":"section","location":"faq/#How-do-I-reject-a-load-step?","page":"FAQ","text":"The bundled models do not reject steps themselves. If you implement line search or adaptive time stepping in your outer loop and want to roll the material state back, call revert_states!(assembler), which iterates over all quadrature point states and calls revert_state!.\n\nThe PlaneStress wrapper additionally remembers F33_previous, and the underlying revert_state! method on PlaneStressStateWrapper restores it.","title":"How do I reject a load step?"},{"category":"section","location":"faq/#How-do-I-reduce-dt-after-local-material-nonconvergence?","page":"FAQ","text":"Use try_stiffness_matrix(assembler, u; dt) in the Newton loop when the outer solver should treat local constitutive failures as a rejected step. It returns (converged=false, K=nothing, r=nothing, error=err) for failures that subtype LocalAssemblyFailure, such as VEPD_Detrez2010ConvergenceError, VEVP_MOAMMMConvergenceError, and PlaneStressConvergenceError. Other exceptions are rethrown.\n\nOn converged=false, restore the material states with revert_states!(assembler), restore u to the last accepted displacement, reduce dt using dt = reject_step!(controller, dt) or an equivalent policy, update the boundary conditions for the smaller trial step, rebuild any time-dependent external loads, and try again. Do not call update_states! until the retried step has converged. Use TimeStepController or an equivalent dt_min guard and rejection budget so a permanently failing step cannot retry forever. TimeStepController() defaults to a practically unbounded rejection budget, so pass a finite max_rejections when exhaustion should be enforced. See Adaptive time stepping and recoverable local failures for a full solver-loop skeleton.\n\nIn MPI runs, try_stiffness_matrix synchronizes failure states across all ranks to prevent deadlocks. Unrecoverable errors are thrown collectively. If a recoverable failure occurs on any rank, all ranks safely abort assembly and return converged=false. If the original failure happened on another rank, result.error is a RemoteAssemblyFailure marker on the ranks that did not fail locally. All ranks must then reject the step collectively.","title":"How do I reduce dt after local material nonconvergence?"},{"category":"section","location":"faq/#Plane-strain-and-plane-stress","page":"FAQ","text":"","title":"Plane strain and plane stress"},{"category":"section","location":"faq/#What-is-the-difference-between-Hooke2D-and-PlaneStrain(Hooke)?","page":"FAQ","text":"Hooke2D is a direct 2D analytical form – it takes (E, ν) and builds the appropriate 2D stiffness tensor (plane strain by default; plane_stress=true for the analytical plane stress matrix). It has no internal state and uses is_linear == true.\n\nPlaneStrain(Hooke) wraps the 3D Hooke and reduces it to plane strain by embedding the 2D gradient into 3D (F̄₃₃ = 1), calling compute_PK1_3D, and extracting the in-plane part. The wrapper always reports is_linear == false because the generic element path is unaware that the wrapped model is in fact linear.\n\nFor a linear problem, hence prefer using Hooke2D. It avoids the AD overhead of the PlaneStrain wrapper and the assembler preassembles its stiffness into the static K_linear.","title":"What is the difference between Hooke2D and PlaneStrain(Hooke)?"},{"category":"section","location":"faq/#Why-is-PlaneStress-always-treated-as-nonlinear?","page":"FAQ","text":"Because it has to solve for the out-of-plane stretch F̄₃₃ at every quadrature point such that the 3D PK1 satisfies P̄₃₃ = 0. The local Newton iteration runs inside the wrapper's material_response at every quadrature point, with configurable controls (PlaneStress(model; maxiter=20, tol=1e-10)). The wrapper therefore reports is_linear == false even when the wrapped 3D material is linearly elastic. Consequently, K_linear is never filled for cells using PlaneStress.\n\nIf the local Newton solve does not converge, or if the local derivative needed for the Newton update or tangent condensation is too small, assembly raises a PlaneStressConvergenceError instead of committing a non-converged plane stress state. Use try_stiffness_matrix when an outer load step controller should receive a failure flag and reduce dt instead of handling an exception.","title":"Why is PlaneStress always treated as nonlinear?"},{"category":"section","location":"faq/#PlaneStress-is-slow.-What-can-I-do?","page":"FAQ","text":"The practical options to speed up the computations are:\n\nLoosen the local Newton tolerance. You can configure the local Newton solver via PlaneStress(model; tol=1e-6). A looser tolerance requires fewer iterations per quadrature point, which speeds up the evaluation at the cost of satisfying the out-of-plane zero-stress condition less accurately.\nImplement a direct 2D analytical formulation. If the plane stress equations for your material can be derived analytically, write a custom 2D material model (e.g. My2DMaterial <: AbstractMaterial) that directly evaluates the 2D stress and tangent. This bypasses the numerical condensation and is compatible with the assembler.","title":"PlaneStress is slow. What can I do?"},{"category":"section","location":"faq/#My-stress-output-looks-like-it-dropped/increased-a-factor.-What-happened?","page":"FAQ","text":"Two possible causes:\n\nalpha_value is not 1. If you supplied a non-nothing ah to create_assembler and the alpha values at some QPs are 0 or very small, the stress at those QPs is silently scaled. Inspect alpha_value(av, qp) for the QPs in question.\nthickness mismatch. The element residual and stiffness are multiplied by thickness (passed as a keyword argument to create_assembler, default is 1.0). If you pass a value other than 1.0, your reaction forces (which are derived from the residual) are scaled accordingly. thickness acts as a geometric multiplier during the 2D volume integration to compute forces, while Cauchy stress is a physical property independent of plate thickness. Therefore, compute_stresses always returns the true physical stress, while the integrated nodal reaction forces scale with thickness.","title":"My stress output looks like it dropped/increased a factor. What happened?"},{"category":"section","location":"faq/#The-alpha_value-extension-point","page":"FAQ","text":"","title":"The alpha_value extension point"},{"category":"section","location":"faq/#What-is-alpha_value?","page":"FAQ","text":"alpha_value returns a scalar for one quadrature point, which the generic element routine multiplies into the integration weight:\n\nα_dΩ = alpha_value(alphavalues, qp) * getdetJdV(cellvalues, qp)\n\nThe package defines a single fallback method alpha_value(::Nothing, ::Int) = 1.0. The assembler creates an alphavalues object for every CellValues when (and only when) the user passes a non-nothing ah to create_assembler. The provided extension hook is\n\nFerriteSolidMechanics.create_alpha_values(ah::MyAlphaSource, cv)\n\nThe fallback also looks for an AlphaValues(ah, cv) constructor in ah's defining module. The returned object is reinitialized with Ferrite.reinit!(av, cellid) before each cell is assembled or postprocessed.","title":"What is alpha_value?"},{"category":"section","location":"faq/#What-is-it-*for*?","page":"FAQ","text":"The intended use is quadrature point stiffness scaling, such as coupling to external scalar fields or reducing the material stiffness by a fixed scalar factor at each quadrature point. Element routines multiply α into the residual and element stiffness tangent, and compute_stresses applies the same factor to the postprocessed stress.","title":"What is it for?"},{"category":"section","location":"faq/#Multithreading","page":"FAQ","text":"","title":"Multithreading"},{"category":"section","location":"faq/#How-do-threads-come-into-play?","page":"FAQ","text":"FerriteSolidMechanics automatically runs element assembly in parallel across all available Julia threads. You do not need to change any code to enable this. Simply start your script with multiple threads (e.g., julia -t 4 or by setting the JULIA_NUM_THREADS=4 environment variable).\n\nWhen you call (try_)stiffness_matrix, the package distributes the nonlinear cells across threads using a dynamic task scheduler (OhMyThreads.@tasks). The element matrices are computed in parallel and then safely scattered into the global sparse stiffness matrix K. Similarly, the state commit phase (update_states!) is also parallelized across threads.\n\nThe generic assembler handles all thread-safety internally by pre-allocating dedicated workspaces and cell buffers for each thread during create_assembler.","title":"How do threads come into play?"},{"category":"section","location":"faq/#Hooking-up-MPI","page":"FAQ","text":"","title":"Hooking up MPI"},{"category":"section","location":"faq/#Do-I-have-to-do-anything-to-enable-MPI?","page":"FAQ","text":"No. The assembler automatically detects whether MPI is active. If your script calls MPI.Init(), the per-rank results are summed across MPI.COMM_WORLD after every (try_)stiffness_matrix and compute_stresses call. If MPI.Init() has not been called, the MPI reductions are no-ops and the package runs perfectly sequentially without throwing errors. If your MPI run fails during initialization or reports one rank per process, see MPI troubleshooting.","title":"Do I have to do anything to enable MPI?"},{"category":"section","location":"faq/#Does-MPI-parallelize-the-linear-solve?","page":"FAQ","text":"Not by default, but distributed_solve does. FerriteSolidMechanics partitions the element-local assembly work, including the constitutive updates at quadrature points. After assembly, the full sparse matrix and residual are available on every rank. By default, every rank then performs the same replicated solving step (e.g., K \\ residual) in the outer Newton loop, so MPI accelerates the constitutive assembly while each rank builds the whole factorization. For this replicated path, LinearAlgebra.BLAS.set_num_threads(recommended_blas_threads(dh)) speeds up the solve in 3D problems.\n\ndistributed_solve(K, residual, MPI.COMM_WORLD) replaces K \\ residual and splits a single MUMPS factorization across the ranks. The method comes from a package extension: add MUMPS.jl to the environment and load it with import MUMPS, otherwise the call raises a MethodError. Measured on 3D hexahedral NeoHooke at 108k DOFs, distributed_solve on 18 ranks × 4 threads took 25 s and 2.1 GB per rank against 145 s and 20.7 GB for the replicated solve on 1 rank × 72 threads. The two cost the same per solve at about 3k DOFs in 3D, and in 2D the replicated solve is faster up to roughly 1M DOFs. recommended_solve_settings applies these rules to a given grid; Performance and parallel execution and the MPI four-point bending tutorial cover the measurements and the rank, thread, and BLAS layout for each solver.","title":"Does MPI parallelize the linear solve?"},{"category":"section","location":"faq/#How-are-cells-partitioned-across-ranks?","page":"FAQ","text":"Via mod(cellid - 1, nranks) == rank. This is a static, round-robin partition by cell id. There is no load balancing: if some cells are much more expensive than others, the computational load per rank may be imbalanced. The static partition guarantees that a specific cell is always processed by the exact same rank across all assembly calls throughout the entire simulation, preserving its local history state.","title":"How are cells partitioned across ranks?"},{"category":"section","location":"faq/#Output","page":"FAQ","text":"","title":"Output"},{"category":"section","location":"faq/#What-is-the-output-of-the-compute_stresses-function?","page":"FAQ","text":"The compute_stresses function returns the assembler's reusable stress cache matrix of size (max_nquadpoints, ncells). The value max_nquadpoints is the maximum number of quadrature points across the grid, and ncells is the total number of cells. The second dimension of the returned cache matrix corresponds to the Ferrite cell ID. The elements of this matrix are of type Tensor{2,2,Float64,4} in 2D and Tensor{2,3,Float64,9} in 3D. Because it's a reusable cache, subsequent compute_stresses calls overwrite it; use copy(stresses) if you need to retain the result.","title":"What is the output of the compute_stresses function?"},{"category":"section","location":"faq/#What-are-the-dimensions-of-the-global-stiffness-matrix-K?","page":"FAQ","text":"The (try_)stiffness_matrix function returns a SparseMatrixCSC{Float64,Int} representing the global stiffness matrix K. K has dimensions of ndofs(dh) × ndofs(dh). Its sparsity pattern is dictated by the ConstraintHandler. Constrained degrees of freedom (DOFs) are allocated, while their corresponding rows and columns should be zeroed out by apply_zero! (see e.g. the Plate with hole tutorial).","title":"What are the dimensions of the global stiffness matrix K?"},{"category":"section","location":"faq/#Performance","page":"FAQ","text":"","title":"Performance"},{"category":"section","location":"faq/#Should-I-use-a-Vector{Int}-or-Int-for-quadrature_order?","page":"FAQ","text":"A scalar Int is broadcast to every sub-dof-handler. A Vector{Int} assigns the specified quadrature orders directly to the sub-dof-handlers in declaration order. For example, in mixed-element grids (e.g., quads + triangles), you can use the vector form to set a different quadrature order for each element type.","title":"Should I use a Vector{Int} or Int for quadrature_order?"},{"category":"section","location":"faq/#Why-is-my-first-Newton-step-slow?","page":"FAQ","text":"This is almost entirely due to Julia's just-in-time (JIT) compilation. When you run (try_)stiffness_matrix for the first time, Julia compiles the element routine. This compilation cost is paid only once; subsequent Newton steps will be significantly faster. To benchmark your assembly, warm up with a (try_)stiffness_matrix(assembler, u0) call on a zero u0 before timing.","title":"Why is my first Newton step slow?"},{"category":"section","location":"faq/#Does-the-assembler-store-simulation-state?","page":"FAQ","text":"Yes. The assembler holds the material's history state (e.g., plastic strain) for every cell. During Newton iterations, (try_)stiffness_matrix updates a temporary trial state, and update_states! commits the converged state for the next load step. If you need to start a completely fresh simulation, you must build a new assembler to reset this history.","title":"Does the assembler store simulation state?"},{"category":"section","location":"faq/#Are-Plots-and-WriteVTK-required?","page":"FAQ","text":"No. They are optional example-output dependencies. Core assembly, material models, tests, and stress postprocessing do not use Plots.jl or WriteVTK.jl directly. Install Plots.jl before calling plot_results, and install WriteVTK.jl before running tutorials that export data, such as the DMA tutorial.","title":"Are Plots and WriteVTK required?"},{"category":"section","location":"models/vepd_detrez2010/#VEPD-Detrez-2010","page":"VEPD Detrez 2010","text":"VEPD_Detrez2010 is a finite-strain viscoelastic-plastic damage model (src/models/VEPD_Detrez2010.jl), implemented from a C reference [2] and adapted with Julia-specific integration choices. It couples a St. Venant–Kirchhoff crystalline phase, an Arruda–Boyce network phase, multiple Maxwell branches, and isotropic hardening with damage. Figure 1 sketches its rheological layout.\n\n(Image: Rheological representation of the Detrez VEPD model)\n\nFigure 1. Rheological representation of the VEPD Detrez 2010 model with crystalline elasto-plastic damage, equilibrium network, and Maxwell branch contributions.","title":"VEPD Detrez 2010"},{"category":"section","location":"models/vepd_detrez2010/#Mathematical-formulation","page":"VEPD Detrez 2010","text":"Detrez et al. [1] split the free energy and dissipation into an intra-lamellar crystalline contribution and an entropic-network contribution as\n\nvarphi = varphi^mathrmcs + varphi^mathrmnet\nqquad\nphi = phi^mathrmcs + phi^mathrmnet\n\nwith varphi denoting the free energy and phi the dissipation potential. The Cauchy stress is assembled as the sum of the crystalline cohesive stress, the equilibrium network stress, and the Maxwell-branch stresses as\n\nboldsymbolsigma\n=\nboldsymbolsigma^mathrmcs\n+ boldsymbolsigma^mathrmnet\n+ boldsymbolsigma^mathrmvisco text","title":"Mathematical formulation"},{"category":"section","location":"models/vepd_detrez2010/#1.-Crystalline-phase-with-plasticity-and-damage","page":"VEPD Detrez 2010","text":"The crystalline cohesive part uses the multiplicative split\n\nboldsymbolF\n=\nboldsymbolF^mathrmeboldsymbolF^mathrmp\nqquad\nboldsymbolC^mathrme\n=\nleftboldsymbolF^mathrmeright^TboldsymbolF^mathrme quadtextand\nqquad\nboldsymbolE^mathrme\n=\nfrac12leftboldsymbolC^mathrme-boldsymbolIright text\n\nHere, boldsymbolF is the total deformation gradient, boldsymbolF^mathrme and boldsymbolF^mathrmp are its elastic and plastic parts, boldsymbolC^mathrme is the elastic right Cauchy-Green tensor, boldsymbolE^mathrme is the elastic Green-Lagrange strain, and boldsymbolI is the identity tensor. The operators operatornametr(cdot) and operatornamedev(cdot) denote the trace and deviatoric part.\n\nWith Lamé parameters lambda_E and mu_E computed from the crystalline Young's modulus E and Poisson's ratio ν, the damage-free crystalline second Piola-Kirchhoff stress renders as\n\nboldsymbolS^mathrmc\n=\nlambda_mathrmEoperatornametr(boldsymbolE^mathrme)boldsymbolI\n+ 2mu_mathrmEboldsymbolE^mathrme text\n\nwhile the scalar damage variable D follows the plastic strain coupling of Detrez et al. [1]. With accumulated plastic strain p and damage parameters alpha and beta, the Julia value is capped at full damage,\n\nD(p)\n=\nminleft(1alphaleft1-e^-beta prightright) text\n\nThe crystalline Cauchy stress is\n\nboldsymbolsigma^mathrmcs\n=\nleft1-Drightfrac1J^mathrme\nboldsymbolF^mathrmeboldsymbolS^mathrmcleftboldsymbolF^mathrmeright^T text\n\nThe denominator J^mathrme=detboldsymbolF^mathrme is the elastic Jacobian – the volume change between the intermediate and the reference configuration. The implementation keeps both factors explicit: the crystalline Cauchy stress is scaled by left1-Dright and divided by J^mathrme in the push-forward.\n\nPlastic loading is checked with the elastically rescaled Mandel stress\n\nwidetildeboldsymbolSigma\n=\nfracboldsymbolC^mathrmeboldsymbolS^mathrmcJ^mathrme\nqquadtextandquad\nwidetildeSigma_mathrmeq\n=\nsqrtfrac32\noperatornamedevwidetildeboldsymbolSigma operatornamedevwidetildeboldsymbolSigma text\n\nThe factor 1J^mathrme rescales the elastic Mandel stress boldsymbolC^mathrmeboldsymbolS^mathrmc to the same volumetric frame as boldsymbolsigma^mathrmcs, so the yield residual f compares consistent state forces.\n\nWith initial yield stress R_0, saturation modulus Q, and saturation exponent b, the hardening stress R(p) and yield residual f are\n\nR(p)\n=\nR_0 + Qleft1-e^-bpright\nqquadtextandquad\nf\n=\nwidetildeSigma_mathrmeq - R(p) le 0 text\n\nFor plastic_update=:end_step, when f  10^-9E, a scalar Newton problem is solved for Delta p and the plastic deformation gradient is updated with the linear plastic map\n\nboldsymbolF^mathrmp_n+1\n=\nleftboldsymbolI+Delta pboldsymbolNrightboldsymbolF^mathrmp_n\nqquad\nboldsymbolN\n=\nfrac32fracoperatornamedevwidetildeboldsymbolSigma\nwidetildeSigma_mathrmeq text\nqquadtextandquad\np_n+1=p_n+Delta p text\n\nHere, boldsymbolN is the plastic flow direction, index n marks the start-of-step state, and n+1 the end-of-step state of an incremental update over Delta t. With plastic_update=:path_substepped, the deformation change over the global time step is split into 8 linear subincrements, and the scalar plastic return solve is applied successively at the end of each subincrement.","title":"1. Crystalline phase with plasticity and damage"},{"category":"section","location":"models/vepd_detrez2010/#2.-Equilibrium-network-stress","page":"VEPD Detrez 2010","text":"The Arruda–Boyce network contribution is evaluated from the isochoric first invariant\n\nbarI_1\n=\nJ^-23operatornametrboldsymbolC text\nqquadtextwherequad\nboldsymbolC = boldsymbolF^TboldsymbolF\nquadtextandquad\nJ=detboldsymbolF\n\nwith J as the total Jacobian and boldsymbolC as the total right Cauchy-Green tensor.\n\nThe derivative of the truncated Arruda–Boyce energy is\n\nfracpartial WpartialbarI_1\n=\nmu_mathrmab\nleft\nfrac12\n+ fracbarI_110n_mathrmab\n+ frac11barI_1^2350n_mathrmab^2+ frac19barI_1^31750n_mathrmab^3 + frac519barI_1^4134750n_mathrmab^4 right\n\nwhere W is the network energy, mu_mathrmab is the network shear modulus, and n_mathrmab is the number of Kuhn segments per chain.\n\nThe corresponding second Piola-Kirchhoff stress is\n\nboldsymbolS^mathrmnet\n=\n2fracpartial WpartialbarI_1\nJ^-23\nleft\nboldsymbolI- frac13operatornametr(boldsymbolC)boldsymbolC^-1 right text\n\nThis network contribution is kept deviatoric after push-forward as\n\nboldsymbolsigma^mathrmnet\n=\noperatornamedev\nleft(frac1J boldsymbolFboldsymbolS^mathrmnetboldsymbolF^T right) text","title":"2. Equilibrium network stress"},{"category":"section","location":"models/vepd_detrez2010/#3.-Viscoelastic-Maxwell-branches","page":"VEPD Detrez 2010","text":"Detrez et al. [1] write the Maxwell branch evolution in rate form as\n\noversetnablaboldsymbolsigma_i\n+ frac1tau_iboldsymbolsigma_i\n=\nG_imathbbJboldsymbolD text\n\nwhere the over-nabla denotes the objective stress rate, i is the Maxwell-branch index, G_i the branch shear modulus, tau_i the branch relaxation time, mathbbJ the deviatoric projection, boldsymbolD the rate-of-deformation tensor, and boldsymbolsigma_i the per-branch Cauchy stress. The code-level reference [2] integrates this rate equation with a fourth-order Runge-Kutta scheme, while this implementation exposes two Maxwell-branch update choices: The default maxwell_update=:closed_form_cv uses a closed-form update for the per-branch viscous metric, where boldsymbolC^mathrmv_in is branch i's start-of-step viscous right Cauchy-Green tensor, Delta t is the time increment, and boldsymbolC_n+1=boldsymbolF_n+1^TboldsymbolF_n+1 is the total right Cauchy-Green tensor at end-of-step, reading\n\nboldsymbolC^mathrmv_in+1\n=\ne^-Delta ttau_iboldsymbolC^mathrmv_in\n+ left1-e^-Delta ttau_irightboldsymbolC_n+1 text\n\nThe branch stresses are then assembled as\n\nboldsymbolsigma^mathrmvisco\n=\nsum_iG_ioperatornamedevleft(boldsymbolFleftboldsymbolC^mathrmv_in+1right^-1 boldsymbolF^T right) text\n\nWith the optional maxwell_update=:objective_rate, the per-branch Cauchy stress from the rate equation above is instead advanced with a fixed 16-substep fourth-order Runge-Kutta objective-rate update.","title":"3. Viscoelastic Maxwell branches"},{"category":"section","location":"models/vepd_detrez2010/#Implementation-details","page":"VEPD Detrez 2010","text":"VEPD_Detrez2010State stores the deformation gradient, plastic deformation gradient, accumulated plastic strain p, and per-branch viscous right Cauchy-Green tensors boldsymbolC_mathrmvi as current_* end-of-step values and previous_* committed start-of-step values; branch stresses are additionally stored for maxwell_update=:objective_rate.\n\nThe tangent is obtained via automatic differentiation (Tensors.gradient) over the local stress update.\n\nThe local integration uses a return mapping algorithm: a Newton–Raphson iteration on the yield residual, stopped at 1e-9 * E, combined with the selected Maxwell-branch update. If the return mapping does not converge, VEPD_Detrez2010ConvergenceError is thrown; adaptive outer solvers can catch this via try_stiffness_matrix and reduce the time step with TimeStepController or an equivalent policy.","title":"Implementation details"},{"category":"section","location":"models/vepd_detrez2010/#Constructor","page":"VEPD Detrez 2010","text":"# Positional constructor\nVEPD_Detrez2010(E, ν, R0, Q, b, α, β, n_ab, μ_ab, G, τ;\n           plastic_update=:end_step,\n           maxwell_update=:closed_form_cv)\n\nThe plastic_update and maxwell_update keyword arguments are optional (omit them for simple tests). G and τ are vectors with one entry per Maxwell branch. They must have the same length, and every relaxation time in τ must be positive.\n\nThe keyword-controlled update modes are as follows:\n\nKeyword Values Meaning\nplastic_update :end_step One plastic return map using the end-of-step deformation (default)\nplastic_update :path_substepped The same return map applied over 8 linear deformation-path subincrements\nmaxwell_update :closed_form_cv Closed-form update of boldsymbolC^mathrmv, the viscous right Cauchy-Green branch state (default)\nmaxwell_update :objective_rate Fixed 16-substep fourth-order Runge-Kutta objective-rate update of the Maxwell branch Cauchy stresses\n\nThe default plastic_update=:end_step, maxwell_update=:closed_form_cv is the fastest option. Use :path_substepped when plasticity and damage evolve strongly over coarse time increments, and consider :objective_rate when the Maxwell response behaves highly time step dependent. The plastic path-substepping count is hardcoded to 8; the Maxwell objective-rate substep count is hardcoded to 16. The non-default update modes add significant work per quadrature point, but can reduce sensitivity to coarse time stepping. If the calibrated response depends strongly on the loading history, it's recommended to test and compare the keyword-controlled update modes in identical load cases.","title":"Constructor"},{"category":"section","location":"models/vepd_detrez2010/#2D-usage","page":"VEPD Detrez 2010","text":"material = PlaneStress(VEPD_Detrez2010(E, ν, R0, Q, b, α, β, n_ab, μ_ab, G, τ))\nmaterial = PlaneStrain(VEPD_Detrez2010(E, ν, R0, Q, b, α, β, n_ab, μ_ab, G, τ))\n\nBoth wrappers run through the model's own compute_PK1_3D and update_state_from_3D! methods, which the finite-strain kinematics require. The keyword-controlled update modes apply unchanged under both wrappers. For wrapper mechanics, see Wrappers.","title":"2D usage"},{"category":"section","location":"models/vepd_detrez2010/#Model-parameters","page":"VEPD Detrez 2010","text":"Group Constructor argument Description\nElasticity E Young's modulus of the crystalline phase\nElasticity ν Poisson's ratio of the crystalline phase\nIsotropic hardening R0 Initial yield stress; higher values delay the onset of plasticity\nIsotropic hardening Q Saturation modulus; higher values raise the asymptotic hardening limit\nIsotropic hardening b Saturation exponent; larger values cause the yield stress to plateau at smaller plastic strains\nDamage α Damage scaling factor; sets the asymptotic damage level (value is capped at 1 in Julia)\nDamage β Damage exponent; larger values accelerate damage accumulation with plastic strain\nArruda–Boyce network n_ab Number of Kuhn segments per chain; larger values soften the network at moderate stretches and push locking to higher stretches\nArruda–Boyce network μ_ab Network shear modulus; scales the Arruda–Boyce network stress contribution\nMaxwell branches G Vector of branch shear moduli; higher values raise the branch elastic stiffness\nMaxwell branches τ Vector of branch relaxation times; larger values delay branch equilibration\nIntegration option plastic_update Selects end-step or path-substepped plastic return mapping\nIntegration option maxwell_update Selects closed-form boldsymbolC^mathrmv or objective-rate Maxwell update","title":"Model parameters"},{"category":"section","location":"models/vepd_detrez2010/#Provenance-and-acknowledgements","page":"VEPD Detrez 2010","text":"The code-level reference is an unpublished C implementation provided by Fabrice Detrez [2]. Fabrice Detrez gave explicit permission to use and translate that reference for the open-source FerriteSolidMechanics.jl implementation.\n\nThe implementation is maintained by Lukas Laubert. Fabrice Detrez is gratefully acknowledged for providing the original C implementation and rheological layout figure, for proofreading this page, and for clarifying details about the model formulation and parameterization.","title":"Provenance and acknowledgements"},{"category":"section","location":"models/vepd_detrez2010/#Main-differences-from-the-reference","page":"VEPD Detrez 2010","text":"The tangent modulus is obtained via automatic differentiation (Tensors.gradient) instead of using a pre-derived analytical stiffness.\nThe plastic_update and maxwell_update keyword options on the Julia constructor have no equivalent in the C routine [2].\n\n<!–- - The equilibrium network is the Arruda–Boyce/Cohen-series contribution above, while the optional C-reference network penalty, alternative HE_POTENTIALJ plug-ins, and vd time-scaling parameter are not constructor fields.–->","title":"Main differences from the reference"},{"category":"section","location":"models/vepd_detrez2010/#See-also","page":"VEPD Detrez 2010","text":"Developer guide – for the general material hooks.\nWrappers – for 2D usage.\nExperimental variants: VEPD_Detrez2010_Optimized, VEPD_Detrez2010_Implicit, VEPD_Detrez2010_ExactVisco, VEPD_Detrez2010_ClosedCVEndStep (see Experimental models).","title":"See also"},{"category":"section","location":"models/vepd_detrez2010/#References","page":"VEPD Detrez 2010","text":"[1] Detrez, F., Cantournet, S., & Séguéla, R. (2010). A constitutive model for semi-crystalline polymer deformation involving lamellar fragmentation. Comptes Rendus Mécanique, 338(12), 681–687. doi:10.1016/j.crme.2010.10.008\n\n[2] Code-level reference for the Julia implementation in this package is an unpublished C routine that was kindly provided by Fabrice Detrez.","title":"References"},{"category":"section","location":"models/vevp_moammm/#VEVP-MOAMMM","page":"VEVP MOAMMM","text":"VEVP_MOAMMM is a finite-strain viscoelastic–viscoplastic model with a logarithmic strain formulation, a Drucker–Prager yield surface, isotropic hardening through separate compression and tension yield stress evolution, and kinematic hardening. The implementation is translated from the Fortran reference [2] and is available in src/models/VEVP_MOAMMM.jl. Figure 1 sketches the rheological layout used by the model.\n\n(Image: Rheological representation of the MOAMMM VEVP model)\n\nFigure 1. Rheological representation of the VEVP MOAMMM model with generalized Maxwell branches coupled to Drucker-Prager/Perzyna viscoplasticity; shown in small-strain notation.","title":"VEVP MOAMMM"},{"category":"section","location":"models/vevp_moammm/#Mathematical-formulation","page":"VEVP MOAMMM","text":"The model comprises a generalized Maxwell viscoelastic part: an equilibrium branch and n non-equilibrium Maxwell branches act in parallel, while each Maxwell branch contains its spring-dashpot pair in series. This viscoelastic part is coupled to the viscoplastic part through the multiplicative split of the deformation gradient.","title":"Mathematical formulation"},{"category":"section","location":"models/vevp_moammm/#1.-Viscoelastic-part","page":"VEVP MOAMMM","text":"The model uses a logarithmic strain measure, namely the Hencky strain boldsymbolE_(0)^mathrmve, defined from the elastic right Cauchy-Green tensor boldsymbolC^e as\n\nboldsymbolE_(0)^mathrmve = frac12lnboldsymbolC^e text\n\nHere, boldsymbolC^e=leftboldsymbolF^eright^TboldsymbolF^e uses the elastic deformation gradient boldsymbolF^e, and J^mathrmve=detboldsymbolF^e=sqrtdetboldsymbolC^e is the elastic volume ratio used below. The operators operatornametr(cdot) and operatornamedev(cdot) denote the trace and deviatoric part. Each viscoelastic branch j in infty 1 dots n is assigned a bi-logarithmic quadratic potential function for the springs, with branch bulk modulus K_j and shear modulus G_j, given as\n\npsi_j(boldsymbolE_(0)^mathrmve) = fracK_j2ln^2J^mathrmve + G_j mathrmdevboldsymbolE_(0)^mathrmve  mathrmdevboldsymbolE_(0)^mathrmve text\n\nand a quadratic dissipation function for the dashpots, governing the evolution of the internal variables boldsymbolq_j. The evolution of the volumetric and deviatoric parts of boldsymbolq_j is respectively defined by the volumetric and deviatoric relaxation times tau_mathrmk_j and tau_mathrmg_j as\n\nmathrmtrdotboldsymbolq_j = frac3K_jtau_mathrmk_jmathrmtrboldsymbolE_(0)^mathrmve - frac1tau_mathrmk_jmathrmtrboldsymbolq_j quadtextand\n\nmathrmdevdotboldsymbolq_j = frac2G_jtau_mathrmg_jmathrmdevboldsymbolE_(0)^mathrmve - frac1tau_mathrmg_jmathrmdevboldsymbolq_j text\n\nwhile the overdot denotes a material time derivative. The conjugate viscoelastic stress measure is the corotational Kirchhoff stress boldsymboltau, assembled from the equilibrium branch (infty) and the non-equilibrium Maxwell branches as\n\nboldsymboltau = boldsymboltau_infty^0 + sum_j=1^nleft boldsymboltau_j^0 - boldsymbolq_j right text quad textwhere  boldsymboltau_j^0 = fracpartialpsi_jpartialboldsymbolE_(0)^mathrmve text\n\nThe Julia port follows the UMAT integration variables rather than storing the stress-like boldsymbolq_j of Nguyen et al. [1] directly. For each branch it stores a deviatoric tensor AA[j] and a volumetric scalar BB[j], updated in closed form from the logarithmic strain increment as\n\nboldsymbolA_jn+1\n=\ne^-Delta ttau_gjboldsymbolA_jn\n+ e^-Delta tleft2tau_gjright\noperatornamedev(boldsymbolE^mathrmve_n+1\n- boldsymbolE^mathrmve_n) quadtextand\n\nB_jn+1\n=\ne^-Delta ttau_kjB_jn\n+ e^-Delta tleft2tau_kjright\noperatornametr(boldsymbolE^mathrmve_n+1\n- boldsymbolE^mathrmve_n) text\n\nHere, boldsymbolA_j and B_j denote the mathematical branch variables stored as AA[j] and BB[j], while boldsymbolE^mathrmve_n and boldsymbolE^mathrmve_n+1 are the start-of-step and end-of-step logarithmic strains, and Delta t the time increment.\n\nThe code evaluates this update first with the trial viscoelastic strain increment for the yield check and, if viscoplastic flow occurs, again with the converged strain increment for the stored state.\n\nIn the stress assembly, AA[j] enters the deviatoric branch stress as 2 GG[j] AA[j], while BB[j] enters the volumetric branch stress as KK[j] BB[j] I (trace 3 KK[j] BB[j]).","title":"1. Viscoelastic part"},{"category":"section","location":"models/vevp_moammm/#2.-Viscoplastic-part","page":"VEVP MOAMMM","text":"Beyond the elastic limit, viscoplastic flow is triggered. The model uses a combined stress measure boldsymbolphi = boldsymboltau - boldsymbolb, where boldsymbolb is the corotational backstress tensor for kinematic hardening.\n\nThe yield function mathcalF is a generalized Drucker-Prager condition, written as a linear combination of the mean stress phi_mathrmpr=operatornametr(boldsymbolphi)3 and the von Mises equivalent stress phi_mathrmeq=sqrtfrac32operatornamedevboldsymbolphioperatornamedevboldsymbolphi as\n\nmathcalF(boldsymbolphi) = a_2phi_mathrmeq^alpha - a_1 phi_mathrmpr - a_0 text\n\nHere, alpha is the yield exponent, and the coefficients a_2 a_1 a_0 depend on the tension-compression asymmetry ratio and the current yield stresses for compression and tension, sigma_mathrmc and sigma_mathrmt.\n\nTo enable the accurate prediction of the Poisson effect during plastic deformation, a non-associated Perzyna-type viscoplastic flow rule boldsymbolD^mathrmvp is used, defined by the gradient of a quadratic flow potential P by\n\nP = phi_mathrmeq^2 + beta phi_mathrmpr^2 text\n\nwhere the parameter beta is derived from the plastic Poisson's ratio nu_mathrmp as beta = frac92frac1-2nu_mathrmp1+nu_mathrmp. The viscoplastic flow rate is then given by\n\nboldsymbolD^mathrmvp = frac1etalanglemathcalFrangle^frac1pfracpartialPpartialboldsymboltau\n\nwith the viscoplastic coefficient eta, the viscoplastic exponent p, and the Macaulay bracket langle xrangle=max(x0). The equivalent plastic strain gamma evolves according to\n\ndotgamma = frac1sqrt1 + 2 nu_mathrmp^2 sqrtboldsymbolD^mathrmvp  boldsymbolD^mathrmvp text\n\nThe translated implementation evaluates the compression and tension yield stresses directly as functions of the equivalent plastic strain:\n\nsigma_mathrmc(gamma)\n=\nsigma_mathrmc^0\n+ h_c1gamma+ h_c2left1-e^-h_cexpgammaright quadtextand\n\nsigma_mathrmt(gamma)\n=\nsigma_mathrmt^0\n+ h_t1gamma\n+ h_t2left1-e^-h_texpgammaright text\n\nHere, sigma_mathrmc^0 and sigma_mathrmt^0 are the initial compression and tension yield stresses; h_c1 and h_t1 are linear hardening coefficients, h_c2 and h_t2 are saturation magnitudes, and h_cexp and h_texp are saturation exponents.\n\nKinematic hardening follows a polynomial factor in the equivalent plastic strain. In the translated update this factor is evaluated from the start-of-step plastic strain gamma_0 using\n\ndotboldsymbolb\n=\nlefth_mathrmb0\n+ h_mathrmb1gamma_0+ h_mathrmb2gamma_0^2rightboldsymbolD^mathrmvp text\n\nThe coefficients h_mathrmb0, h_mathrmb1, and h_mathrmb2 are the constant, linear, and quadratic kinematic hardening factors.","title":"2. Viscoplastic part"},{"category":"section","location":"models/vevp_moammm/#Implementation-details","page":"VEVP MOAMMM","text":"","title":"Implementation details"},{"category":"section","location":"models/vevp_moammm/#State-layout","page":"VEVP MOAMMM","text":"VEVP_MOAMMMState stores the quadrature point state as typed fields rather than as one flat state vector. Each state variable is stored as a trial value and a committed start-of-step value: the current_* fields hold the state written after each constitutive update, and the matching previous_* fields are saved by update_state! and restored by revert_state!. The stored variables follow the original Fortran UMAT state-variable set [2] but are represented explicitly: Fvp stores the plastic deformation gradient, Eve stores the elastic logarithmic strain, gma stores the equivalent plastic strain, b stores the corotational backstress tensor, and the per-branch fields AA[j] and BB[j] store the deviatoric viscoelastic branch tensor and volumetric branch scalar for each Maxwell branch.","title":"State layout"},{"category":"section","location":"models/vevp_moammm/#Tangent-and-local-integration","page":"VEVP MOAMMM","text":"The quadrature point constitutive update performs a Newton–Raphson solve along the Perzyna flow law: It solves the overstress consistency equation for the viscoplastic multiplier, unless the trial state already satisfies Perzyna consistency ft ≤ 1e-11, in which case the predictor is accepted directly without entering the loop. The Newton loop has a fixed budget of maxiter = 500 iterations with a tolerance of 1e-11 on the residual f. If this budget is exhausted, VEVP_MOAMMMConvergenceError is thrown, and adaptive outer solvers can catch it via try_stiffness_matrix to reduce the time step with TimeStepController or an equivalent policy.\n\nThe tangent is obtained via automatic differentiation (Tensors.gradient), replacing the finite-difference perturbation of the original Fortran UMAT [2].","title":"Tangent and local integration"},{"category":"section","location":"models/vevp_moammm/#Logarithmic-strain","page":"VEVP MOAMMM","text":"The local constitutive update (material_response) uses a polynomial approximant of log for the elastic right Cauchy-Green tensor boldsymbolC^e=leftboldsymbolF^eright^TboldsymbolF^e and a polynomial approximant of exp for the plastic flow-map update. Both are controlled by the order parameter (see the Model parameters table for the trade-off between approximation accuracy and per-step cost).","title":"Logarithmic strain"},{"category":"section","location":"models/vevp_moammm/#Constructor","page":"VEVP MOAMMM","text":"VEVP_MOAMMM(order, KK_inf, GG_inf, alpha, nu_p, eta, p_exp,\n              sigmac0, hc1, hc2, hcexp,\n              sigmat0, ht1, ht2, htexp,\n              hb0, hb1, hb2,\n              KK, k, GG, g)\n\nThe number of Maxwell branches is inferred from the length of the four branch parameter vectors (KK, k, GG, g); all four must have the same length. Passing empty vectors sets nbr = 0, disabling the Maxwell branches. The constructor raises an ArgumentError if the vector lengths disagree or if eta or p_exp are non-positive.","title":"Constructor"},{"category":"section","location":"models/vevp_moammm/#2D-usage","page":"VEVP MOAMMM","text":"mp = VEVP_MOAMMM(order, KK_inf, GG_inf, ...)  # the 22 positional arguments listed above\nmaterial = PlaneStress(mp)\nmaterial = PlaneStrain(mp)\n\nBoth wrappers run through the model's own compute_PK1_3D and update_state_from_3D! methods, which the finite-strain kinematics require. For wrapper mechanics, see Wrappers.","title":"2D usage"},{"category":"section","location":"models/vevp_moammm/#Model-parameters","page":"VEVP MOAMMM","text":"ID Constructor argument Symbol Description\n1 order - Polynomial order for the logarithm/exponential approximations\n2 KK_inf K_infty Equilibrium bulk modulus\n3 GG_inf G_infty Equilibrium shear modulus\n4 alpha alpha Drucker–Prager yield exponent; 1 → linear Drucker-Prager, 2 → quadratic\n5 nu_p nu_mathrmp Plastic Poisson ratio\n6 eta eta Viscoplastic viscosity; higher values delay rate-dependent flow\n7 p_exp p Viscoplastic exponent; controls stress sensitivity of the viscoplastic rate\n8 sigmac0 sigma_mathrmc^0 Initial compression yield limit; higher values delay compressive yield\n9 hc1 h_c1 Linear compression yield stress growth coefficient\n10 hc2 h_c2 Saturation compression yield stress increment\n11 hcexp h_cexp Compression yield stress saturation exponent in the saturation factor left1 - e^-h_cexpgammaright; at gamma = 1h_cexp it is ≈ 63%\n12 sigmat0 sigma_mathrmt^0 Initial tension yield limit; higher values delay tensile yield\n13 ht1 h_t1 Linear tension yield stress growth coefficient\n14 ht2 h_t2 Saturation tension yield stress increment\n15 htexp h_texp Tension yield stress saturation exponent in the saturation factor left1 - e^-h_texpgammaright; at gamma = 1h_texp it is ≈ 63%\n16 hb0 h_mathrmb0 Constant term of the kinematic hardening polynomial that scales the total backstress rate\n17 hb1 h_mathrmb1 Linear term of the kinematic hardening polynomial; scales with plastic strain\n18 hb2 h_mathrmb2 Quadratic term of the kinematic hardening polynomial; scales with the square of plastic strain\n19 KK K_j Vector of bulk moduli, one per Maxwell branch (nbr = length(KK))\n20 k tau_mathrmk_j Vector of volumetric relaxation times, one per Maxwell branch\n21 GG G_j Vector of shear moduli, one per Maxwell branch\n22 g tau_mathrmg_j Vector of deviatoric relaxation times, one per Maxwell branch","title":"Model parameters"},{"category":"section","location":"models/vevp_moammm/#Provenance-and-acknowledgements","page":"VEVP MOAMMM","text":"The code-level reference is the finite-strain VEVP Fortran UMAT in the public moammmPublic repository [2]. Mohib Mustafa gave explicit permission to include the Julia translation in the open-source FerriteSolidMechanics.jl implementation after receiving approval from the MAOMMM project lead.\n\nThe implementation is maintained by Lukas Laubert. Mohib Mustafa is gratefully acknowledged for providing the original UMAT implementation and for clarifying details about the model formulation and parameterization.","title":"Provenance and acknowledgements"},{"category":"section","location":"models/vevp_moammm/#Main-differences-from-the-reference","page":"VEVP MOAMMM","text":"Similar to the reference UMAT [2], this port implements the damage-free, no-softening subset of Nguyen et al.'s full viscoelastic–viscoplastic–damage paper [1]. The main implementation differences from the Fortran umat.f reference [2] are:\n\nThe tangent is obtained via automatic differentiation (Tensors.gradient over the PK1 wrapper) instead of the Fortran's finite-difference perturbation (TOLL = 1e-3, six Voigt perturbations, 6 or 12 VEVP re-evaluations per call).\nThe state is stored as a typed trial/commit pair of named current_* / previous_* fields in VEVP_MOAMMMState rather than as a flat Abaqus STATEV array.\nThe Julia implementation infers the number of Maxwell branches from the branch parameter vectors, while the reference UMAT hardcodes eight branches.\nThe Julia implementation uses stricter local-solve settings (1e-11, maxiter = 500) than the reference UMAT (TOLL_G = 0.999e-6, MAX_i = 100).\nThe Julia implementation includes small numerical guards, for example for zero time increments and denominator lower bounds.\n\nThe underlying constitutive equations and the set of state variables follow the Fortran UMAT. For eight Maxwell branches, one state copy holds the same 108 scalar values; more generally, a copy holds 28 + 10 N_textbr values.","title":"Main differences from the reference"},{"category":"section","location":"models/vevp_moammm/#See-also","page":"VEVP MOAMMM","text":"Experimental variant with flat-vector state layout similar to the reference UMAT [2]: VEVP_MOAMMM_VarsN (see Experimental models).","title":"See also"},{"category":"section","location":"models/vevp_moammm/#References","page":"VEVP MOAMMM","text":"[1] Nguyen, V.-D., Lani, F., Pardoen, T., Morelle, X. P., & Noels, L. (2016). A large strain hyperelastic viscoelastic-viscoplastic-damage constitutive model based on a multi-mechanism non-local damage continuum for amorphous glassy polymers. International Journal of Solids and Structures, 96, 192–216. doi:10.1016/j.ijsolstr.2016.06.008\n\n[2] Mustafa, M. (2023). Fortran UMAT umat.f for the finite-strain VEVP model. moammmPublic code repository.","title":"References"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"This page provides the public API for FerriteSolidMechanics, grouped by functionality. The material model constructors and the dimensionality wrappers are covered on the Material model API page. For examples and core concepts, see the Tutorials and Concepts pages.\n\nYou likely only need four functions to run a basic simulation: create_assembler, stiffness_matrix, update_states!, and compute_stresses (or try_stiffness_matrix alongside TimeStepController for adaptive time stepping).\n\n","title":"API reference"},{"category":"section","location":"api/#Assembler","page":"API reference","text":"The GenericMaterialAssembler is the central object that connects all phases of the simulation. You construct it using create_assembler, evaluate it during the solver loop via stiffness_matrix (or try_stiffness_matrix or compute_forces), and extract results for postprocessing through compute_stresses. Once a step has converged, you must call update_states! to commit the material history (if it is history-dependent). The compute_stresses function acts as a non-evolving postprocessing call for the bundled stateful materials. In normal use, call compute_stresses(assembler, u) without a dt keyword, as this optional argument is only forwarded for custom stress-output methods and must never be used to advance or commit history.\n\nBecause the direct sparse solve (K \\ r) lives in the driver's Newton loop rather than in this package, you manage its performance yourself. As general guidance, three helpers provide settings measured on the NHR@FAU Fritz cluster:\n\nrecommended_solve_settings returns solver, ranks, threads, BLAS threads and estimated memory for a given grid and node, and whether that memory fits. recommended_blas_threads returns the BLAS thread count alone: ncores in 3D when the rank has the node to itself, and 1 in 2D or when ranks share a node. The 3D gain is mesh-gated, reaching 2.1× at 108k DOFs while a 15k-DOF grid solved faster on a single thread. estimated_replicated_memory_gb predicts the per-rank memory peak of the replicated solve, where every rank holds its own copy of the LU factorization.\n\nThe Performance and parallel execution page provides the measurements behind them.\n\nFor large 3D systems, where the replicated direct solve dominates time and its factors may not fit on a single node, distributed_solve is a drop-in replacement for K \\ r that solves the system with the distributed MUMPS solver across MPI ranks. It is provided by a package extension and is only available once you load MUMPS.jl (import MUMPS); see the MPI four-point bending tutorial for when it pays off and how to launch it.\n\nA failed factorization or solve throws DistributedSolveError.","title":"Assembler"},{"category":"section","location":"api/#External-loads","page":"API reference","text":"LoadHandler assembles the external force vector. It is built and closed like Ferrite's ConstraintHandler, using add! and close!, and evaluated once per load step with external_forces!. Prescribed displacements are not part of it; use Ferrite's Dirichlet for those.\n\nEach load type is named after the units of the value its function returns: Traction and Pressure are force per unit area in 3D and per unit length in 2D, NodalForce is a force, and BodyForce is force per unit volume in 3D and per unit area in 2D. Load functions accept the call forms f(x), f(x, t) and, on facet loads, f(x, t, n), so spatial and temporal load profiles are written directly in the load function.\n\nThe External loads page covers the units, the placement of external_forces! in the Newton loop, the MPI semantics, and how to change the loading during a simulation.","title":"External loads"},{"category":"section","location":"api/#Adaptive-time-stepping","page":"API reference","text":"You can use try_stiffness_matrix to safely capture recoverable local material failures into a result object, which pairs well with the TimeStepController that provides a scalar step size adaptation policy. However, because the package leaves the overall solver logic up to you, your custom Newton loop must still handle state rollback, boundary conditions, external loads, and convergence criteria.","title":"Adaptive time stepping"},{"category":"section","location":"api/#Material-interface","page":"API reference","text":"A custom material typically implements the quadrature point-level constitutive interface by providing material_response and kinematics, while history-dependent models must additionally supply a state type with the lifecycle hooks listed below. Once these are defined, the generic element routine automatically handles the quadrature loop, integration weights, and trial-state bookkeeping. For concrete implementation details, see the Developer guide, or refer to the Concepts page for an explanation of trial and commit semantics.\n\nSmall-strain materials may additionally declare tangent_symmetry. The default Unsymmetric integrates every entry of the element stiffness; returning MajorSymmetric integrates only its lower triangle and mirrors the rest, which is roughly 1.4× faster but valid only when D[i,j,k,l] == D[k,l,i,j].","title":"Material interface"},{"category":"section","location":"api/#Hyperelastic-quick-path","page":"API reference","text":"If you are implementing a stateless hyperelastic model, you can subtype AbstractHyperelastic and implement only the strain energy density Ψ(C, material). Because the stress, tangent, element assembly, stress output, and 2D wrapper hooks are then derived automatically, you only need to override them with analytic expressions if you require better performance.","title":"Hyperelastic quick path"},{"category":"section","location":"api/#MaterialModelsBase.jl-bridge","page":"API reference","text":"If you want to use material models implementing the MaterialModelsBase.jl interface, they can plug in through a wrapper as described in Using MaterialModelsBase.jl models. The necessary interface methods are provided by a package extension that is automatically activated when you call import MaterialModelsBase.","title":"MaterialModelsBase.jl bridge"},{"category":"section","location":"api/#Element-level-extension-points","page":"API reference","text":"These underscore-prefixed methods are exported, stable extension points for materials with model-specific element structures (such as hand-assembled nodal tangent blocks, mixed formulations, or nonlocal coupling). Because materials implementing material_response receive generic fallbacks for both functions, you normally do not need to define them. History models with expensive tangents are an exception and should provide a cheap, tangent-free stress path for postprocessing: override the higher-level material_stress (which receives the strain measure directly), and reach for _compute_stress_qp only when you need control over the quadrature point extraction itself.","title":"Element-level extension points"},{"category":"section","location":"api/#Kinematics","page":"API reference","text":"The deformation_gradient function is the main kinematics helper intended for direct use in element routines. In contrast, wrapper hooks such as compute_PK1_3D and update_state_from_3D! are documented separately below.","title":"Kinematics"},{"category":"section","location":"api/#Recoverable-assembly-failures","page":"API reference","text":"","title":"Recoverable assembly failures"},{"category":"section","location":"api/#Material-models-and-dimensionality-wrappers","page":"API reference","text":"The constructor docstrings of the eleven bundled material models, the PlaneStrain and PlaneStress wrappers, and the two wrapper extension points compute_PK1_3D and update_state_from_3D! are covered on the Material model API page.","title":"Material models and dimensionality wrappers"},{"category":"section","location":"api/#Alpha-Scaling","page":"API reference","text":"These hooks define optional quadrature point scaling for stiffness, residual, and stress output.","title":"Alpha Scaling"},{"category":"function","location":"api/#FerriteSolidMechanics.create_assembler","page":"API reference","text":"create_assembler(material, dh, ch; ah=nothing, quadrature_order=nothing, thickness=1.0)\ncreate_assembler(materials_by_cellset, dh, ch; ah=nothing, quadrature_order=nothing, thickness=1.0)\n\nBuild a GenericMaterialAssembler from either a single material applied to all cells, or a dictionary mapping Ferrite cellset names to materials. Create the assembler once, after the DofHandler and ConstraintHandler are closed, and reuse it for the whole simulation.\n\nArguments\n\nmaterial / materials_by_cellset: a single AbstractMaterial or a Dict{String,<:AbstractMaterial}. The dictionary form assigns different materials to different cellsets of the same grid, for example a stiff inclusion embedded in a soft matrix. The named cellsets must cover the active cells of dh exactly once. Cells outside get_cells(dh) are ignored.\ndh::DofHandler: a closed Ferrite DofHandler. Every sub-dof-handler of a mixed-element grid is supported.\nch::ConstraintHandler: a closed Ferrite ConstraintHandler. Its Dirichlet pattern allocates both the linear and tangent sparse matrices.\nah=nothing: optional user-supplied object. When it is non-nothing, the assembler calls create_alpha_values for a per-CellValues alpha-values object, then calls reinit!(av, cellid) for each cell. Material element routines apply alpha_value(av, qp) in the integration weight of stiffness and residual contributions, and compute_stresses applies the same factor to stress output. With ah === nothing, the default alpha_value(::Nothing, ::Int) = 1.0 applies.\nquadrature_order=nothing: optional quadrature order. An Int sets one order for every sub-dof-handler, a Vector{Int} sets one order per sub-dof-handler in order, and nothing auto-detects from the field interpolation (typically order(ip) + 1 for order > 1, else 2). The order is keyed by sub-dof-handler, not by material, so two materials sharing one SubDofHandler also share its quadrature rule. To give the cellsets of a multi-material grid different orders, declare one SubDofHandler per cellset and pass a Vector{Int}.\nthickness=1.0: scalar multiplier applied to the element stiffness and residual as ke .*= thickness; re .*= thickness. Omit it for 3D problems, where the default 1.0 is a no-op. For 2D plane problems, pass the physical out-of-plane thickness the model requires, for example thickness=0.1 for a millimetre-thick sheet. The wrappers PlaneStrain and PlaneStress require no thickness; their stress is per-unit-thickness by convention.\n\nAdditional call shapes\n\nFour positional call shapes are accepted, taking either a single material or a material dictionary, with or without thickness:\n\ncreate_assembler(material, dh, ah, ch, quadrature_order)\ncreate_assembler(material, dh, ah, ch, quadrature_order, thickness)\ncreate_assembler(material_dict, dh, ah, ch, quadrature_order)\ncreate_assembler(material_dict, dh, ah, ch, quadrature_order, thickness)\n\nThese forward to the keyword form.\n\nWhat is precomputed\n\nSparse-matrix allocation matching the Dirichlet pattern of ch\nPer-cell mapping from cellid to material and to sub-dof-handler\nOne CellValues per sub-dof-handler\nThe linear stiffness of materials with is_linear == true (Hooke, Hooke2D), preassembled once into K_linear\nOne AbstractMaterialState per owned nonlinear cell per quadrature point\nPer-owned-cell result buffers used by the two-phase assembly\nA stress cache of shape (max_nquadpoints, ncells)\n\nSee GenericMaterialAssembler for the full list of fields.\n\n\n\n\n\n","title":"FerriteSolidMechanics.create_assembler"},{"category":"type","location":"api/#FerriteSolidMechanics.GenericMaterialAssembler","page":"API reference","text":"GenericMaterialAssembler\n\nStateful, preallocated Ferrite-based assembler for material models implementing the FerriteSolidMechanics material model interface.\n\nThe assembler owns the preassembled linear stiffness, the current nonlinear tangent and residual buffers, the material mapping for each active cell, and the quadrature point state vectors for nonlinear materials. It also caches sub-dofhandler metadata, task-owned workspaces for parallel assembly, per-cell element results used during scatter, and reusable stress-output buffers.\n\nConstruct it with create_assembler, then call stiffness_matrix, compute_forces, compute_stresses, and update_states!. The Concepts page describes the assembly and state-management model.\n\n\n\n\n\n","title":"FerriteSolidMechanics.GenericMaterialAssembler"},{"category":"function","location":"api/#FerriteSolidMechanics.stiffness_matrix","page":"API reference","text":"stiffness_matrix(assembler, u; dt=0.0)\n\nAssemble and return (K, r), the current tangent stiffness matrix and the internal residual vector for the displacement vector u.\n\nK is the algorithmic tangent. For materials that obtain their tangent via automatic differentiation (e.g. NeoHooke, VEPD_Detrez2010, VEVP_Zhao2021_AD, VEVP_MOAMMM) it is exact up to AD numerics, and for materials with an analytic tangent it is the linearized form returned by the model.\n\ndt is the time step size of the current load step and is forwarded to every material's element routine. Rate-independent models (Hooke, Hooke2D, NeoHooke, J2Plasticity) typically ignore it, while viscoelastic and viscoplastic models use it to evolve internal variables. For viscous models, dt=0.0 freezes the viscous evolution rather than producing rate-independent behaviour.\n\nAssembly runs in two passes:\n\nEach owned nonlinear cell computes its element ke and re in parallel (OhMyThreads.@tasks, greedy scheduler)\nPer-cell results are scattered into the global matrix and vector, and reduced across MPI ranks when MPI.COMM_WORLD has more than one process\n\nLinear cells, those whose material has is_linear == true, are preassembled once when the assembler is created and are added to the tangent on every call without re-evaluation.\n\nUnder MPI this is a collective call that every rank must reach. A local failure on one rank is made collective before it propagates, so peer ranks throw instead of blocking in the reduction. Use try_stiffness_matrix when the outer solver should recover from local constitutive failures rather than abort.\n\nK and r are the assembler's reusable buffers and are overwritten by the next assembly call. copy them to keep a previous tangent or residual.\n\n\n\n\n\n","title":"FerriteSolidMechanics.stiffness_matrix"},{"category":"function","location":"api/#FerriteSolidMechanics.try_stiffness_matrix","page":"API reference","text":"try_stiffness_matrix(assembler, u; dt=0.0)\n\nNon-throwing wrapper around stiffness_matrix for adaptive outer solvers that reject the current load or time step and retry with a smaller dt when a local constitutive update fails. Returns a named tuple:\n\n(converged = true, K = K, r = r, error = nothing) on success\n(converged = false, K = nothing, r = nothing, error = err) when a recoverable LocalAssemblyFailure occurs\n\nOnly LocalAssemblyFailure is treated as recoverable. Every other exception is rethrown, including a MethodError in a material implementation and a generic DomainError that does not subtype LocalAssemblyFailure. A material whose local constitutive failure should be retried by an adaptive outer solver must therefore throw an exception subtyping LocalAssemblyFailure. Threaded assembly may wrap failures in task exceptions; a wrapped failure is recoverable only when every wrapped leaf exception is a LocalAssemblyFailure.\n\nOn failure, reject the current outer step, restore the quadrature states with revert_states!, restore the displacement vector to the last accepted step, reduce dt, and assemble again. Do not call update_states! for a failed attempt; commit only after the outer Newton solve has converged. The assembler's matrix and residual buffers are meaningless after a failed attempt, so use only the returned error.\n\nIn MPI runs, unrecoverable local errors are made collective before they are thrown, so peer ranks do not wait in a later reduction. Recoverable failure flags are also reduced across MPI.COMM_WORLD before the global tangent and residual reductions. If one rank fails recoverably, every rank receives converged = false, and ranks without the original exception receive RemoteAssemblyFailure as error.\n\n\n\n\n\n","title":"FerriteSolidMechanics.try_stiffness_matrix"},{"category":"function","location":"api/#FerriteSolidMechanics.compute_forces","page":"API reference","text":"compute_forces(assembler, u; dt=0.0)\n\nReturn only the internal force (residual) vector for displacement u.\n\ncompute_forces wraps stiffness_matrix and discards the tangent matrix. It runs the same assembly path, so the per-cell element loops still compute both ke and re. Inside a Newton solve, call stiffness_matrix directly and ignore the matrix instead, which avoids a second assembly.\n\ndt has the same meaning as in stiffness_matrix.\n\nThe returned vector is the assembler's reusable residual buffer and is overwritten by the next assembly call. copy it to keep a previous residual.\n\n\n\n\n\n","title":"FerriteSolidMechanics.compute_forces"},{"category":"function","location":"api/#FerriteSolidMechanics.compute_stresses","page":"API reference","text":"compute_stresses(assembler, u; dt=0.0)\n\nEvaluate quadrature point stresses for the current displacement vector u and return them as an array of Tensor values, specifically Tensor{2,dim}.\n\nThe returned array has shape (max_nquadpoints, getncells(grid)) and is indexed σ[qp, cellid] by Ferrite cell id. It is sized for the whole grid, so when the DofHandler covers only a subset of cells, the columns of inactive cells stay zero and the active cells keep their own ids as column indices; they are not compacted to 1:n_active. For mixed sub-dof-handlers, entries above a cell's own quadrature count remain zero. The array is the assembler's reusable internal stress cache, and a later compute_stresses call on the same assembler overwrites it. copy it to keep a previous stress field.\n\nEach quadrature point stress is multiplied by alpha_value(av, qp) when the assembler was built with a non-nothing ah keyword, and by 1.0 otherwise. See alpha_value.\n\ndt defaults to 0.0 and is passed to the material's stress routine for API consistency. For postprocessing, call compute_stresses(assembler, u) and leave that default in place. Stress postprocessing is expected to be non-evolving: bundled stateful materials report the current trial or the committed stress without advancing history or committing state. compute_stresses is not a substitute for stiffness_matrix, compute_forces, or update_states!. Perform the time step update with assembly, then call update_states! after the outer solve has converged.\n\ncompute_stresses loops over all owned cells, linear and nonlinear, and runs in parallel with OhMyThreads.@tasks. When MPI.COMM_WORLD has more than one process the per-rank results are summed across ranks, so every rank ends up with the same global stress field. It is therefore a collective call that every rank must reach. A local failure on one rank is made collective before it propagates, so peer ranks throw instead of blocking in the reduction.\n\n\n\n\n\n","title":"FerriteSolidMechanics.compute_stresses"},{"category":"function","location":"api/#FerriteSolidMechanics.update_states!","page":"API reference","text":"update_states!(assembler)\n\nCommit the trial states of all owned nonlinear quadrature points after a converged Newton step.\n\nupdate_states! calls update_state! on every quadrature point state of every owned nonlinear cell, copying the trial values into the committed slot. Call it once per load step, after Newton has converged, and never inside the Newton loop. If the step is rejected instead, call revert_states! to discard the trial values.\n\nupdate_state! either mutates the state in place and returns nothing, or returns a replacement AbstractMaterialState. A returned state is stored back into the quadrature point state vector.\n\nCells with a linear material hold no history and are skipped, as are cells owned by other MPI ranks.\n\n\n\n\n\n","title":"FerriteSolidMechanics.update_states!"},{"category":"function","location":"api/#FerriteSolidMechanics.revert_states!","page":"API reference","text":"revert_states!(assembler)\n\nDiscard the trial states of all owned nonlinear quadrature points and restore their last committed values.\n\nrevert_states! calls revert_state! on every quadrature point state of every owned nonlinear cell, copying the committed values back into the trial slot. Call it when a trial step is rejected, typically before retrying with a smaller time step.\n\nrevert_state! either mutates the state in place and returns nothing, or returns a replacement AbstractMaterialState. A returned state is stored back into the quadrature point state vector.\n\nCells with a linear material hold no history and are skipped, as are cells owned by other MPI ranks.\n\n\n\n\n\n","title":"FerriteSolidMechanics.revert_states!"},{"category":"function","location":"api/#FerriteSolidMechanics.recommended_solve_settings","page":"API reference","text":"recommended_solve_settings(dh::DofHandler; cores_per_node=Sys.CPU_THREADS,\n                           memory_per_node_gb=nothing, mumps_available=false,\n                           assembly_bound=false) -> NamedTuple\n\nReturn the launch settings for a run on one node: which linear solver to use, how many MPI ranks to start, how many Julia threads to give each, and how many BLAS threads.\n\nThere are two ways to solve K \\ r each Newton iteration, and the choice is worth up to 5× here:\n\nK \\ r (:replicated_lu) – every rank solves the whole system by itself, so each pays the full memory of one solve\ndistributed_solve (:distributed_solve) – the ranks split one solve between them, so each pays a share of the memory\n\ndistributed_solve needs import MUMPS; pass mumps_available=true once you have it.\n\nrecommended_solve_settings returns (; solver, ranks_per_node, threads_per_rank, blas_threads, memory_gb, fits, note). memory_gb is what one rank needs for K \\ r, from estimated_replicated_memory_gb. fits is true when all ranks together stay within 85% of memory_per_node_gb, budgeting 2.6× memory_gb per rank. That budget is deliberately pessimistic and reports false before a run would actually fail; Memory explains the headroom behind the 2.6×. VEVP_Zhao2021_AT measured 9× the estimate and exceeds even this budget, so size a model with many Newton iterations per load step from its own measurement. fits is missing if memory_per_node_gb is not specified or if distributed_solve has been selected: In this case, the factorization is split across the ranks, so the replicated-copies budget checked by fits does not apply. In 2D above a few million dofs also check that all ranks' copies of K fit in the node's memory, no matter the solver: every rank holds its own full copy, and at 16M dofs 18 such copies alone exceed the memory of a 256 GB node.\n\nPass assembly_bound=true for a 2D run that spends more time building element matrices than solving, which is typical of rate-dependent models that run their own local Newton solve per quadrature point. The flag moves the 2D layout toward more ranks. Raise the rank count further yourself if assembly dominates and the memory allows it; Rank and thread layout shows where the highest rank count stops fitting as the mesh grows.\n\ns = recommended_solve_settings(dh; cores_per_node=72, memory_per_node_gb=256,\n                               mumps_available=true)\nLinearAlgebra.BLAS.set_num_threads(s.blas_threads)\n# launch with s.ranks_per_node ranks and s.threads_per_rank threads each\n\nThe rules below were measured on the 72-core nodes of NHR@FAU Fritz:\n\nCase Setting\n3D, MUMPS available, ≳4k dofs distributed_solve, cores ÷ 4 ranks × 4 threads, BLAS 1\n3D, no MUMPS 1 rank × cores threads, BLAS cores\n2D K \\ r, cores ÷ 8 ranks × 8 threads, BLAS 1\n\nrecommended_solve_settings holds the default 2D rank count at cores ÷ 8 and, with mumps_available=true and memory_per_node_gb given, returns distributed_solve once that many copies would exceed 85% of the node's memory. The rank and thread counts come from one mesh per case, so treat them as starting points and better time your own problem before committing to a large run. Higher-order elements were not measured; at equal dof count they change the sparsity of K, so remeasure the thresholds and the memory behind fits before relying on them for another element order. Choosing the linear solver states the timings behind these rules, and Reproducing these on your cluster shows how to check them on your own cluster.\n\n\n\n\n\n","title":"FerriteSolidMechanics.recommended_solve_settings"},{"category":"function","location":"api/#FerriteSolidMechanics.recommended_blas_threads","page":"API reference","text":"recommended_blas_threads(dh::DofHandler; ncores=Sys.CPU_THREADS, ranks_per_node=nothing) -> Int\n\nReturn the number of BLAS threads for the K \\ r solve the driver runs each Newton iteration. Set it in the driver before solving: LinearAlgebra.BLAS.set_num_threads(recommended_blas_threads(dh)).\n\nrecommended_blas_threads returns ncores for a 3D grid whose rank has the node to itself, and 1 in 2D or when several ranks share a node. ranks_per_node defaults to the number of ranks on this rank's node, read from MPI. Julia starts BLAS at Sys.CPU_THREADS ÷ 2, so the returned count is higher in the 3D single-rank case and lower in the other two.\n\nBLAS threading pays off in 3D when the rank owns the node: at 108k dofs the solve took 312 s at 1 BLAS thread against 145 s at 36. In 2D it measured slower at every thread count. Ranks sharing a node each start their own BLAS threads, which cost distributed_solve 6.3× per solve at 18 ranks.\n\nrecommended_solve_settings returns all launch settings at once. BLAS threads states the measurements behind this rule, including the mesh size below which BLAS threading stops paying.\n\n\n\n\n\n","title":"FerriteSolidMechanics.recommended_blas_threads"},{"category":"function","location":"api/#FerriteSolidMechanics.estimated_replicated_memory_gb","page":"API reference","text":"estimated_replicated_memory_gb(ndofs, dim) -> Float64\n\nEstimate the memory one MPI rank needs, in GB, when the driver solves K \\ r directly.\n\nSolving K \\ r builds a decomposition of K that is much bigger than K itself, and every rank builds its own copy. That copy is what fills up a node: running 18 ranks needs 18 times this number. distributed_solve splits one copy across the ranks instead, so it needs far less memory per rank.\n\nMemory grows faster than the problem: ndofs^1.43 in 3D and ndofs^1.09 in 2D. Doubling a 3D mesh's dofs therefore costs about 2.7× the memory, so a size that fits comfortably can stop fitting after one refinement.\n\nThe fitted curve is scaled to NeoHooke over three load steps, on linear hexahedra in 3D and linear quadrilaterals in 2D, and the predicted peak memory stays within 15% of every point measured on the 72-core nodes of NHR@FAU Fritz. Higher-order elements were not measured; at equal dof count they change the sparsity of K, so remeasure before relying on the estimate for another element order. An expensive material and a longer run both raise the peak above it, together by 2.6×, which is the headroom recommended_solve_settings budgets in its fits field. Either the expensive material or the longer run reaches that budget on its own, and VEVP_Zhao2021_AT exceeded it at 9× the estimate, so take the memory of a long run, or of a model with many Newton iterations per load step, from its own measurement.\n\nMemory documents the range of grid sizes the fit was validated over, the measured series behind the 2.6×, and the size above which each rank's own copy of K rather than the factorization sets the limit.\n\n\n\n\n\n","title":"FerriteSolidMechanics.estimated_replicated_memory_gb"},{"category":"function","location":"api/#FerriteSolidMechanics.distributed_solve","page":"API reference","text":"distributed_solve(K, residual, comm; verify_rtol=1e-8, workspace_pct=nothing) -> du\n\nSolve K * du = residual with the distributed sparse direct solver MUMPS, returning the update du on every rank. It is a drop-in replacement for du = K \\ residual in an MPI Newton loop:\n\nu .-= distributed_solve(K, residual, MPI.COMM_WORLD)\n\nThe method comes from a package extension. Add MUMPS.jl to the environment and load it with import MUMPS, which is what defines the method. Calling distributed_solve beforehand raises a MethodError.\n\nEvery rank passes its own copy of the assembled K and residual. MUMPS factorizes across comm and broadcasts the solution back, so all ranks return the same du. residual is left unchanged.\n\nBefore returning, distributed_solve checks du against K * du ≈ residual to a relative tolerance of verify_rtol and throws a DistributedSolveError when that check fails. The check exists because MUMPS reported success on a cluster run whose solution was wrong. It costs one sparse matrix-vector product per call (one per Newton iteration; on each rank's own copy and without synchronizing), which is negligible next to the factorization. A working solve satisfies it to roughly machine precision. Set verify_rtol=0 to skip it.\n\nMUMPS sizes its internal workspace from an estimate, and fails the factorization when that estimate turns out too small (INFO(1) = -9), the most frequent failure on the problems measured here. distributed_solve retries that case automatically with progressively more workspace, so that a solve claims extra memory and completes instead of ending the run. workspace_pct sets that percentage (MUMPS ICNTL(14)) explicitly and turns the retries off, which caps the memory a single solve may claim.\n\nUse this in 3D above between roughly 4k..20k dofs. With K \\ residual every rank builds the whole factorization, so time and memory both grow with the rank count, while MUMPS splits one factorization across the ranks instead. Measured on the 72-core nodes of NHR@FAU Fritz against the best replicated configuration (one rank × 72 cores, BLAS 36), on 3D hexahedral NeoHooke problems, where both methods produce the same solution to within 1e-9 relative:\n\n3D grid replicated K \\ residual (BLAS 36; 1 rank × 72) distributed_solve (BLAS 1; 18 ranks × 4, 36 × 2 at 273k)\n2.2k dofs 1.4 s 1.2 s\n47k dofs 37.7 s, 7.2 GB 11.8 s, 1.5 GB\n108k dofs 145 s, 20.7 GB 25.0 s, 2.1 GB\n273k dofs 634 s, 62.5 GB 62.3 s, 3.4 GB\n\nThe two ways of solving cost the same per solve at about 3k dofs, and the 2.2k row covers eight repeats each whose ranges overlap, so the replicated solve is the more straightforward choice in the vicinity of the crossover. In 2D, K \\ residual was faster at every size measured (291k: 52 s vs 62 s; 4.0M with Hooke: 30 s vs 39 s), both at BLAS 1, so use it there with fewer ranks than the 18 the distributed layout runs, since each rank's whole factorization copy must fit in node RAM. At 16M 2D dofs one replicated rank completed using 95 GB of memory while 18 ranks running distributed_solve were killed on a 256 GB node: each of them holds its own 4.3 GB copy of K on top of its share of the factorization, so at that scale fewer ranks save more memory than splitting the factorization does. recommended_solve_settings applies these rules to a given grid, keeping 18 ranks × 4 threads in 3D rather than the 36 × 2 layout the 273k row used.\n\nLaunch it the opposite way from the threaded K \\ residual, which runs one rank with all 72 cores as threads: distributed_solve takes its parallelism from MPI processes instead, so run many ranks per node (18 to 36 on 72 cores) with a single BLAS thread each. At 18 ranks, 4 BLAS threads per rank measured 6.3× slower per solve than 1 thread. recommended_blas_threads applies to the replicated solve. The factorization communicates heavily between ranks, so these measurements all used the cluster's own MPI module rather than the build MPI.jl installs by default. Use one node while the problem fits its RAM. At 273k dofs the run took 102 s on 1 node against 123 s on 2 and 76 s on 4, all at 18 ranks per node, while 36 ranks on the single node took 62 s and beat every one of them. At 985k dofs it took 377 s on 2 nodes, 708 s on 4 and 821 s on 8, the factorization itself accounting for 122 s, 233 s and 270 s per solve, so adding nodes bought no solve time at either size. Element assembly does scale with the ranks, from 0.168 s to 0.064 s per Newton iteration at 273k dofs over 18 to 72 ranks, but that saving is negligible next to the factorization. Furthremore, more nodes help only once the memory requires them. The MPI tutorial covers layout, mesh-size, and memory scaling.\n\n\n\n\n\n","title":"FerriteSolidMechanics.distributed_solve"},{"category":"type","location":"api/#FerriteSolidMechanics.DistributedSolveError","page":"API reference","text":"DistributedSolveError(phase, infog1, [relative_residual]) <: LocalAssemblyFailure\n\ndistributed_solve did not return a usable du.\n\nphase is :factorize or :solve when MUMPS reported the failure itself, and infog1 is then its INFOG(1)/INFO(1) error code (-10 singular, -13 allocation failure, -9 workarray too small after every workspace retry has been exhausted). phase is :verify when MUMPS reported success but K * du did not reproduce residual, and relative_residual is then how far apart the two were, relative to residual, while infog1 is 0.\n\nDistributedSolveError subtypes LocalAssemblyFailure, so a driver that shrinks dt and retries the step can catch it.\n\n\n\n\n\n","title":"FerriteSolidMechanics.DistributedSolveError"},{"category":"type","location":"api/#FerriteSolidMechanics.LoadHandler","page":"API reference","text":"LoadHandler(assembler; quadrature_order=nothing)\nLoadHandler(dh; thickness=1.0, quadrature_order=nothing)\n\nExternal load container, built and used the way Ferrite's ConstraintHandler is:\n\nch = ConstraintHandler(dh)\nadd!(ch, Dirichlet(:u, getfacetset(grid, \"left\"), (x, t) -> [0.0, 0.0], [1, 2]))\nclose!(ch)\n\nlh = LoadHandler(assembler)\nadd!(lh, Traction(\"top\", (x, t) -> Vec(0.0, -p * t)))\nadd!(lh, NodalForce(\"tip\", x -> Vec(0.0, -F); distribute=true))\nclose!(lh)\n\nexternal_forces! then returns the assembled external force vector for a given t. The handler follows the pattern and keeps the same name as the LoadHandler of FerriteAssembly.jl (K. A. Meyer).\n\nConstructing from a GenericMaterialAssembler takes the DofHandler, the thickness and the quadrature orders from it, so external and internal forces are integrated consistently. Passing a DofHandler instead is for drivers that assemble no internal forces; give the same thickness the assembler uses, because facet and body integrals scale with it exactly as ke and re do.\n\nquadrature_order accepts an Int for one order across all sub-dof-handlers, a Vector{Int} for one order each, and nothing to take the assembler's orders or, without an assembler, the interpolation default.\n\nAdding loads after closing\n\nclose! prepares the entries added since the last call and leaves earlier ones untouched, so loads may be added part-way through a simulation and the handler closed again:\n\nadd!(lh, Traction(\"second_stage\", (x, t) -> Vec(0.0, -q * (t - t_switch))))\nclose!(lh)\n\nChanging the magnitude, direction or profile of an existing load needs no such call, because the load function is evaluated on every external_forces! call and may close over computed results:\n\nstage = Ref(1)\nadd!(lh, Traction(\"top\", (x, t) -> stage[] == 1 ? Vec(0.0, -p) : zero(Vec{2})))\n\nThe same pattern removes a load, since a function returning zero contributes nothing to the external force vector.\n\nReplication under MPI\n\nLoadHandler assembles serially and, under MPI, redundantly: every rank builds the complete external force vector and no reduction is performed. This matches the replicated-data design of the assembler, whose residual is already complete on every rank after its own Allreduce. Every rank must add the same loads in the same order so that the per-rank copies agree. An MPI.Allreduce over the external force vector would sum those identical copies and apply the load nranks times.\n\n\n\n\n\n","title":"FerriteSolidMechanics.LoadHandler"},{"category":"function","location":"api/#FerriteSolidMechanics.external_forces!","page":"API reference","text":"external_forces!(lh::LoadHandler, t=0.0)\n\nAssemble the external force vector of every load in lh at t and return it.\n\nt is passed unchanged as the second argument of each load function. The driver chooses its meaning: physical time for rate-dependent materials, or a load factor for quasi-static ramping. Pass the same value to Ferrite's update!(ch, t) so that prescribed displacements and loads refer to the same instant.\n\nDead loads do not depend on the displacement, so call external_forces! once per time step, outside the Newton loop, and subtract the result from the internal force returned by stiffness_matrix:\n\nf_ext = external_forces!(lh, t)\nfor iter in 1:max_newton\n    K, f_int = stiffness_matrix(assembler, u; dt=dt)\n    residual .= f_int .- f_ext\n    apply_zero!(K, residual, ch)\n    norm(residual) <= tol * max(norm(f_ext), 1.0) && break\n    u .-= K \\ residual\nend\n\nExternal loads contribute nothing to K.\n\nUnder MPI every rank assembles the complete vector and no reduction is performed; see LoadHandler.\n\nThe returned vector belongs to lh. Calling external_forces! again overwrites it, so copy the result when a step's values are still needed afterwards.\n\nThrows when loads have been added since the last close!(lh), which must be called before the added loads can be assembled.\n\n\n\n\n\n","title":"FerriteSolidMechanics.external_forces!"},{"category":"type","location":"api/#FerriteSolidMechanics.Traction","page":"API reference","text":"Traction(set, f)\n\nSurface traction on a facet set, in force per unit area in 3D and force per unit length in 2D.\n\nset is a facetset name or a set of FacetIndex. A facet of a 2D cell is an edge, so the same type covers area loads in 3D and edge loads in 2D.\n\nf returns the traction vector and may be written in any of three forms, resolved once when the LoadHandler is closed:\n\nTraction(\"top\", x -> Vec(0.0, -p))                   # f(x)\nTraction(\"top\", (x, t) -> Vec(0.0, -p * t))          # f(x, t)\nTraction(\"top\", (x, t, n) -> -p * t * n)             # f(x, t, n)\n\nx is the spatial coordinate of the quadrature point, t is the value passed to external_forces!, and n is the outward facet normal. Because f receives x, any spatial profile is expressed directly, for example (x, t) -> Vec(0.0, -p * (x[1] / L) * t) for a load growing linearly along the x-axis.\n\nn is evaluated on the undeformed geometry, so a normal-aligned traction is a dead load, fixed in direction at the start and not rotating as the surface deforms. Follower loads, whose direction tracks the deforming surface, would contribute an extra term to the tangent and are not available. For the common normal-aligned case, Pressure carries the sign convention.\n\n\n\n\n\n","title":"FerriteSolidMechanics.Traction"},{"category":"type","location":"api/#FerriteSolidMechanics.Pressure","page":"API reference","text":"Pressure(set, f)\n\nNormal pressure on a facet set, in the same units as Traction.\n\nf returns a scalar and accepts the forms f(x), f(x, t) and f(x, t, n). The applied traction is -f * n with n the outward facet normal, so a positive value acts inward and compresses the body.\n\nPressure(\"hole\", (x, t) -> p * t)\n\nPressure is equivalent to Traction(set, (x, t, n) -> -f(x, t) * n) and exists to keep that sign convention in one place. The normal is taken on the undeformed geometry, so this is a dead load; see Traction.\n\n\n\n\n\n","title":"FerriteSolidMechanics.Pressure"},{"category":"type","location":"api/#FerriteSolidMechanics.NodalForce","page":"API reference","text":"NodalForce(set, f; distribute=false)\n\nPoint force applied at the nodes of a node set, in force.\n\nset is a nodeset name or a collection of node ids. f returns the force vector and accepts the forms f(x) and f(x, t), where x is the node coordinate. The facet normal is not defined at a node, so the f(x, t, n) form is not available here.\n\ndistribute=false, the default, applies the returned value at every node of the set. distribute=true treats the returned value as the resultant of the whole set and splits it equally over the nodes, so the sum over the set is the value itself.\n\nNodalForce(\"tip\", x -> Vec(0.0, -F))                      # F at each node\nNodalForce(\"tip\", x -> Vec(0.0, -F); distribute=true)     # F in total\n\nEqual splitting matches the finite element load distribution only for first-order shape functions. Above first order the shape function integration no longer splits the load equally, so corner and midside nodes take different shares. To spread a resultant over a surface on such a mesh, use Traction with magnitude F / A instead.\n\nOnly nodes carrying degrees of freedom can receive a force. Closing the handler throws when the set contains nodes without dofs.\n\n\n\n\n\n","title":"FerriteSolidMechanics.NodalForce"},{"category":"type","location":"api/#FerriteSolidMechanics.BodyForce","page":"API reference","text":"BodyForce(f)\nBodyForce(set, f)\n\nBody force over cells, in force per unit volume in 3D and force per unit area in 2D.\n\nThe one-argument form applies to every cell carrying degrees of freedom. The two-argument form takes a cellset name or a collection of cell ids. f returns the force vector and accepts the forms f(x) and f(x, t), where x is the spatial coordinate of the quadrature point.\n\nBodyForce(x -> Vec(0.0, -rho * g))                        # self-weight\nBodyForce(\"rotor\", (x, t) -> rho * omega(t)^2 * x)        # centrifugal, per cellset\n\n\n\n\n\n","title":"FerriteSolidMechanics.BodyForce"},{"category":"type","location":"api/#FerriteSolidMechanics.AbstractLoad","page":"API reference","text":"AbstractLoad\n\nSupertype of the external load specifications Traction, Pressure, NodalForce and BodyForce.\n\nLoads are Neumann boundary conditions: they contribute to the external force vector and never to the tangent. Prescribed displacements are Dirichlet boundary conditions and are not part of this hierarchy; use Ferrite's Dirichlet and ConstraintHandler for those.\n\nEach load type is named after the units of the value its function returns. The 2D and 3D units differ only because the entity integrated over does.\n\n\n\n\n\n","title":"FerriteSolidMechanics.AbstractLoad"},{"category":"type","location":"api/#FerriteSolidMechanics.TimeStepController","page":"API reference","text":"TimeStepController(; dt_min=NaN, dt_max=NaN, shrink=0.5, grow=2.0,\n                   max_rejections=typemax(Int))\n\nAdaptive time step controller for user-owned load and time loops.\n\nThe controller runs no assembly, Newton iterations, rollback, or MPI communication. It only updates scalar time step sizes after the caller has accepted or rejected a trial step, which makes it usable with any material model and with serial, threaded, or MPI assembly.\n\ndt_min and dt_max are optional bounds, and NaN disables the corresponding lower or upper bound. shrink must satisfy 0 < shrink < 1. grow must be finite and satisfy grow >= 1. Both factors are fixed when the controller is constructed; accept_step! and reject_step! do not adapt them. max_rejections = N allows N consecutive rejected steps since the last accepted step and throws on rejection N + 1. The default leaves that count practically unbounded.\n\nrejections counts the rejected steps since the last accepted one. reject_step! increments it, accept_step! resets it to zero, and reset_controller! clears it without returning a new step size. It is the only field meant to change after construction; leave dt_min, dt_max, shrink, grow and max_rejections at the values given to the constructor.\n\nUse one TimeStepController per Newton loop. The mutating functions assume a single caller and are not safe to call from several tasks at once.\n\n\n\n\n\n","title":"FerriteSolidMechanics.TimeStepController"},{"category":"type","location":"api/#FerriteSolidMechanics.TimeStepControllerExhausted","page":"API reference","text":"TimeStepControllerExhausted\n\nException thrown by reject_step! when the controller has no smaller time step to offer, because the proposed step would violate dt_min or the consecutive rejection budget.\n\n\n\n\n\n","title":"FerriteSolidMechanics.TimeStepControllerExhausted"},{"category":"function","location":"api/#FerriteSolidMechanics.accept_step!","page":"API reference","text":"accept_step!(controller, dt)\n\nReturn the next trial time step after an accepted step.\n\naccept_step! resets the controller's consecutive rejection counter, multiplies dt by controller.grow, and clamps the result to controller.dt_max when that upper bound is enabled. dt_max = NaN disables the upper clamp.\n\n\n\n\n\n","title":"FerriteSolidMechanics.accept_step!"},{"category":"function","location":"api/#FerriteSolidMechanics.reject_step!","page":"API reference","text":"reject_step!(controller, dt)\n\nReturn the next trial time step after a rejected step.\n\nreject_step! computes dt * controller.shrink, checks the lower bound and the rejection budget, then increments the controller's consecutive rejection counter on success. It throws when the new step would fall below controller.dt_min and that lower bound is enabled. It does not clamp rejected steps to dt_min. dt_min = NaN disables the lower-bound check. The controller state is left unchanged when reject_step! throws.\n\n\n\n\n\n","title":"FerriteSolidMechanics.reject_step!"},{"category":"function","location":"api/#FerriteSolidMechanics.reset_controller!","page":"API reference","text":"reset_controller!(controller)\n\nReset the controller's consecutive rejection counter to zero and return the controller.\n\n\n\n\n\n","title":"FerriteSolidMechanics.reset_controller!"},{"category":"type","location":"api/#FerriteSolidMechanics.AbstractMaterial","page":"API reference","text":"AbstractMaterial\n\nSupertype for all material models used with GenericMaterialAssembler.\n\n\n\n\n\n","title":"FerriteSolidMechanics.AbstractMaterial"},{"category":"type","location":"api/#FerriteSolidMechanics.AbstractMaterialState","page":"API reference","text":"AbstractMaterialState\n\nSupertype for all quadrature point state objects that store internal history.\n\n\n\n\n\n","title":"FerriteSolidMechanics.AbstractMaterialState"},{"category":"type","location":"api/#FerriteSolidMechanics.LocalAssemblyFailure","page":"API reference","text":"LocalAssemblyFailure\n\nAbstract exception type for recoverable local assembly failures. Public non-throwing helpers such as try_stiffness_matrix catch this type and return it to the caller, so an outer load step controller can reduce the step size and retry.\n\nThose helpers catch only exceptions that opt in by subtyping LocalAssemblyFailure. Exceptions from invalid inputs or buggy material methods are rethrown.\n\n\n\n\n\n","title":"FerriteSolidMechanics.LocalAssemblyFailure"},{"category":"type","location":"api/#FerriteSolidMechanics.NoState","page":"API reference","text":"NoState\n\nPlaceholder state for materials that do not require internal history.\n\n\n\n\n\n","title":"FerriteSolidMechanics.NoState"},{"category":"function","location":"api/#FerriteSolidMechanics.material_response","page":"API reference","text":"material_response(material, ε_or_F, state, dt, cache=nothing) -> (stress, tangent, new_state)\n\nConstitutive update at a single quadrature point. A typical material implements material_response, kinematics, and, for a history-dependent model, a state type.\n\nArguments\n\nε_or_F: strain measure matching kinematics(material) – ε::SymmetricTensor{2,dim} for SmallStrain, F::Tensor{2,dim} for FiniteStrain\nstate: the quadrature point state object, NoState() for a stateless material. Read the committed history from it, by convention the previous-side fields. Do not commit.\ndt: time increment of the current step, where 0.0 freezes viscous flow\ncache: scratch object from allocate_material_cache, or nothing\n\nReturns\n\nstress: σ for SmallStrain, P for FiniteStrain\ntangent: ∂σ/∂ε for SmallStrain, ∂P/∂F for FiniteStrain\nnew_state: the trial state produced by this evaluation\n\nThe generic element routine accepts three forms of new_state:\n\nthe passed state itself, for a stateless material or a material that wrote its trial fields in place\na snapshot of internal variables that is not an AbstractMaterialState, stored via set_trial!(state, new_state)\na new AbstractMaterialState, which replaces the quadrature point state slot and must have the same concrete type as the state it replaces\n\nThe generic element routine pins one state type per cell, taken from the first quadrature point.\n\nmaterial_response must be AD-transparent where the material relies on automatic differentiation. It must throw a LocalAssemblyFailure subtype for a recoverable local-solve failure.\n\nThe material_response name and the AbstractMaterial/AbstractMaterialState pair come from MaterialModels.jl by K. Auth and contributors. K. A. Meyer's MaterialModelsBase.jl was developed from that package as an implementation-independent interface. The design here follows the MaterialModelsBase conventions: a single response function returning (stress, tangent, new_state), strain-measure-driven stress/tangent pairs, and the cache hook. Materials implementing the MaterialModelsBase interface can be used directly via FromMaterialModelsBase.\n\n\n\n\n\n","title":"FerriteSolidMechanics.material_response"},{"category":"function","location":"api/#FerriteSolidMechanics.material_stress","page":"API reference","text":"material_stress(material, ε_or_F, state, dt, cache=nothing) -> stress\n\nReturn the postprocessing stress at one quadrature point, without the tangent.\n\nmaterial_stress is the optional stress-only companion to material_response. The generic _compute_stress_qp fallback calls it to obtain the output stress: Cauchy σ for FiniteStrain, small-strain σ for SmallStrain. The default method computes that stress from material_response at the passed state and discards the tangent and the trial state, so a plain constitutive material needs no method of its own.\n\nDefine a method when computing the tangent is expensive, or when the output stress follows a model-specific convention. An AD-based history model, for example, can return the stress directly from its local solve and skip the automatic differentiation. Override material_stress in preference to the lower-level _compute_stress_qp: material_stress receives the strain measure ε or F directly, whereas _compute_stress_qp receives the cellvalues and the quadrature point index and leaves the strain extraction to the method.\n\nA material_stress method must be deterministic and must not commit or evolve state. A rate-dependent model, for example, passes dt = 0.0 to its local solve by default, so it reports the stress at the current time and state without evolving state.\n\n\n\n\n\n","title":"FerriteSolidMechanics.material_stress"},{"category":"function","location":"api/#FerriteSolidMechanics.kinematics","page":"API reference","text":"kinematics(material) -> AbstractKinematics\n\nDeclare the strain measure of a material: SmallStrain or FiniteStrain.\n\nExtension point. Every material that uses the generic element routine, meaning every material that implements material_response, must define kinematics. A material that provides its own _assemble_element! method does not need it.\n\n\n\n\n\n","title":"FerriteSolidMechanics.kinematics"},{"category":"type","location":"api/#FerriteSolidMechanics.AbstractKinematics","page":"API reference","text":"AbstractKinematics\n\nTrait supertype describing which strain measure a material consumes. kinematics returns it, and the generic element routine uses it to select what to evaluate at each quadrature point and which weak form to integrate. Concrete traits: SmallStrain, FiniteStrain.\n\n\n\n\n\n","title":"FerriteSolidMechanics.AbstractKinematics"},{"category":"type","location":"api/#FerriteSolidMechanics.SmallStrain","page":"API reference","text":"SmallStrain <: AbstractKinematics\n\nMaterial works on the small-strain tensor ε = sym(∇u), a SymmetricTensor{2,dim}, and returns the Cauchy stress σ and the algorithmic tangent D = ∂σ/∂ε.\n\n\n\n\n\n","title":"FerriteSolidMechanics.SmallStrain"},{"category":"type","location":"api/#FerriteSolidMechanics.FiniteStrain","page":"API reference","text":"FiniteStrain <: AbstractKinematics\n\nMaterial works on the deformation gradient F = I + ∇u, a Tensor{2,dim}, and returns the first Piola–Kirchhoff stress P and the tangent ∂P/∂F.\n\n\n\n\n\n","title":"FerriteSolidMechanics.FiniteStrain"},{"category":"function","location":"api/#FerriteSolidMechanics.tangent_symmetry","page":"API reference","text":"tangent_symmetry(material) -> AbstractTangentSymmetry\n\nDeclare whether a SmallStrain material's tangent D = ∂σ/∂ε is major-symmetric. Defaults to Unsymmetric.\n\nReturning MajorSymmetric halves the tangent-integration work, worth roughly 1.4× on small-strain element assembly. The declaration is a promise about the values, not about the type. SymmetricTensor{4} carries only the minor symmetries D[i,j,k,l] == D[j,i,k,l] == D[i,j,l,k], which do not imply major symmetry. Check before claiming it:\n\nD ≈ permutedims(D, (3, 4, 1, 2))\n\nFiniteStrain materials do not use this trait, because their ∂P/∂F is never assumed symmetric.\n\n\n\n\n\n","title":"FerriteSolidMechanics.tangent_symmetry"},{"category":"type","location":"api/#FerriteSolidMechanics.AbstractTangentSymmetry","page":"API reference","text":"AbstractTangentSymmetry\n\nTrait supertype describing whether a SmallStrain material's tangent D = ∂σ/∂ε is major-symmetric. tangent_symmetry returns it, and the generic element routine uses it to select the tangent-integration loop. Concrete traits: MajorSymmetric, Unsymmetric.\n\n\n\n\n\n","title":"FerriteSolidMechanics.AbstractTangentSymmetry"},{"category":"type","location":"api/#FerriteSolidMechanics.MajorSymmetric","page":"API reference","text":"MajorSymmetric <: AbstractTangentSymmetry\n\nD[i,j,k,l] == D[k,l,i,j], so the element stiffness is symmetric. The element routine integrates only its lower triangle and mirrors the upper triangle.\n\nDeclaring this trait for a tangent that is not major-symmetric silently discards the antisymmetric part and yields a wrong element stiffness, so Newton converges slowly or not at all. The residual stays correct, which makes the error easy to miss.\n\n\n\n\n\n","title":"FerriteSolidMechanics.MajorSymmetric"},{"category":"type","location":"api/#FerriteSolidMechanics.Unsymmetric","page":"API reference","text":"Unsymmetric <: AbstractTangentSymmetry\n\nNo symmetry is assumed, so the element routine integrates every entry of ke. This is the default, and the correct choice for non-associated flow, nonlinear kinematic hardening, and anisotropic damage.\n\n\n\n\n\n","title":"FerriteSolidMechanics.Unsymmetric"},{"category":"function","location":"api/#FerriteSolidMechanics.set_trial!","page":"API reference","text":"set_trial!(state, new_state)\n\nStore the trial result new_state returned by material_response into the quadrature point state without committing it. The default method covers the current/previous layout:\n\nset_trial!(state, new) = (state.current = new; nothing)\n\nOverride it for a state with a different layout; VEPD_Detrez2010 is a field-wise example. The generic element routine does not call set_trial! when material_response returns the state itself or a replacement AbstractMaterialState.\n\n\n\n\n\n","title":"FerriteSolidMechanics.set_trial!"},{"category":"function","location":"api/#FerriteSolidMechanics.allocate_material_cache","page":"API reference","text":"allocate_material_cache(material) -> cache\n\nAllocate a reusable scratch object that is passed as the cache argument of material_response. The default method returns nothing. Element assembly calls allocate_material_cache once per element, and the generic stress fallback calls it once per quadrature point.\n\nOverride it to reuse buffers across the quadrature points of an element, such as workspace arrays for a local Newton solve. A material whose cache is expensive to build should also override material_stress.\n\n\n\n\n\n","title":"FerriteSolidMechanics.allocate_material_cache"},{"category":"function","location":"api/#FerriteSolidMechanics.is_linear","page":"API reference","text":"is_linear(material)\n\nReturn true when material has a displacement-independent tangent stiffness. GenericMaterialAssembler preassembles linear materials once.\n\n\n\n\n\n","title":"FerriteSolidMechanics.is_linear"},{"category":"function","location":"api/#FerriteSolidMechanics.create_state","page":"API reference","text":"create_state(material)\n\nCreate the quadrature point state object of material. A stateless material uses the default method, which returns NoState().\n\n\n\n\n\n","title":"FerriteSolidMechanics.create_state"},{"category":"function","location":"api/#FerriteSolidMechanics.update_state!","page":"API reference","text":"update_state!(state)\n\nCommit one converged quadrature point state at the end of a load or time step.\n\nupdate_state! either mutates a mutable state in place and returns nothing, or returns a new AbstractMaterialState for an immutable or replacement-style state. update_states! stores a returned state back into the assembler state vector.\n\n\n\n\n\n","title":"FerriteSolidMechanics.update_state!"},{"category":"function","location":"api/#FerriteSolidMechanics.revert_state!","page":"API reference","text":"revert_state!(state)\n\nRestore one quadrature point state to the previously committed value.\n\nrevert_state! either mutates a mutable state in place and returns nothing, or returns a new state value for a replacement-style state. A caller that owns the state container is responsible for storing that returned value.\n\n\n\n\n\n","title":"FerriteSolidMechanics.revert_state!"},{"category":"function","location":"api/#FerriteSolidMechanics.copy_state!","page":"API reference","text":"copy_state!(dest, src)\n\nCopy fields from src to dest recursively, using reflection. copy_state! copies arrays in place where possible and returns immutable values, so a caller that owns the storage can replace the destination value. This is the generic history-state fallback for arbitrary structs.\n\n\n\n\n\n","title":"FerriteSolidMechanics.copy_state!"},{"category":"type","location":"api/#FerriteSolidMechanics.AbstractHyperelastic","page":"API reference","text":"AbstractHyperelastic <: AbstractMaterial\n\nSupertype for stateless hyperelastic materials defined by a strain energy density Ψ(C). A subtype implements the energy function and receives the complete material interface:\n\nstruct MyModel <: AbstractHyperelastic\n    μ::Float64\nend\nFerriteSolidMechanics.Ψ(C, mp::MyModel) = 0.5 * mp.μ * (tr(C) - 3)  # + volumetric part\n\nDerived defaults, each overridable with an analytic expression:\n\nconstitutive_driver(C, mp) – S = 2∂Ψ/∂C and ∂S/∂C = 2∂²Ψ/∂C² via Tensors.hessian\n_second_piola(C, mp) – S = 2∂Ψ/∂C via Tensors.gradient\nmaterial_response, kinematics (= FiniteStrain()), _compute_stress_qp, compute_PK1_3D and update_state_from_3D!, so PlaneStrain and PlaneStress wrapping works without additional methods\n\n\n\n\n\n","title":"FerriteSolidMechanics.AbstractHyperelastic"},{"category":"function","location":"api/#FerriteSolidMechanics.Ψ","page":"API reference","text":"Ψ(C, material)\n\nStrain energy density as a function of the right Cauchy–Green tensor C = FᵀF. This is the extension point for AbstractHyperelastic materials. Extend it as FerriteSolidMechanics.Ψ.\n\n\n\n\n\n","title":"FerriteSolidMechanics.Ψ"},{"category":"type","location":"api/#FerriteSolidMechanics.FromMaterialModelsBase","page":"API reference","text":"FromMaterialModelsBase(material; kinematics=SmallStrain())\n\nWrap a material implementing the MaterialModelsBase.jl interface, for example a model from MechanicalMaterialModels.jl, for use everywhere a FerriteSolidMechanics AbstractMaterial is accepted: create_assembler, stiffness_matrix, compute_stresses, update_states!, try_stiffness_matrix, and so on.\n\nThe kinematics keyword declares which strain measure the wrapped material consumes, since MaterialModelsBase selects it by dispatch on the input type:\n\nSmallStrain, the default, passes ε::SymmetricTensor{2,dim} and expects (σ, ∂σ∂ε, new_state)\nFiniteStrain passes F::Tensor{2,dim} and expects (P, ∂P∂F, new_state)\n\nThe constructor is always available, while a package extension provides the interface methods behind it. Load the weak dependency first:\n\nimport MaterialModelsBase                    # activates the extension\nusing MechanicalMaterialModels: Plastic, LinearElastic, Voce,\n                                ArmstrongFrederick, NortonOverstress\n\nmat = FromMaterialModelsBase(Plastic(;\n    elastic   = LinearElastic(E=210.0e3, ν=0.3),\n    yield     = 100.0,\n    isotropic = Voce(Hiso=50.0e3, κ∞=100.0),\n    kinematic = ArmstrongFrederick(Hkin=200.0e3, β∞=200.0),\n    overstress = NortonOverstress(tstar=1.0, nexp=2.0),\n))\nfem = create_assembler(mat, dh, ch)\n\nPrefer import MaterialModelsBase over using MaterialModelsBase: both packages export a material_response function with the same semantics by design, so using both makes the unqualified name ambiguous.\n\nNeither MaterialModelsBase nor MechanicalMaterialModels is registered in the General registry at the time of writing, so install them by URL: Pkg.add(url=\"https://github.com/KnutAM/MaterialModelsBase.jl\"). MechanicalMaterialModels additionally depends on Newton.jl, which must be added first via Pkg.add(url=\"https://github.com/KnutAM/Newton.jl\").\n\nFromMaterialModelsBase stores the wrapped material's immutable state snapshots in an MMBState trial/commit pair, so update_states! and revert_states! work as for any bundled material. A local convergence failure, MaterialModelsBase.MaterialConvergenceError, is rethrown as MaterialModelsBaseConvergenceError, a LocalAssemblyFailure, so try_stiffness_matrix and TimeStepController-driven adaptive stepping work unchanged.\n\n\n\n\n\n","title":"FerriteSolidMechanics.FromMaterialModelsBase"},{"category":"type","location":"api/#FerriteSolidMechanics.MMBState","page":"API reference","text":"MMBState(current, previous)\n\nTrial/commit state pair for FromMaterialModelsBase. Both fields hold immutable MaterialModelsBase state snapshots, and the default set_trial! semantics apply. update_state! commits previous ← current, and revert_state! rolls back current ← previous.\n\n\n\n\n\n","title":"FerriteSolidMechanics.MMBState"},{"category":"type","location":"api/#FerriteSolidMechanics.MaterialModelsBaseConvergenceError","page":"API reference","text":"MaterialModelsBaseConvergenceError(error)\n\nRecoverable LocalAssemblyFailure wrapping a MaterialModelsBase.MaterialConvergenceError thrown by a wrapped material's local solve, so that try_stiffness_matrix reports converged = false instead of aborting the outer solve.\n\n\n\n\n\n","title":"FerriteSolidMechanics.MaterialModelsBaseConvergenceError"},{"category":"function","location":"api/#FerriteSolidMechanics._assemble_element!","page":"API reference","text":"_assemble_element!(ke, re, states, material, cellvalues, alphavalues, u, dt)\n\nElement-level extension hook for assembling an element stiffness matrix ke and internal force vector re.\n\nMost materials do not implement _assemble_element!. A generic fallback loops over the quadrature points, evaluates material_response, and integrates the weak form selected by kinematics. Implement _assemble_element! directly only when the element structure itself is material-specific, such as a hand-derived tangent assembled from nodal shape-gradient blocks (VEVP_Zhao2021_AT) or nonlocal regularization.\n\nThis hook does not reach mixed or multi-field formulations. The assembler builds one CellValues per SubDofHandler from its single field interpolation, so it rejects a SubDofHandler carrying more than one field when the assembler is created.\n\nExtension point. The leading underscore indicates that users normally call higher-level assembler functions, but this method is exported so material packages can extend it. The Developer guide walks through a complete worked example.\n\nA custom method is responsible for:\n\nlooping over the quadrature points\nevaluating the strain or the deformation gradient\ncomputing the stress and the tangent\nmultiplying by the integration weight, including the alpha_value(alphavalues, qp) * getdetJdV(cellvalues, qp) factor, which the assembler does not apply\nadding the contributions to ke and re\nwriting trial values into the material-specific current/trial state fields, without committing them\n\nThe assembler calls update_state! only after the outer Newton step has converged. A custom method mutates ke, re, and the state in place and returns nothing; its return value is ignored.\n\n\n\n\n\n","title":"FerriteSolidMechanics._assemble_element!"},{"category":"function","location":"api/#FerriteSolidMechanics._compute_stress_qp","page":"API reference","text":"_compute_stress_qp(material, cellvalues, alphavalues, qp, u_local, state, dt)\n\nMaterial extension hook for postprocessing the stress at one quadrature point.\n\nExtension point. The leading underscore indicates that users normally call higher-level assembler functions, but this method is exported so material packages can extend it.\n\nA generic fallback derives the stress from material_stress, which itself defaults to material_response with the tangent and the trial state discarded. A material implementing the constitutive interface therefore needs no _compute_stress_qp method. For a tangent-free stress path, override the higher-level material_stress instead, which receives the strain measure directly. Define a _compute_stress_qp method only when the output convention needs the cellvalues and quadrature point context itself, as in element-structured models.\n\nA _compute_stress_qp method returns the Cauchy stress at the quadrature point for a finite-strain material, or the Cauchy-like stress for a small-strain material. compute_stresses multiplies that value by alpha_value(alphavalues, qp) before storing it in the output array, so the material must not apply the alpha factor itself. A linear cell that owns no state is redispatched from state === nothing to NoState(). A material with neither a _compute_stress_qp method nor the material_response/kinematics pair throws an ArgumentError instead of returning a silent zero stress.\n\n\n\n\n\n","title":"FerriteSolidMechanics._compute_stress_qp"},{"category":"function","location":"api/#FerriteSolidMechanics.deformation_gradient","page":"API reference","text":"deformation_gradient(cellvalues, qp, u_local)\n\nCompute the deformation gradient F = I + ∇u at quadrature point qp from the given cellvalues and the element-local displacement vector u_local.\n\ncellvalues is a Ferrite CellValues. qp is the quadrature point index. u_local is the displacement vector restricted to the current cell, in the order returned by celldofs(cell).\n\nReturns a Tensor{2,dim} with the element type of u_local, where dim is the spatial dimension. For Float64 displacements this is Tensor{2,3,Float64,9} in 3D or Tensor{2,2,Float64,4} in 2D.\n\n\n\n\n\n","title":"FerriteSolidMechanics.deformation_gradient"},{"category":"type","location":"api/#FerriteSolidMechanics.RemoteAssemblyFailure","page":"API reference","text":"RemoteAssemblyFailure()\n\nRecoverable assembly failure marker that try_stiffness_matrix returns on MPI ranks which did not fail locally, when another rank reported a LocalAssemblyFailure.\n\nThe rank where the failure occurred reports the original material-specific exception as result.error. All ranks receive converged = false, so an adaptive outer solver can reject the step collectively and retry with a smaller dt.\n\n\n\n\n\n","title":"FerriteSolidMechanics.RemoteAssemblyFailure"},{"category":"type","location":"api/#FerriteSolidMechanics.PlaneStressConvergenceError","page":"API reference","text":"PlaneStressConvergenceError\n\nRecoverable failure raised by PlaneStress when the local out-of-plane Newton solve or tangent condensation cannot produce a valid plane stress state. Catch this directly, or call try_stiffness_matrix to receive it as result.error.\n\n\n\n\n\n","title":"FerriteSolidMechanics.PlaneStressConvergenceError"},{"category":"type","location":"api/#FerriteSolidMechanics.VEPD_Detrez2010ConvergenceError","page":"API reference","text":"VEPD_Detrez2010ConvergenceError\n\nRecoverable failure raised when the local VEPD Detrez plastic correction does not converge within its fixed Newton iteration budget.\n\n\n\n\n\n","title":"FerriteSolidMechanics.VEPD_Detrez2010ConvergenceError"},{"category":"type","location":"api/#FerriteSolidMechanics.VEVP_MOAMMMConvergenceError","page":"API reference","text":"VEVP_MOAMMMConvergenceError\n\nRecoverable failure raised when the local VEVP_MOAMMM plastic correction does not converge within its fixed Newton iteration budget.\n\n\n\n\n\n","title":"FerriteSolidMechanics.VEVP_MOAMMMConvergenceError"},{"category":"function","location":"api/#FerriteSolidMechanics.alpha_value","page":"API reference","text":"alpha_value(alphavalues, qp::Int) -> Real\n\nReturn the scaling factor for quadrature point qp.\n\nThe element routine multiplies this factor into the integration weight before assembling into the residual and the stiffness, and compute_stresses applies the same factor to its output:\n\nα_dΩ = alpha_value(alphavalues, qp) * getdetJdV(cellvalues, qp)\n\ncreate_alpha_values creates alphavalues, and the assembler forwards it as the positional av argument to every material's element routine.\n\nWhen create_assembler is called with ah=nothing, alphavalues is nothing and the built-in fallback returns 1.0, so no scaling is applied.\n\nThe alpha_value name, the AlphaValues companion type that create_alpha_values resolves by name, and the α_dΩ integration weight come from the CapriccioSimulation FE–MD coupling code: J. Roksvaag. CAPRICCIO - Tool to run concurrent Finite Element-Molecular Dynamics Simulations (Version 3.0.0), Zenodo (2026). https://doi.org/10.5281/zenodo.18326736\n\n\n\n\n\n","title":"FerriteSolidMechanics.alpha_value"},{"category":"function","location":"api/#FerriteSolidMechanics.create_alpha_values","page":"API reference","text":"create_alpha_values(ah, cellvalues)\n\nBuild the per-cell alpha-values object that the assembler passes to alpha_value at every quadrature point.\n\nThe returned object av must satisfy alpha_value(av, qp::Int) -> Real. The assembler calls create_alpha_values once per cell during construction and stores the result for reuse in every assembly pass.\n\nHow to extend\n\nProvide one of the following; the first match wins:\n\nA method FerriteSolidMechanics.create_alpha_values(ah::MyAlphaSource, cellvalues)\nA constructor AlphaValues(ah, cellvalues) in the same module that defines typeof(ah), which the default fallback calls automatically\n\ncreate_alpha_values throws an ArgumentError if it finds neither.\n\nWhen create_assembler is called with ah=nothing, create_alpha_values receives nothing and returns nothing. That pairs with the alpha_value(::Nothing, ::Int) = 1.0 fallback, so no scaling is applied.\n\n\n\n\n\n","title":"FerriteSolidMechanics.create_alpha_values"},{"category":"section","location":"loads/#External-loads","page":"External loads","text":"LoadHandler assembles the external force vector of a simulation. The code lives in src/Loads.jl.\n\nThis page understands loads as Neumann boundary conditions, i.e., prescribed forces. Prescribed displacements are Dirichlet boundary conditions and are handled by Ferrite's Dirichlet and ConstraintHandler.","title":"External loads"},{"category":"section","location":"loads/#Load-types","page":"External loads","text":"Each load type is named after the units of the value its function returns.\n\nType Units Sets Applies\nBodyForce force/volume in 3D, force/area in 2D cell sets, or all cells the returned vector\nTraction force/area in 3D, force/length in 2D facet sets the returned vector\nPressure same as Traction facet sets -f * n – positive acts inward along normal n\nNodalForce force node sets the returned vector at each node\n\nTraction covers area loads in 3D and edge loads in 2D without requiring a separate type: a facet is a face in 3D and an edge in 2D. Line loads along an edge of a 3D cell are not currently available.","title":"Load types"},{"category":"section","location":"loads/#Building-a-handler","page":"External loads","text":"LoadHandler is built and used the way ConstraintHandler is:\n\nch = ConstraintHandler(dh)\nadd!(ch, Dirichlet(:u, getfacetset(grid, \"left\"), (x, t) -> [0.0, 0.0], [1, 2]))\nclose!(ch)\n\nlh = LoadHandler(assembler)\nadd!(lh, BodyForce(x -> Vec(0.0, -rho * g)))\nadd!(lh, Traction(\"top\", (x, t) -> Vec(0.0, -p * t)))\nadd!(lh, Pressure(\"hole\", (x, t) -> p * t))\nadd!(lh, NodalForce(\"tip\", x -> Vec(0.0, -F); distribute=true))\nclose!(lh)\n\nThe strings \"top\", \"hole\" and \"tip\" are Ferrite grid set names. They are checked against the grid once the handler is closed using close!. Alternatively, a set object is accepted as well, so Traction(\"top\", f) and Traction(getfacetset(grid, \"top\"), f) are equivalent.\n\nLoadHandler(assembler) reads the DofHandler, the thickness and the quadrature orders from the assembler, so external and internal forces are integrated consistently. This matters in 2D, where facet and body integrals scale with thickness exactly as ke and re do. The bare LoadHandler(dh) form is for drivers that assemble no internal forces and defaults to thickness = 1.0.\n\nclose! throws an ArgumentError, naming the entry at fault, for an unknown or empty set, a facet or node carrying no degrees of freedom, a load function whose call signature matches none of the accepted ones, or a function returning the wrong shape.","title":"Building a handler"},{"category":"section","location":"loads/#Load-functions","page":"External loads","text":"A load function may be defined as f(x), f(x, t) or f(x, t, n), and close! selects the signature the function actually defines. x is the spatial coordinate and n is the outward facet normal, available on Traction and Pressure only. t is the value handed to external_forces! in the time loop, as shown in Placement in the Newton loop below.\n\nSince f receives x and t, spatial and temporal profiles are written directly in the load function:\n\nTraction(\"top\", x -> Vec(0.0, -p))                                 # uniform, constant in t\nTraction(\"top\", (x, t) -> Vec(0.0, -p * t))                        # uniform, ramped in t\nTraction(\"top\", x -> Vec(0.0, -p * x[1] / L))                      # triangular along x, constant in t\nTraction(\"top\", (x, t) -> Vec(0.0, -p * (x[1] / L) * t))           # triangular along x, ramped in t\nTraction(\"top\", (x, t) -> Vec(0.0, -p * (x[1] / L)^2 * sin(w*t)))  # quadratic in x, sinusoidal in t\nPressure(\"dam\", x -> rho * g * max(H - x[2], 0.0))                 # hydrostatic, zero above the free surface at y = H\nBodyForce(x -> Vec(0.0, -rho * g))                                 # self-weight\nBodyForce(\"rotor\", x -> rho * omega^2 * x)                         # centrifugal about the origin\n\nt carries the meaning the calling code gives it: physical time for rate-dependent materials, or a load factor for quasi-static ramping. Give the same value to Ferrite's update!(ch, t) so that prescribed displacements and loads refer to the same instant.\n\nn is evaluated on the undeformed geometry. A normal-aligned traction is therefore a dead load, i.e., fixed in direction at the start and not rotating as the surface deforms. Follower loads, whose direction tracks the deforming surface, are not currently available.","title":"Load functions"},{"category":"section","location":"loads/#Placement-in-the-Newton-loop","page":"External loads","text":"stiffness_matrix returns the internal force vector, so the residual is formed in your Newton loop:\n\nf_ext = external_forces!(lh, t)\nfor iter in 1:max_newton\n    K, f_int = stiffness_matrix(assembler, u; dt=dt)\n    residual .= f_int .- f_ext\n    apply_zero!(K, residual, ch)\n    norm(residual) <= tol * max(norm(f_ext), 1.0) && break\n    u .-= K \\ residual\nend\n\nThree points follow from this:\n\nDead loads do not depend on the displacement, so external_forces! is called once per step, outside the Newton loop.\nExternal loads contribute nothing to K.\nScaling the convergence threshold with norm(f_ext) makes it relative to the applied load. The max(..., 1.0) covers pure displacement control for cases where norm(f_ext) is zero.\n\nUnder adaptive time stepping, a rejected step changes the trial time, so external_forces! is called again with the new value before the step is retried.\n\nexternal_forces! writes into a vector belonging to the LoadHandler and returns it. Calling external_forces! again overwrites that vector, so copy the result when a step's values are needed afterwards.","title":"Placement in the Newton loop"},{"category":"section","location":"loads/#Changing-the-loading-during-a-simulation","page":"External loads","text":"Changing the magnitude, direction or profile of a load needs no further call to close!, because the load function is evaluated on every external_forces! call.\n\nAny profile that can be written in terms of t works directly, including switching a load off:\n\n# zero(Vec{2}) is the null traction of a 2D problem; use Vec{3} in 3D\nTraction(\"top\", (x, t) -> t < t_hold ? Vec(0.0, -p * t) : zero(Vec{2}))  # ramp, then release\nTraction(\"top\", (x, t) -> Vec(0.0, -p * sin(w * t)))                     # cyclic, sign reversal included\n\nLoading that depends on future conditions rather than on t alone is written by closing over a Ref:\n\nstage = Ref(1)\nadd!(lh, Traction(\"top\", (x, t) -> stage[] == 1 ? Vec(0.0, -p) : zero(Vec{2})))\n# inside the time loop; the next external_forces! call picks up the new value\npeak_stress > sigma_crit && (stage[] = 2)\n\nThe same pattern removes a load, since a function returning zero contributes nothing to the external force vector.\n\nA load on a set that was never declared is the only case requiring another close!(lh) call. This prepares the entries added since the last call and leaves earlier ones untouched:\n\nadd!(lh, Traction(\"second_stage_face\", (x, t) -> Vec(0.0, -q * (t - t_switch))))\nclose!(lh)","title":"Changing the loading during a simulation"},{"category":"section","location":"loads/#Replication-under-MPI","page":"External loads","text":"LoadHandler assembles serially and, under MPI, redundantly: every rank walks every node, facet, and cell, and builds the complete external force vector. No reduction is performed, and every rank must add the same loads in the same order so that the per-rank copies agree.\n\nThis matches the replicated-data design described on the Concepts page, where stiffness_matrix has already reduced the internal force so that it is complete on every rank as well. Adding an MPI.Allreduce over f_ext would sum those identical copies and apply the load nranks times.","title":"Replication under MPI"},{"category":"section","location":"loads/#Distributing-a-resultant","page":"External loads","text":"NodalForce(set, f) applies the force returned by f at every node of the set, which is the default distribute=false. With distribute=true that force is instead read as the resultant of the whole set and split equally over its nodes, so the sum over the set is the force itself.\n\nEqual splitting matches the finite element load distribution only for first-order shape functions. Above first order the shape function integration no longer splits the load equally, so a uniform Traction on a quadratic mesh leads to different shares at corner and midside nodes. To spread a resultant over a surface on such a mesh, use Traction with magnitude F / A.","title":"Distributing a resultant"},{"category":"section","location":"models/arruda_boyce/#Arruda–Boyce","page":"Arruda–Boyce","text":"ArrudaBoyce is a stateless finite-strain hyperelastic material model (src/models/ArrudaBoyce.jl). It implements Arruda and Boyce's eight-chain network response [1] with an isochoric network term and a quadratic volumetric penalty.","title":"Arruda–Boyce"},{"category":"section","location":"models/arruda_boyce/#Mathematical-formulation","page":"Arruda–Boyce","text":"The model uses the deformation gradient boldsymbolF, the right Cauchy-Green tensor boldsymbolC = boldsymbolF^mathrmTboldsymbolF, and the Jacobian J = detboldsymbolF = sqrtdetboldsymbolC text Its isochoric first invariant is defined as\n\nbarI_1 = J^-23operatornametrboldsymbolC text\n\nThe implemented free energy density is defined as\n\nPsi(boldsymbolC)\n=\nmu sum_i=1^5fracc_iN^i-1\nleftbarI_1^i - 3^iright\n+ frackappa2leftJ - 1right^2 text\n\nwhile the truncated inverse-Langevin series coefficients are\n\nc_1=frac12 textquad\nc_2=frac120 textquad\nc_3=frac111050 textquad\nc_4=frac197000 textquad\nc_5=frac519673750 text\n\nHere, mu is the network shear modulus, kappa the bulk modulus, and N the limiting chain extensibility parameter. It sets the stiffness scale of the eight-chain network; the resulting small-strain shear modulus is mu_0 = muleft(1 + frac35N + mathcalO(N^-2)right), which approaches mu as Ntoinfty. The subtracted constants 3^i make the isochoric part vanish at boldsymbolC=boldsymbolI, where barI_1 = 3. The volumetric part vanishes there as well, so Psi(boldsymbolI)=0 holds in the undeformed state.\n\nThe second Piola-Kirchhoff stress (PK2) boldsymbolS follows from differentiating the free energy density as\n\nboldsymbolS\n=\n2fracpartialPsipartialboldsymbolC text\n\nThe first Piola-Kirchhoff stress (PK1) used by the assembler is\n\nboldsymbolP = boldsymbolFboldsymbolS text\n\nwhile the Cauchy stress follows as\n\nboldsymbolsigma\n=\nfrac1JboldsymbolPboldsymbolF^mathrmT text","title":"Mathematical formulation"},{"category":"section","location":"models/arruda_boyce/#Implementation-details","page":"Arruda–Boyce","text":"ArrudaBoyce is rate-independent and stateless (create_state returns NoState()). The dt keyword is ignored.\n\nBy default (tangent=:AT), the element assembly uses a hardcoded exact material tangent for ∂S/∂C and maps it to the tangent ∂P/∂F. Using the keyword argument tangent=:AD, the same stress formulation is differentiated by Tensors.gradient over the PK1 response. Both tangent modes are exact. Depending on your case, :AD may compute faster than the constructor default :AT. The generic 2D wrappers use the model's compute_PK1_3D and update_state_from_3D! hooks. Since the material has no history, update_state_from_3D! is a no-op.","title":"Implementation details"},{"category":"section","location":"models/arruda_boyce/#Constructor","page":"Arruda–Boyce","text":"ArrudaBoyce(μ, κ, N; tangent=:AT)\n\nAll three constructor arguments must be positive. The optional tangent keyword must be :AT or :AD.","title":"Constructor"},{"category":"section","location":"models/arruda_boyce/#2D-usage","page":"Arruda–Boyce","text":"material = PlaneStress(ArrudaBoyce(μ, κ, N))   # plane stress\nmaterial = PlaneStrain(ArrudaBoyce(μ, κ, N))   # plane strain\n\nFor wrapper mechanics, see Wrappers.","title":"2D usage"},{"category":"section","location":"models/arruda_boyce/#Model-parameters","page":"Arruda–Boyce","text":"Constructor argument Symbol Description\nμ mu Network shear modulus\nκ kappa Bulk modulus\nN N Limiting chain extensibility parameter","title":"Model parameters"},{"category":"section","location":"models/arruda_boyce/#Provenance-and-acknowledgements","page":"Arruda–Boyce","text":"The constitutive equations follow Arruda and Boyce's eight-chain model [1]. The decoupled isochoric–volumetric split follows Flory [2]. The Julia implementation was written for FerriteSolidMechanics.jl; no external code was translated or adapted.\n\nThe implementation is maintained by Lukas Laubert. Sebastian Pfaller is gratefully acknowledged for proofreading this page.","title":"Provenance and acknowledgements"},{"category":"section","location":"models/arruda_boyce/#References","page":"Arruda–Boyce","text":"[1] Arruda, E. M., & Boyce, M. C. (1993). A three-dimensional constitutive model for the large stretch behavior of rubber elastic materials. Journal of the Mechanics and Physics of Solids, 41(2), 389-412. doi:10.1016/0022-5096(93)90013-6\n\n[2] Flory, P. J. (1961). Thermodynamic relations for high elastic materials. Transactions of the Faraday Society, 57, 829-838. doi:10.1039/TF9615700829","title":"References"},{"category":"section","location":"models/neo_hooke/#Neo–Hookean","page":"Neo–Hookean","text":"The compressible Neo–Hookean hyperelastic material model NeoHooke (src/models/NeoHooke.jl) is parameterized by Young's modulus E and Poisson's ratio ν.","title":"Neo–Hookean"},{"category":"section","location":"models/neo_hooke/#Mathematical-formulation","page":"Neo–Hookean","text":"The material's behavior is derived from its free energy density function Psi(boldsymbolC) as\n\nPsi(boldsymbolC) = frac12mu leftI_1 - 3 - 2ln Jright + frac12lambda leftJ - 1right^2 text\n\nwhere boldsymbolF is the deformation gradient, boldsymbolC = boldsymbolF^T boldsymbolF the right Cauchy–Green tensor, I_1 = mathrmtr(boldsymbolC) its first invariant, J = det(boldsymbolF) = sqrtdet(boldsymbolC) the volume ratio, and mu lambda the Lamé parameters.\n\nThe parameters mu (shear modulus) and lambda (first Lamé parameter) are computed from the input Young's modulus E and Poisson's ratio nu as\n\nmu = fracE2left1 + nuright qquad lambda = fracEnuleft1 + nurightleft1 - 2nuright text","title":"Mathematical formulation"},{"category":"section","location":"models/neo_hooke/#Implementation-details","page":"Neo–Hookean","text":"NeoHooke is rate-independent and stateless (create_state returns NoState()). The tangent is obtained analytically via Tensors.hessian over the free energy Ψ(C), yielding both the second Piola-Kirchhoff stress (PK2) S = 2 ∂Ψ/∂C and the material derivative ∂S/∂C in a single call. The generic 2D wrappers use the model's compute_PK1_3D and update_state_from_3D! hooks. Since the material has no history, update_state_from_3D! is a no-op.","title":"Implementation details"},{"category":"section","location":"models/neo_hooke/#Constructor","page":"Neo–Hookean","text":"NeoHooke(E, ν)","title":"Constructor"},{"category":"section","location":"models/neo_hooke/#2D-usage","page":"Neo–Hookean","text":"material = PlaneStress(NeoHooke(E, ν))   # plane stress\nmaterial = PlaneStrain(NeoHooke(E, ν))   # plane strain\n\nFor wrapper mechanics, see Wrappers.","title":"2D usage"},{"category":"section","location":"models/neo_hooke/#Model-parameters","page":"Neo–Hookean","text":"Constructor argument Symbol Description\nE E Young's modulus\nν nu Poisson's ratio","title":"Model parameters"},{"category":"section","location":"models/neo_hooke/#Provenance","page":"Neo–Hookean","text":"The constitutive energy, stress/tangent derivation, element-level linearization, and related documentation are adapted from the Ferrite.jl hyperelasticity tutorial [1]. The upstream copyright/license notice is retained in the repository LICENSE.","title":"Provenance"},{"category":"section","location":"models/neo_hooke/#References","page":"Neo–Hookean","text":"[1] Ferrite.jl documentation. Hyperelasticity tutorial. https://ferrite-fem.github.io/Ferrite.jl/stable/tutorials/hyperelasticity/","title":"References"},{"category":"section","location":"experimental/#Experimental-models","page":"Experimental models","text":"note: Note\nThe variants under FerriteSolidMechanics.Experimental are not exported by the top-level package. They are alternative implementations kept for reference and comparison, are not part of the stable API, and may be renamed or removed in future versions.\n\nThe exported models are listed on the Material models page. Provenance and permissions for these variants follow the corresponding stable model pages: VEPD Detrez 2010, VEVP Zhao 2021, and VEVP MOAMMM.\n\nwarning: Rollback behavior\nSome VEPD experimental variants do not define model-specific revert_state! methods. Treat rejected-step rollback behavior as variant-specific unless documented below.","title":"Experimental models"},{"category":"section","location":"experimental/#Importing-variants","page":"Experimental models","text":"The variants live in a separate submodule. To use one, import the symbol explicitly:\n\nusing FerriteSolidMechanics\nusing FerriteSolidMechanics.Experimental: VEPD_Detrez2010_Optimized, VEPD_Detrez2010_Implicit, VEPD_Detrez2010_ExactVisco, VEPD_Detrez2010_ClosedCVEndStep, VEVP_Zhao2021_AD_Simplified, VEVP_Zhao2021_AT_Matlab, VEVP_MOAMMM_VarsN","title":"Importing variants"},{"category":"section","location":"experimental/#VEVP_MOAMMM_VarsN","page":"Experimental models","text":"src/models/experimental/VEVP_MOAMMM_VarsN.jl. Archived previous implementation of VEVP_MOAMMM with a flat Vector{Float64} state buffer (vars/vars_n) rather than the named typed fields of the exported version. It follows the same constitutive equations and Drucker–Prager/Perzyna local solver, stores history in the original Fortran STATEV-style layout, and uses the same Tensors.gradient AD tangent. Use VEVP_MOAMMM for new work.","title":"VEVP_MOAMMM_VarsN"},{"category":"section","location":"experimental/#VEVP_Zhao2021_AD_Simplified","page":"Experimental models","text":"src/models/experimental/VEVP_Zhao2021_AD_Simplified.jl. Comparison variant of VEVP_Zhao2021_AD with the same history fields (Cik, muVk, strain_maxk). It uses a simpler stress path: the stress returned from the local solve is assembled from trial/previous-state branch stresses, while the updated history fields are still stored for the next step. It supports the same optional dt_scale keyword as VEVP_Zhao2021_AD and applies it to dt inside the constitutive update.","title":"VEVP_Zhao2021_AD_Simplified"},{"category":"section","location":"experimental/#VEVP_Zhao2021_AT_Matlab","page":"Experimental models","text":"src/models/experimental/VEVP_Zhao2021_AT_Matlab.jl. Archived previous AT implementation with Matrix{Float64} storage, following the CAPRICCIO/Zhao Matlab routines more directly than the exported VEVP_Zhao2021_AT. It is kept so the translation is not lost; use VEVP_Zhao2021_AT for new work. It supports the same optional dt_scale keyword as VEVP_Zhao2021_AT.","title":"VEVP_Zhao2021_AT_Matlab"},{"category":"section","location":"experimental/#VEPD_Detrez2010_Optimized","page":"Experimental models","text":"src/models/experimental/VEPD_Detrez2010_Optimized.jl. Experimental variant that closely follows VEPD_Detrez2010 under separate type and state names. It uses the same state quantities (Fp, p, Cv) and the same broad update structure: plastic return loop, linear (1 + Delta p*N) plastic map, exponential Maxwell-branch update, damage scaling, and AD tangent assembly.\n\nThis variant is not a full drop-in replacement for VEPD_Detrez2010: it lacks the positional constructor and a model-specific revert_state! method.","title":"VEPD_Detrez2010_Optimized"},{"category":"section","location":"experimental/#VEPD_Detrez2010_Implicit","page":"Experimental models","text":"src/models/experimental/VEPD_Detrez2010_Implicit.jl. Variant of VEPD_Detrez2010 that keeps the same plastic-return structure and linear (1 + Delta p*N) plastic flow map, but uses an implicit/rational Maxwell-branch update: Cv_next = (Cv_n + (2dt/tau) C) / (1 + 2dt/tau). It also omits the damage cap used by VEPD_Detrez2010 and does not define a model-specific revert_state!.","title":"VEPD_Detrez2010_Implicit"},{"category":"section","location":"experimental/#VEPD_Detrez2010_ExactVisco","page":"Experimental models","text":"src/models/experimental/VEPD_Detrez2010_ExactVisco.jl. Comparison variant of VEPD_Detrez2010 around the Maxwell-branch evolution. Both this variant and VEPD_Detrez2010 use an exponential Maxwell update, so this is not a higher-accuracy viscoelastic option. It also differs mechanically from VEPD_Detrez2010: the Arruda–Boyce network stress is omitted, the network parameters n_ab and mu_ab are stored but unused, and damage is not clamped to 1.0. This variant also does not implement a model-specific revert_state!. Use it only for source comparison.","title":"VEPD_Detrez2010_ExactVisco"},{"category":"section","location":"experimental/#VEPD_Detrez2010_ClosedCVEndStep","page":"Experimental models","text":"src/models/experimental/VEPD_Detrez2010_ClosedCVEndStep.jl. Archived copy of the former VEPD_Detrez2010 default implementation: end-step plastic return mapping with closed-form boldsymbolC^mathrmv Maxwell updates. It is available as FerriteSolidMechanics.Experimental.VEPD_Detrez2010_ClosedCVEndStep.\n\nThis variant is kept as a performance/reference baseline for the current VEPD_Detrez2010 default settings plastic_update=:end_step, maxwell_update=:closed_form_cv. In the local scratch benchmark it was about 2–4% faster than the current default implementation, with zero differences in the compared final sigma_11, accumulated plastic strain, and boldsymbolC^mathrmv branch states.","title":"VEPD_Detrez2010_ClosedCVEndStep"},{"category":"section","location":"tutorials/cantilever_dma/#DMA-cantilever-beam","page":"DMA cantilever beam","text":"A 3D cantilever beam under sinusoidal, displacement-controlled loading, solved with the VEVP_MOAMMM viscoelastic-viscoplastic model. The runnable script is examples/cantilever_beam_dma.jl and can be executed from the repository root with:\n\njulia --project=. examples/cantilever_beam_dma.jl\n\nThe direct run writes plot and VTK output. Install Plots.jl and WriteVTK.jl in the active environment before running it with output enabled.\n\nFor a short run without plot or VTK output, start Julia with julia --project=. and run:\n\ninclude(\"examples/cantilever_beam_dma.jl\")\nresult = run_cantilever_beam_dma(; nx=3, ny=1, nz=1, nsteps=4, vtk=nothing)\nprintln(\"converged steps: \", result.converged_steps)","title":"DMA cantilever beam"},{"category":"section","location":"tutorials/cantilever_dma/#Problem-setup","page":"DMA cantilever beam","text":"The beam extends along the x-axis from the clamped root at x = 0 to the controlled tip at Lx. The root face is fixed in all three displacement directions. The tip face is fixed in x and z, while the y-displacement follows u_y = A sin(4*pi*t/total_time).\n\nThis prescribed displacement creates a cyclic loading-unloading path. The force-displacement loop in Figure 1 shows the phase lag and indicates energy dissipation of the viscoelastic response.\n\n(Image: Computed DMA cantilever response) Figure 1. Hysteresis loop showing viscoelastic response of the cantilever.","title":"Problem setup"},{"category":"section","location":"tutorials/cantilever_dma/#Grid,-interpolation,-constraints","page":"DMA cantilever beam","text":"We set up a 3D hexahedral beam, a quadratic displacement field, and the two Dirichlet boundaries with standard Ferrite.jl calls:\n\nnx, ny, nz = 8, 2, 2                              # default mesh resolution\nLx, Ly, Lz = 5.0, 1.0, 1.0                        # beam dimensions\ngrid = generate_grid(Hexahedron, (nx, ny, nz), Vec(0.0, 0.0, 0.0), Vec(Lx, Ly, Lz)) # create the beam mesh\n\ninterpolation = Serendipity{RefHexahedron, 2}()^3 # quadratic displacement shape functions in 3D\ndh = DofHandler(grid)                             # create the dof handler for the grid\nadd!(dh, :u, interpolation)                       # add displacement dofs under the field name `:u`\nclose!(dh)                                        # finalize the dof numbering\n\namplitude = 0.05                                  # prescribed tip-displacement amplitude\ntotal_time = 8.0                                  # duration of two loading cycles\nch = ConstraintHandler(dh)                        # collect Dirichlet boundary conditions\nadd!(ch, Dirichlet(:u, getfacetset(grid, \"left\"),  (x, t) -> [0.0, 0.0, 0.0], [1, 2, 3])) # clamp the root\nadd!(ch, Dirichlet(:u, getfacetset(grid, \"right\"), (x, t) -> [0.0, amplitude * sin(4.0 * pi * t / total_time), 0.0], [1, 2, 3])) # drive the tip in y\nclose!(ch)                                        # finalize the constraint data\n\nFor the tip reaction, only the y-component displacement DOFs on the right face are collected:\n\nfunction _facet_component_dofs(dh, facets, interpolation, component)\n    ncomp = Ferrite.n_components(interpolation)     # number of vector components in :u\n    base_interpolation = Ferrite.get_base_interpolation(interpolation) # scalar shape functions behind the vector field\n    facet_dofs = Ferrite.dirichlet_facetdof_indices(base_interpolation) # local scalar dofs on each facet\n    facet_cache = FacetCache(dh)                    # reusable cache for cell/facet dof lookup\n    dofs = Int[]                                    # collected global dof numbers\n\n    for facet in facets\n        reinit!(facet_cache, facet)                 # load the current boundary facet\n        _, facet_id = facet                         # local face number in the current cell\n        for local_shape_dof in facet_dofs[facet_id] # scalar shape dofs on this face\n            local_component_dof = (local_shape_dof - 1) * ncomp + component # select component 2, i.e. y\n            push!(dofs, celldofs(facet_cache)[local_component_dof]) # map local dof to global dof\n        end\n    end\n\n    sort!(dofs)                                     # neighboring facets share edge/corner dofs\n    unique!(dofs)                                   # keep each reaction dof once\n    return dofs\nend\n\nreact_dofs = _facet_component_dofs(dh, getfacetset(grid, \"right\"), interpolation, 2) # right-face y dofs\n\nreact_dofs is only used to extract the reaction force from the residual vector before constrained entries are zeroed. It does not affect assembly or the actual Dirichlet constraints.","title":"Grid, interpolation, constraints"},{"category":"section","location":"tutorials/cantilever_dma/#Material","page":"DMA cantilever beam","text":"We apply VEVP_MOAMMM with eight bulk and eight shear Maxwell branches:\n\nmat = VEVP_MOAMMM(\n    6, 80.0, 40.0, 0.25, 0.3, 2.0, 1.0,\n    40.0, 10.0, 5.0, 1.0, 45.0, 10.0, 5.0, 1.0,\n    1.0, 0.1, 0.01,\n    [60.0, 70.0, 80.0, 90.0, 100.0, 110.0, 120.0, 130.0],\n    [0.2625, 0.3375, 0.4125, 0.4875, 0.60, 0.75, 0.9375, 1.20],\n    [60.0, 70.0, 80.0, 90.0, 100.0, 110.0, 120.0, 130.0],\n    [0.2625, 0.3375, 0.4125, 0.4875, 0.60, 0.75, 0.9375, 1.20],\n)\n\nThe relaxation times are chosen near the loading period to cause a significant phase lag. With the default amplitude and parameters, the response stays in the viscoelastic range, so the visible hysteresis is driven by the Maxwell branches rather than by plastic flow. See VEVP MOAMMM for the parameter glossary.","title":"Material"},{"category":"section","location":"tutorials/cantilever_dma/#Assembler","page":"DMA cantilever beam","text":"create_assembler combines the material, dof handler, and constraints into the reusable FerriteSolidMechanics object:\n\nassembler = create_assembler(mat, dh, ch; quadrature_order=2)\n\nThe assembler stores one material state per quadrature point of each nonlinear cell and reuses its global stiffness matrix, residual vector, and per-cell element matrices/vectors across time steps.","title":"Assembler"},{"category":"section","location":"tutorials/cantilever_dma/#Time-and-Newton-loop","page":"DMA cantilever beam","text":"The prescribed tip displacement is advanced in equal physical time steps. stiffness_matrix evaluates the material response for the current trial displacement, and update_states!(assembler) commits the converged material state:\n\nu = zeros(ndofs(dh))                    # allocate the displacement vector\nnsteps = 96                             # number of equal physical time steps\nmax_newton = 180                        # maximum Newton iterations per time step\nnewton_tol = 1e-6                       # residual tolerance\ndt = total_time / nsteps                # physical time increment\n\nfor step in 1:nsteps                    # advance the imposed displacement\n    t = step * dt                       # current physical time\n    update!(ch, t)                      # update the sinusoidal boundary value\n    apply!(u, ch)                       # write prescribed displacements into `u`\n\n    for iter in 1:max_newton            # Newton iterations for this time step\n        K, r = stiffness_matrix(assembler, u; dt=dt) # constitutive evaluation and tangent/residual assembly\n        fy_tip = isempty(react_dofs) ? 0.0 : sum(r[react_dofs]) # exract reaction before constrained dofs are zeroed\n        apply_zero!(K, r, ch)           # apply constraints for constrained solving\n        if norm(r) < newton_tol         # stop once the residual is small enough\n            break\n        end\n        u .-= K \\ r                     # update the displacement vector\n    end\n    update_states!(assembler)           # commit material states after convergence\nend\n\nHere, dt is the physical time increment of the sinusoidal loading. Because VEVP_MOAMMM is strain rate-dependent, this value enters the local viscoelastic-viscoplastic update and affects the result.\n\nThe tip reaction must be read before apply_zero!. After apply_zero!, the constrained residual entries are overwritten to consider the Cirichlet boundary conditions and no longer contain the reaction force.","title":"Time and Newton loop"},{"category":"section","location":"tutorials/cantilever_dma/#Postprocessing","page":"DMA cantilever beam","text":"We record three histories at each converged step: time t, tip displacement u_y, and tip reaction F_y. After the last step, compute_stresses evaluates the Cauchy stress field:\n\nstresses = compute_stresses(assembler, u)\n\nphase_lag_degrees(result) estimates the first-harmonic phase lag between tip displacement and reaction force. plot_results(result) writes a three-panel figure with displacement history, reaction history, and the hysteresis loop.\n\nWith the default settings, the direct run prints a short summary similar to:\n\nSolved 3D cantilever beam DMA\n  cells: 32\n  dofs: 783\n  converged steps: 96\n  peak tip uy: 0.05\n  force/displacement phase lag: 38.92 degrees\n  stress entries: 256\n\nOutput files are written to examples/results/cantilever_beam_dma/ when plotting or VTK output is enabled.","title":"Postprocessing"},{"category":"section","location":"tutorials/cantilever_dma/#Swapping-the-material-model","page":"DMA cantilever beam","text":"The solve loop above can drive any material model that is a subtype of AbstractMaterial. To swap VEVP_MOAMMM for another material model, change only the material constructor:\n\n# Same script, different material:\nmat = J2Plasticity(70e3, 0.3, 250.0, 1e3)\nmat = VEVP_Zhao2021_AD(1e3, 1e3, 8, 0.1, 0.5, 0.8, 0.5, 1e-3, 1.0, 2.0, 4.0, 0.0, 0.5, 1.0)\n\nThe assembler calls the new material's element routine and state-update methods through the same interface.","title":"Swapping the material model"},{"category":"section","location":"tutorials/cantilever_dma/#Plain-program","page":"DMA cantilever beam","text":"The full runnable source is rendered from examples/cantilever_beam_dma.jl:\n\nusing Markdown\nimport FerriteSolidMechanics\npath = joinpath(pkgdir(FerriteSolidMechanics), \"examples\", \"cantilever_beam_dma.jl\")\nMarkdown.parse(\"```julia\\n\" * read(path, String) * \"\\n```\")","title":"Plain program"},{"category":"section","location":"models/mooney_rivlin/#Mooney–Rivlin","page":"Mooney–Rivlin","text":"MooneyRivlin is a stateless finite-strain hyperelastic material model (src/models/MooneyRivlin.jl). It implements the two-parameter Mooney–Rivlin response [1, 2], expressed here in the isochoric–volumetric split with a quadratic volumetric penalty.","title":"Mooney–Rivlin"},{"category":"section","location":"models/mooney_rivlin/#Mathematical-formulation","page":"Mooney–Rivlin","text":"The model uses the deformation gradient boldsymbolF, the right Cauchy-Green tensor boldsymbolC=boldsymbolF^mathrmTboldsymbolF, and the volume ratio J=detboldsymbolF=sqrtdetboldsymbolC. The first two invariants of boldsymbolC are\n\nI_1\n=\noperatornametrboldsymbolC text\nqquad\nI_2\n=\nfrac12leftleft(operatornametrboldsymbolCright)^2 - boldsymbolCboldsymbolCright text\n\nMooney's original theory [1] was expressed in principal stretches; Rivlin [2] later developed the invariant-based form. The same invariants can equivalently be evaluated from the left Cauchy-Green tensor boldsymbolb=boldsymbolFboldsymbolF^mathrmT, since boldsymbolb and boldsymbolC have identical principal invariants (see e.g., Holzapfel [3], Ch. 6).\n\nThe implemented isochoric invariants are\n\nbarI_1 = J^-23 I_1 text\nqquad\nbarI_2 = J^-43 I_2 text\n\nwhile the implemented free energy density is\n\nPsi(boldsymbolC)\n=\nC_10leftbarI_1 - 3right\n+ C_01leftbarI_2 - 3right\n+ frackappa2leftJ - 1right^2 text\n\nHere, C_10 and C_01 control the distortional response and kappa is the bulk modulus of the volumetric penalty. The small-strain shear modulus at the reference state is mu_0 = 2leftC_10+C_01right.","title":"Mathematical formulation"},{"category":"section","location":"models/mooney_rivlin/#Stress-evaluation","page":"Mooney–Rivlin","text":"The second Piola-Kirchhoff stress (PK2) is\n\nboldsymbolS\n=\n2fracpartialPsipartialboldsymbolC text\n\nWith boldsymbolI denoting the identity tensor, the implemented closed-form PK2 stress is\n\nboldsymbolS\n=\n2 C_10 J^-23\nleftboldsymbolI - fracI_13boldsymbolC^-1right\n+ 2 C_01 J^-43\nleftI_1boldsymbolI - boldsymbolC - frac2I_23boldsymbolC^-1right\n+ kappa JleftJ-1rightboldsymbolC^-1 text\n\nThe first Piola-Kirchhoff stress (PK1) used by the assembler is\n\nboldsymbolP = boldsymbolFboldsymbolS text\n\nwhile the Cauchy stress is\n\nboldsymbolsigma\n=\nfrac1JboldsymbolPboldsymbolF^mathrmT text","title":"Stress evaluation"},{"category":"section","location":"models/mooney_rivlin/#Implementation-details","page":"Mooney–Rivlin","text":"","title":"Implementation details"},{"category":"section","location":"models/mooney_rivlin/#State-variables","page":"Mooney–Rivlin","text":"MooneyRivlin is rate-independent and stateless (create_state returns NoState()). The dt keyword is ignored.","title":"State variables"},{"category":"section","location":"models/mooney_rivlin/#Tangent-formulations","page":"Mooney–Rivlin","text":"Two exact tangent routes are provided:\n\ntangent=:AD: differentiates the PK1 stress response with Tensors.gradient.\ntangent=:AT: uses the hardcoded material tangent ∂S/∂C and maps it to ∂P/∂F.\n\nThe constructor default is tangent=:AD; representative stiffness_matrix checks favor this route for this model.","title":"Tangent formulations"},{"category":"section","location":"models/mooney_rivlin/#Wrapper-paths","page":"Mooney–Rivlin","text":"The generic 2D wrappers use the model's compute_PK1_3D and update_state_from_3D! hooks. Since the material has no history, update_state_from_3D! is a no-op.","title":"Wrapper paths"},{"category":"section","location":"models/mooney_rivlin/#Constructor","page":"Mooney–Rivlin","text":"MooneyRivlin(C10, C01, κ; tangent=:AD)\n\nThe optional tangent keyword must be :AD or :AT. The constructor requires finite C10 and C01, a positive sum C10 + C01, and a positive finite κ. Individual negative values of C10 or C01 are accepted when their sum is positive. The constructor check does not guarantee stability over all finite-strains.","title":"Constructor"},{"category":"section","location":"models/mooney_rivlin/#2D-usage","page":"Mooney–Rivlin","text":"material = PlaneStress(MooneyRivlin(C10, C01, κ))   # plane stress\nmaterial = PlaneStrain(MooneyRivlin(C10, C01, κ))   # plane strain\n\nFor wrapper mechanics, see Wrappers.","title":"2D usage"},{"category":"section","location":"models/mooney_rivlin/#Model-parameters","page":"Mooney–Rivlin","text":"Constructor argument Symbol Description\nC10 C_10 Coefficient of the first modified invariant\nC01 C_01 Coefficient of the second modified invariant\nκ kappa Bulk modulus\ntangent - Selects the exact tangent route, :AD by default or optional :AT","title":"Model parameters"},{"category":"section","location":"models/mooney_rivlin/#Provenance-and-acknowledgements","page":"Mooney–Rivlin","text":"The constitutive equations follow the two-invariant form of Mooney [1] and Rivlin [2]. The decoupled isochoric–volumetric split follows Flory [4]. The Julia implementation was written for FerriteSolidMechanics.jl; no external code was translated or adapted.\n\nThe implementation is maintained by Lukas Laubert. Sebastian Pfaller is gratefully acknowledged for proofreading this page.","title":"Provenance and acknowledgements"},{"category":"section","location":"models/mooney_rivlin/#References","page":"Mooney–Rivlin","text":"[1] Mooney, M. (1940). A theory of large elastic deformation. Journal of Applied Physics, 11(9), 582-592. doi:10.1063/1.1712836\n\n[2] Rivlin, R. S. (1948). Large elastic deformations of isotropic materials. IV. Further developments of the general theory. Philosophical Transactions of the Royal Society of London. Series A, Mathematical and Physical Sciences, 241(835), 379-397. doi:10.1098/rsta.1948.0024\n\n[3] Holzapfel, G. A. (2000). Nonlinear Solid Mechanics: A Continuum Approach for Engineering. John Wiley & Sons. (Chapter 6, Hyperelastic Materials).\n\n[4] Flory, P. J. (1961). Thermodynamic relations for high elastic materials. Transactions of the Faraday Society, 57, 829-838. doi:10.1039/TF9615700829","title":"References"},{"category":"section","location":"tutorials/plate_with_hole/#2D-plate-with-a-hole","page":"2D plate with a hole","text":"A plane stress, displacement-controlled quarter plate with a circular hole, solved with a NeoHooke material wrapped by PlaneStress. The runnable script is examples/plate_with_hole_planestress.jl and can be executed from the repository root with:\n\njulia --project=. examples/plate_with_hole_planestress.jl","title":"2D plate with a hole"},{"category":"section","location":"tutorials/plate_with_hole/#Problem-setup","page":"2D plate with a hole","text":"We model a quarter of a square plate with a quarter-circle hole in the corner. The plate is plate_size × plate_size, the hole has radius radius. For symmetry, the bottom and left edges are clamped in their normal direction; the right edge is displaced uniformly outward and the top edge is left free. Consequently, the boundary conditions (BCs) follow as\n\ny = 0 (bottom edge): u_y = 0 (symmetry),\nx = 0 (left edge): u_x = 0 (symmetry),\nx = plate_size (right edge): prescribed u_x = displacement * t (loading).\n\nA structured polar-like mesh is generated by the helper quarter_plate_with_hole_grid. Figure 1 shows a schematic of the mesh and the applied BCs.\n\n(Image: Quarter plate with hole setup) Figure 1. Setup of the quarter plate with hole including boundary conditions.","title":"Problem setup"},{"category":"section","location":"tutorials/plate_with_hole/#Grid,-interpolation,-constraints","page":"2D plate with a hole","text":"We set up the grid, displacement field, and boundary conditions with standard Ferrite.jl calls:\n\ngrid = quarter_plate_with_hole_grid(; nr=6, ntheta=16, radius=0.25, plate_size=1.0) # create the quarter-plate mesh (custom function)\n\ninterpolation = Lagrange{RefQuadrilateral,1}()^2 # linear displacement shape functions in 2D\ndh = DofHandler(grid)                            # create the dof handler for the grid\nadd!(dh, :u, interpolation)                      # add displacement dofs under the field name `:u`\nclose!(dh)                                       # finalize the dof numbering\n\ndisplacement = 0.03                              # final prescribed right-edge displacement\nch = ConstraintHandler(dh)                       # collect Dirichlet boundary conditions\nadd!(ch, Dirichlet(:u, getfacetset(grid, \"symmetry_x\"), (x, t) -> [0.0], [2]))             # bottom symmetry: u_y = 0\nadd!(ch, Dirichlet(:u, getfacetset(grid, \"symmetry_y\"), (x, t) -> [0.0], [1]))             # left symmetry: u_x = 0\nadd!(ch, Dirichlet(:u, getfacetset(grid, \"right\"),     (x, t) -> [displacement * t], [1])) # right-edge loading: u_x = displacement*t\nclose!(ch)                                       # finalize the constraint data\n\nEach Dirichlet call constrains one displacement direction on one facetset. The right-edge boundary scales linearly with the load parameter t ∈ [0,1].","title":"Grid, interpolation, constraints"},{"category":"section","location":"tutorials/plate_with_hole/#Material","page":"2D plate with a hole","text":"A compressible Neo–Hookean material in plane stress is applied using\n\nE = 100.0\nν = 0.3\nmaterial = PlaneStress(NeoHooke(E, ν))\n\nThe PlaneStress wrapper turns the 3D NeoHooke into a 2D model by solving for the out-of-plane stretch F̄₃₃ at every quadrature point and statically condensing the tangent. It stores the converged out-of-plane stretch so the next local plane stress solve starts from the previous solution. See Wrappers for the math.","title":"Material"},{"category":"section","location":"tutorials/plate_with_hole/#Assembler","page":"2D plate with a hole","text":"create_assembler is the first FerriteSolidMechanics-specific step: it combines the material, dof handler, and constraints into the reusable object used by the solve loop.\n\nassembler = create_assembler(material, dh, ch; quadrature_order=2)\n\nThe assembler stores the material state at each quadrature point and reuses the arrays needed to assemble the global stiffness matrix and internal force vector.","title":"Assembler"},{"category":"section","location":"tutorials/plate_with_hole/#Load-and-Newton-loop","page":"2D plate with a hole","text":"The prescribed displacement is applied in load steps. stiffness_matrix is called inside each Newton iteration and update_states!(assembler) commits the final state after Newton converges:\n\nu = zeros(ndofs(dh))                             # allocate the displacement vector\nload_steps = 4                                   # number of equal load increments\nmax_newton = 20                                  # maximum Newton iterations per load step\nnewton_tol = 1e-8                                # residual tolerance\n\nfor step in 1:load_steps                         # advance the prescribed load\n    t = step / load_steps                        # current load factor\n    update!(ch, t)                               # update time-dependent boundary values\n    apply!(u, ch)                                # write prescribed displacements into `u`\n\n    for _ in 1:max_newton                        # Newton iterations for this load step\n        K, r = stiffness_matrix(assembler, u; dt=1.0 / load_steps) # constitutive evaluation & tangent + residual assembly\n        apply_zero!(K, r, ch)                    # apply homogeneous constraints to the correction\n        if norm(r) < newton_tol                  # stop once the residual is small enough\n            break\n        end\n        u .-= K \\ r                              # update the displacement vector\n    end\n    update_states!(assembler)                    # commit material states after convergence\nend\n\nThe keyword dt is the time step by which the material model is evaluated in the current load step. For strain rate-dependent models, this is the physical time increment. As NeoHooke is rate-independent, using dt = 1.0 / load_steps is an arbitrary choice.\n\nupdate_states!(assembler) is called after the load step has converged, which commits the internal material states at each quadrature point.","title":"Load and Newton loop"},{"category":"section","location":"tutorials/plate_with_hole/#Postprocessing","page":"2D plate with a hole","text":"compute_stresses evaluates stresses from the converged displacement field without advancing material history:\n\nstresses = compute_stresses(assembler, u)\n\nThe result is indexed by quadrature point and cell position and contains the in-plane Cauchy stress tensors.\n\nAdditionally, this script prints a short summary:\n\nSolved plate with hole\n  cells: 96\n  dofs: 238\n  max displacement: 0.03\n  stress entries: 384\n\nThe final cell-averaged plane stress von Mises stress field from this run is shown in Figure 2. Note that plotting is not covered in this tutorial due to its focus.\n\n(Image: Final cell-averaged von Mises stress) Figure 2. Final cell-averaged von Mises stress field.","title":"Postprocessing"},{"category":"section","location":"tutorials/plate_with_hole/#Swapping-the-material-model","page":"2D plate with a hole","text":"The same Newton loop works with other material models. To swap NeoHooke for another model, change only the material constructor; the assembler, boundary conditions, and postprocessing remain identical:\n\n# Same script, different material:\nmaterial = Hooke2D(E, ν; plane_stress=true) # recommended for linear 2D plane stress\nmaterial = PlaneStress(Hooke(E, ν))         # equivalent wrapper path, but more expensive\nmaterial = PlaneStrain(J2Plasticity(70e3, 0.3, 250.0, 1e3))  # small-strain plasticity\nmaterial = PlaneStress(VEPD_Detrez2010(2000.0, 0.3, 10.0, 5.0, 0.1, 1.0, 1.0, 1.0, 10.0, [100.0, 50.0], [0.1, 1.0]))\n\nThe same solver forwards dt to every material model assigned during assembly. Rate-independent models such as J2Plasticity ignore it; rate-dependent models such as VEPD_Detrez2010 use it in the local history update.\n\nFor pure linear 2D elasticity, prefer Hooke2D as it uses the analytical 2D stiffness directly. Use PlaneStrain(Hooke) or PlaneStress(Hooke) only when you want the 3D Hooke model to go through the same 2D wrapper path as other 3D materials, which lack an analytical 2D implementation.","title":"Swapping the material model"},{"category":"section","location":"tutorials/plate_with_hole/#Plain-program","page":"2D plate with a hole","text":"The full runnable source is rendered from examples/plate_with_hole_planestress.jl:\n\nusing Markdown\nimport FerriteSolidMechanics\npath = joinpath(pkgdir(FerriteSolidMechanics), \"examples\", \"plate_with_hole_planestress.jl\")\nMarkdown.parse(\"```julia\\n\" * read(path, String) * \"\\n```\")","title":"Plain program"},{"category":"section","location":"developer_guide/#Developer-guide","page":"Developer guide","text":"This page is for users who want to add a new constitutive model to FerriteSolidMechanics.jl. It explains the required interface that every material must implement, then walks through a practical example: the J2Plasticity model.\n\ninfo: Info\nWe recommend opening an existing material model from src/models/ side-by-side with this guide to see how these concepts translate to practical code.\n\nIf you are using the package rather than extending it, skip this page and start with the Concepts or Tutorials.","title":"Developer guide"},{"category":"section","location":"developer_guide/#Naming-conventions","page":"Developer guide","text":"tip: Start in experimental – avoid conventions\nYou can skip any instructions on this page to contribute a model in the first place. Drop it into src/models/experimental/ and it is exempt from the naming conventions and the testing and documentation requirements below (see Experimental models). Adopt the conventions only once you decide to promote to the official catalog.\n\nWhen adding a bundled (non-experimental) material model, please stick to these naming conventions:\n\n1. Material Model Constructors\n\nClassic / Universal Models: Use standard PascalCase for well-known models (e.g., J2Plasticity, ArrudaBoyce, MooneyRivlin).\nSpecific Literature Models: Use the format [Class]_[Author/Project][Year]_[Variant], where [Class] refers to the physics acronym (e.g. VEVP for viscoelastic–viscoplastic), [Author/Project] identifies the original author or project name, and [Variant] is an optional variant specifier.\n\nAdding [Year] is recommended to avoid naming collisions but might be left out for unique project names. Examples:\n\nVEPD_Detrez2010 (Class: VEPD, AuthorYear: Detrez2010)\nVEVP_MOAMMM (Class: VEVP, Project: MOAMMM)\nVEVP_Zhao2021_AT (Class: VEVP, AuthorYear: Zhao2021, Variant: AT (analytical tangent))\n\n2. Auxiliary Data Structures Any auxiliary structs required by a model (such as state variables or local caches) should be named by directly appending a PascalCase suffix to the exact constructor name, without additional underscores:\n\nState wrapper: [ConstructorName]State (e.g., VEVP_Zhao2021_ATState)\nInternal state: [ConstructorName]StateInternal (e.g., VEPD_Detrez2010StateInternal)\nCaches / Temp data: [ConstructorName]Cache or [ConstructorName]StiffnessDataGP\n\n3. Documentation The documentation filename is based on the constructor name from rule 1, converted to lowercase snake_case. Avoid the _[Variant] suffix if multiple variants share a documentation page, such as VEVP_Zhao2021_AT/AD.jl → vevp_zhao2021.md (\"VEVP Zhao 2021\"). The sidebar title on the website (generated via the make.jl file) is the model name formatted with spaces instead of underscores, preserving acronym capitalizations and separating the year.\n\nJ2Plasticity.jl → j2_plasticity.md (\"J2 Plasticity\")\nVEPD_Detrez2010.jl → vepd_detrez2010.md (\"VEPD Detrez 2010\")","title":"Naming conventions"},{"category":"section","location":"developer_guide/#Which-level-should-a-new-model-target?","page":"Developer guide","text":"The table below lists the available implementation levels, ordered from simplest to most general. Pick the highest row that fits; drop to a lower one only when the physics requires it.\n\nModel class Level to implement\nStateless hyperelasticity AbstractHyperelastic + Ψ(C)\nLocal small-strain models material_response with SmallStrain()\nLocal finite-strain models material_response with FiniteStrain()\nElement-structured assembly custom _assemble_element! (see VEVP_Zhao2021_AT)\nMixed / multi-field formulations Not supported by the current assembler\nNonlocal regularization custom _assemble_element! (needs cross-point coupling)\nRate-form / hypoelastic models material_response carrying F, σ in state (see VEPD_Detrez2010)\nCohesive-zone / interface models Not supported by the current assembler\nBeams / shells Not supported by the current assembler\n\nThe kinematics interface is deliberately open: new strain measures (and their weak-form contributions) can be added as new AbstractKinematics subtypes.","title":"Which level should a new model target?"},{"category":"section","location":"developer_guide/#The-required-interface","page":"Developer guide","text":"Every material constructor follows this convention: material parameters are positional, integration modes / other options are keyword arguments with defaults. See Concepts → Material constructor conventions for the convention and examples.\n\nA material is a struct that subtypes AbstractMaterial; history objects subtype AbstractMaterialState. There are two levels at which a material can plug into the assembler:\n\nThe constitutive level. Implement material_response, i.e., one function mapping the strain measure and the committed history at a single quadrature point to (stress, tangent, new_state), and declare the strain measure with kinematics (SmallStrain() or FiniteStrain()). The assembler's built-in element routine then handles the quadrature loop, integration weights (including alpha scaling), weak form, and trial-state bookkeeping automatically. (Comparable to Abaqus' UMAT)\nThe element level. Implement _assemble_element! yourself when the element structure is material-specific. The bundled VEVP_Zhao2021_AT provides an example: its hand-derived tangent is assembled from nodal shape-gradient blocks and cannot be expressed as a per-point ∂P/∂F. (Comparable to Abaqus' UEL)\n\nStateless hyperelastic models have a third, even shorter path: subtype AbstractHyperelastic and implement only the strain energy density Ψ(C, material) (see below).\n\nnote: Design acknowledgement\nThe constitutive-level interface follows the conventions established by Knut Andreas Meyer's MaterialModelsBase.jl (single response function returning (stress, tangent, new_state), strain-measure-driven stress/tangent pairs, material cache hook). Materials written for that interface here run through the FromMaterialModelsBase wrapper; state handling differences (e.g., mutable trial and commit vs immutable return values) are bridged by this wrapper.","title":"The required interface"},{"category":"section","location":"developer_guide/#Feature-support","page":"Developer guide","text":"Most solver-side behavior is provided automatically once a material implements the required hooks. Some capabilities, however, depend on the material model or wrapper explicitly supporting them:\n\nCapability What must be implemented or supported\nAssembly material_response + kinematics (generic element routine), or a custom _assemble_element!\nStress output Automatic via the generic fallback; override material_stress for a cheap tangent-free path when the tangent is expensive (AD models) or the output convention is model-specific\nTrial state tracking Automatic for the current/previous pattern; override set_trial! for other state layouts\nHistory commit and rollback create_state, update_state!, and revert_state! for stateful materials\nLinear preassembly is_linear(material) = true\nSymmetric-tangent fast path tangent_symmetry(material) = MajorSymmetric(), for small-strain materials whose D satisfies D[i,j,k,l] == D[k,l,i,j]\nRate dependence The material update must actually use dt\nGeneric PlaneStrain / PlaneStress wrappers compute_PK1_3D and update_state_from_3D! for the wrapped 3D material (derived automatically from material_response for AbstractHyperelastic subtypes and any SmallStrain material; finite-strain models must implement them)\nLocal failure reporting The material or wrapper must throw a LocalAssemblyFailure (try_stiffness_matrix then converts this to converged = false)","title":"Feature support"},{"category":"section","location":"developer_guide/#Required-functions-(linear-elastic-model)","page":"Developer guide","text":"These three methods fully define a linear elastic material model:\n\nis_linear(::MyMaterial) = true\nkinematics(::MyMaterial) = SmallStrain()\n\nfunction material_response(mat::MyMaterial, ε::SymmetricTensor{2}, state, dt, cache=nothing)\n    return mat.C ⊡ ε, mat.C, state\nend\n\nis_linear = true tells the assembler to build the element stiffness matrix exactly once. Consequently, this setting is only valid for models with a constant tangent.\n\nSee src/models/LinearElasticity.jl, which defines both Hooke and Hooke2D, for complete examples.\n\nnote: Note\nFor linear elastic material models, the package's assembler preassembles the element stiffness ke once through the generic element routine and stores it globally in K_linear. During the solver steps, the residual is computed as r = K_linear * u and the stress is evaluated using material_response.","title":"Required functions (linear elastic model)"},{"category":"section","location":"developer_guide/#Required-functions-(nonlinear-elastic-material)","page":"Developer guide","text":"For a generic nonlinear elastic material that is not history-dependent, you only need to define the material properties, flag it as nonlinear, and implement the constitutive response. No state tracking functions are needed:\n\nis_linear(::MyNonlinearMaterial) = false\nkinematics(::MyNonlinearMaterial) = FiniteStrain() # or SmallStrain()\n\nfunction material_response(mat::MyNonlinearMaterial, F::Tensor{2,3}, state, dt, cache=nothing)\n    # Compute P and dP_dF (or the small-strain equivalents)\n    return P, dP_dF, state\nend\n\nTo implement a hyperelastic material, simply subtype AbstractHyperelastic and implement the strain energy function Ψ(C, material) (see The hyperelastic quick path).","title":"Required functions (nonlinear elastic material)"},{"category":"section","location":"developer_guide/#Required-functions-(history-dependent-material)","page":"Developer guide","text":"For a history-dependent (stateful) material model, you must explicitly define the state structure and the methods to create and manage its history:\n\nstruct MyMaterial <: AbstractMaterial end  # replace \"Custom\" with your name here and in the following\nmutable struct CustomState <: AbstractMaterialState\n    current::CustomStateInternal\n    previous::CustomStateInternal\nend\n\nis_linear(::MyMaterial) = false\nkinematics(::MyMaterial) = FiniteStrain()   # or SmallStrain()\n\ncreate_state(::MyMaterial) = CustomState(CustomStateInternal(...), CustomStateInternal(...)) # creates a new state object\n\nfunction update_state!(s::CustomState)  # must include all state variables\n    s.previous = s.current\n    return nothing\nend\n\nfunction revert_state!(s::CustomState)  # must include all state variables\n    s.current = s.previous\n    return nothing\nend\n\nfunction material_response(mat::MyMaterial, F::Tensor{2,3}, state::CustomState, dt, cache=nothing)\n    # Read the committed history (state.previous), run the local update,\n    # compute P and ∂P/∂F (analytic or AD), while not committing state.\n    return P, dP_dF, new_state\nend\n\nThe package's generic element routine stores the returned new_state as the trial state via set_trial!. By default, set_trial!(state, new_state) assigns state.current = new_state, supporting the current/previous pattern above. Models with a different state layout override set_trial! (see VEPD_Detrez2010 for a field-wise example); materials that write their trial in place return the passed state.\n\nA cheap, tangent-free material_stress method is recommended for history models whose tangent is expensive (see VEPD_Detrez2010 and VEVP_Zhao2021_AD for examples, as well as Stress postprocessing below); otherwise the generic stress fallback evaluates material_response for postprocessing and discards the tangent and trial state (-> more expensive).\n\nThe \"trial and commit state\" pattern (current and previous) is convention for history-dependent models. If the internal state is defined as an immutable struct (as shown in the J2Plasticity example below), the default copy_state! function automatically handles copying all fields. Override copy_state! only when the default fallback is not enough (e.g., when a field is a mutable heap-allocated array that requires an explicit deepcopy).\n\nThe state object (CustomState) should be mutable when your functions update its fields in place, as shown above. Immutable state objects are also possible, but then update_state!(state) must return a replacement AbstractMaterialState. In this case, update_states!(assembler) stores that returned state back into the quadrature point state vector. If the state cannot be updated in-place (e.g., an immutable struct) and update_state! returns nothing, the package assumes no state update is required.","title":"Required functions (history-dependent material)"},{"category":"section","location":"developer_guide/#The-hyperelastic-quick-path","page":"Developer guide","text":"For a stateless hyperelastic model, you can subtype AbstractHyperelastic and implement one function:\n\nstruct CustomRubber2026 <: AbstractHyperelastic\n    μ::Float64\n    κ::Float64\nend\n\nfunction FerriteSolidMechanics.Ψ(C, mp::CustomRubber2026)   # strain energy density Ψ(C)\n    J = sqrt(det(C))\n    Ic̄ = det(C)^(-1/3) * tr(C)\n    return 0.5 * mp.μ * (Ic̄ - 3) + 0.5 * mp.κ * (J - 1)^2\nend\n\nIf you follow this hyperelastic path, stress and tangent are automatically derived with Tensors.hessian/Tensors.gradient by the package, and element assembly, stress output, and the PlaneStrain/PlaneStress wrappers are automatically functional. Optionally, every derived quantity can be overridden with analytic expressions (e.g. for speed) – one method at a time: _second_piola(C, mp) (stress only), constitutive_driver(C, mp) (S and ∂S/∂C), or _finite_strain_pk1_tangent(F, mp, dt) (P and ∂P/∂F directly). For example, the bundled NeoHooke, ArrudaBoyce, MooneyRivlin, and Ogden models are all AbstractHyperelastic subtypes, but only NeoHooke keeps the AD energy path, while the other three override the drivers analytically.","title":"The hyperelastic quick path"},{"category":"section","location":"developer_guide/#Optional-hooks-(for-PlaneStrain-/-PlaneStress-wrappers)","page":"Developer guide","text":"If you want your 3D material to be wrappable by PlaneStrain or PlaneStress, also implement\n\ncompute_PK1_3D(mat, F, dt, state) -> P̄\nupdate_state_from_3D!(state, mat, F, dt)\n\nThe two in-plane wrappers call compute_PK1_3D to evaluate the 3D PK1, then embed the 2D gradient, and extract the in-plane parts. Material models implement update_state_from_3D! to update its internal state via the trial/current variables using the converged 3D deformation gradient. (Not to be confused with update_states!, which must commit the state after Newton converges.) See Wrappers for the math.","title":"Optional hooks (for PlaneStrain / PlaneStress wrappers)"},{"category":"section","location":"developer_guide/#The-generic-element-routine","page":"Developer guide","text":"For a material implementing material_response + kinematics, the generic element routine does the following per quadrature point:\n\nCompute the integration weight α_dΩ = alpha_value(alphavalues, qp) * getdetJdV(cellvalues, qp) (alpha_value is 1 by default if not specified).\nEvaluate the strain measure (ε = sym(∇u) for SmallStrain, F = I + ∇u for FiniteStrain).\nCall material_response(material, ε_or_F, state, dt, cache).^1\nStore the returned trial state (see the three accepted return forms in the material_response docstring).\nAdd the weak-form contributions to ke/re (δε ⊡ σ / δε ⊡ D ⊡ Δε for small-strain, exploiting the symmetry of D; ∇δu ⊡ P / ∇δu ⊡ ∂P∂F ⊡ ∇δu for finite-strain).\n\n^1 The cache argument is created once per element via allocate_material_cache (default nothing). You can override this function to return a custom cache object if your material needs to reuse preallocated variables across quadrature points.","title":"The generic element routine"},{"category":"section","location":"developer_guide/#Custom-element-assembly","page":"Developer guide","text":"The package calls _assemble_element!(ke, re, states, material, cellvalues, alphavalues, u, dt) once per owned nonlinear cell. The generic fallback described above provides this function for every model that includes the material_response hook. We therefore recommend implementing a custom _assemble_element! method for your material only when the element structure itself is material-specific. VEVP_Zhao2021_AT is a bundled example: its analytical tangent is assembled from nodal shape-gradient blocks (calc_vevp_zhao_stiffness_block_jk), which does not reduce to a quadrature point ∂P/∂F. Nonlocal regularization is another model class that may require a custom element assembly. Mixed and multi-field formulations are currently out of reach of this hook: the assembler builds one CellValues per SubDofHandler from its single field interpolation, so a SubDofHandler carrying more than one field is rejected when the assembler is created.\n\nArguments passed to _assemble_element! are\n\nke, re: the preallocated element stiffness matrix and residual vector\n\n(A custom method must compute and add the element contributions to them. re is nothing in the linear preassembly path, which only reaches materials with is_linear(material) == true; ke is never nothing. There is no residual-only entry point that would skip ke: compute_forces wraps stiffness_matrix and discards the tangent.);\n\nstates: Vector{AbstractMaterialState} of length nquadpoints for nonlinear materials, or NoState() in the linear preassembly path;\nmaterial: the material model instance (e.g., your MyMaterial struct);\ncellvalues: the Ferrite CellValues object for the current SubDofHandler (already reinitialized for the cell);\nalphavalues: the integration weight scaling factors (if any) for the current SubDofHandler, or nothing;\nu: the element displacement vector;\ndt: the load step time increment.","title":"Custom element assembly"},{"category":"section","location":"developer_guide/#Element-arrays","page":"Developer guide","text":"ke and re are a dense Matrix{Float64} and Vector{Float64} from a preallocated workspace, sized to the cell's own dof count (size(ke, 1) == getnbasefunctions(cellvalues)).\n\nA custom _assemble_element! method is responsible for\n\nlooping over quadrature points;\nevaluating strain or deformation gradient;\ncomputing stress and tangent;\nmultiplying the quadrature point contributions by the integration weight via alpha_value(alphavalues, qp) * getdetJdV(cellvalues, qp) (The generic assembler does not apply this automatically for custom element routines.);\nadding the weak-form contributions to the element arrays ke and re;\nwriting trial state values to state.current\n\n(Do not commit the state here; state commitment happens after the global Newton step converges via update_states!.).\n\nThe return value of _assemble_element! is ignored. You must update the provided arrays and state objects in-place and return nothing.\n\nSome bundled materials delegate the nested shape function loops to internal helper functions after computing the quadrature point stress and tangent. These internal helpers expect specific kinematics: either a PK1 stress and ∂P/∂F tangent combined with standard shape gradients for finite-strain, or a small-strain stress and ∂σ/∂ε tangent combined with symmetric shape gradients. However, material-specific updates, local Newton solves, wrapper condensation, and state writes must remain in your custom _assemble_element! method.","title":"Element arrays"},{"category":"section","location":"developer_guide/#State-variables","page":"Developer guide","text":"State variables are allocated per quadrature point and follow a \"trial and commit\" pattern. They represent the uncommitted trial state (current) and the last converged state (previous). The assembler does not read or write the state variables directly; the material model owns them.\n\ncreate_state initializes current and previous to the same starting value.\nDuring the global Newton step (when calling (try_)stiffness_matrix), trial values are written into current at every quadrature point.\n\nAs a consequence, for models providing the material_response hook, the generic element routine stores them automatically via set_trial!. Custom _assemble_element! methods must update current manually.\n\nmaterial_stress is used for stress postprocessing.\n\nIt reads the state and returns stress, but it must not commit the state or advance history.\n\nupdate_state! is called (by update_states!(assembler) from the user-code) for every quadrature point once per load step, after the global Newton iteration has converged.\n\nIt commits current to previous.\n\nSimilarly, revert_state! is called by revert_states!(assembler) when a load step is rejected; it restores current from previous.\n\nThe user must explicitly call revert_states!(assembler) in their outer solver loop to perform this rollback when a load step is rejected (e.g., Newton does not converge).","title":"State variables"},{"category":"section","location":"developer_guide/#Example:-Implementing-J2Plasticity","page":"Developer guide","text":"A small non-trivial model is the small-strain J2 plasticity with linear isotropic hardening. It is ported from the Ferrite.jl plasticity tutorial [1]; reading that tutorial side-by-side with this section is the recommended way to see the correspondence between pure Ferrite code and the FerriteSolidMechanics.jl hooks.","title":"Example: Implementing J2Plasticity"},{"category":"section","location":"developer_guide/#Material-struct-and-state","page":"Developer guide","text":"struct J2Plasticity{T, S <: SymmetricTensor{4, 3, T}} <: AbstractMaterial\n    G::T   # shear modulus\n    K::T   # bulk modulus\n    σ₀::T  # initial yield limit\n    H::T   # hardening modulus\n    Dᵉ::S  # elastic stiffness tensor\nend\n\nstruct J2PlasticityStateInternal{T}\n    ϵᵖ::SymmetricTensor{2, 3, T, 6}  # plastic strain\n    σ::SymmetricTensor{2, 3, T, 6}   # stress\n    k::T                             # hardening variable\nend\n\nmutable struct J2PlasticityState <: AbstractMaterialState\n    current::J2PlasticityStateInternal{Float64}\n    previous::J2PlasticityStateInternal{Float64}\nend\n\nNote how J2PlasticityState explicitly implements the trial and commit pattern (via current and previous): J2PlasticityStateInternal is immutable, while only the wrapper is mutable, and only so that current can be reassigned to a new internal struct each Newton iteration. The type parameter T keeps the model wrappable via PlaneStrain/PlaneStress: they differentiate the 3D response with ForwardDiff, so the constitutive update below builds the internal state from strain and stress that arrive as ForwardDiff.Dual numbers while the committed history stays Float64.","title":"Material struct and state"},{"category":"section","location":"developer_guide/#State-hooks","page":"Developer guide","text":"We flag the material as nonlinear and define the required hooks to initialize the state object and manage its trial/commit updates:\n\nis_linear(::J2Plasticity) = false\n\nfunction create_state(::J2Plasticity)\n    return J2PlasticityState(\n        J2PlasticityStateInternal(zero(SymmetricTensor{2, 3}), zero(SymmetricTensor{2, 3}), 0.0),\n        J2PlasticityStateInternal(zero(SymmetricTensor{2, 3}), zero(SymmetricTensor{2, 3}), 0.0),\n    )\nend\n\nfunction update_state!(state::J2PlasticityState)\n    state.previous = state.current\n    return nothing\nend\n\nfunction revert_state!(state::J2PlasticityState)\n    state.current = state.previous\n    return nothing\nend","title":"State hooks"},{"category":"section","location":"developer_guide/#Local-constitutive-update","page":"Developer guide","text":"The constitutive update is handled by a single function as follows:\n\nfunction compute_stress_tangent(ϵ::SymmetricTensor{2, 3}, material::J2Plasticity, state::J2PlasticityStateInternal)\n    # unpack some material parameters\n    G = material.G\n    H = material.H\n\n    # Trial-values\n    σᵗ = material.Dᵉ ⊡ (ϵ - state.ϵᵖ)  # trial-stress\n    sᵗ = dev(σᵗ)                        # deviatoric part of trial-stress\n    J₂ = 0.5 * sᵗ ⊡ sᵗ                 # second invariant of sᵗ\n    σᵗₑ = sqrt(3.0 * J₂)                # effective trial-stress (von Mises stress)\n    σʸ = material.σ₀ + H * state.k      # Previous yield limit\n\n    φᵗ = σᵗₑ - σʸ                       # Trial-value of the yield surface\n\n    if φᵗ < 0.0                         # elastic loading\n        T = eltype(σᵗ)\n        ϵᵖ = convert(SymmetricTensor{2, 3, T, 6}, state.ϵᵖ)\n        return σᵗ, material.Dᵉ, J2PlasticityStateInternal(ϵᵖ, σᵗ, convert(T, state.k))\n    else                                # plastic loading\n        h = H + 3G\n        μ = φᵗ / h                      # plastic multiplier\n\n        c1 = 1 - 3G * μ / σᵗₑ\n        s = c1 * sᵗ                     # updated deviatoric stress\n        σ = s + vol(σᵗ)                 # updated stress\n\n        κ = H * (state.k + μ)           # drag stress\n        σₑ = material.σ₀ + κ            # updated yield surface\n\n        # ... algorithmic tangent D ...\n\n        Δϵᵖ = 3 / 2 * μ / σₑ * s        # plastic strain increment\n        ϵᵖ = state.ϵᵖ + Δϵᵖ             # plastic strain\n        k = state.k + μ                 # hardening variable\n        return σ, D, J2PlasticityStateInternal(ϵᵖ, σ, k)\n    end\nend","title":"Local constitutive update"},{"category":"section","location":"developer_guide/#Constitutive-interface","page":"Developer guide","text":"kinematics(::J2Plasticity) = SmallStrain()\n\nfunction material_response(mp::J2Plasticity, ε::SymmetricTensor{2,3}, state::J2PlasticityState, dt, cache=nothing)\n    return compute_stress_tangent(ε, mp, state.previous)\nend\n\nmaterial_response reads the committed history from state.previous and delegates to the constitutive update function. The generic element routine receives the returned (σ, D, new_state) tuple and automatically stores new_state as the uncommitted trial state for the current Newton iteration (via state.current = new_state).","title":"Constitutive interface"},{"category":"section","location":"developer_guide/#Stress-postprocessing","page":"Developer guide","text":"J2's tangent is cheap, so it defines no material_stress: the generic fallback (which evaluates material_response and discards the tangent) is therefore used. Overriding pays off only when the stress has a cheaper path than the tangent, as in AD history models that return it from the local solve without differentiating. If implemented for J2, the method would read:\n\nfunction material_stress(mp::J2Plasticity, ε::SymmetricTensor{2,3}, state::J2PlasticityState, dt, cache=nothing)\n    σ, _, _ = compute_stress_tangent(ε, mp, state.current)\n    return σ\nend\n\nmaterial_stress receives ε directly, reads state.current (the solver's latest values), and returns only σ; the tangent and updated state that compute_stress_tangent also returns are discarded, since postprocessing must not advance history. The generic _compute_stress_qp fallback extracts ε from the element values and calls material_stress.","title":"Stress postprocessing"},{"category":"section","location":"developer_guide/#wrapper-hooks","page":"Developer guide","text":"J2 plasticity is small-strain, so it needs no wrapper code at all: every SmallStrain material is wrappable automatically. A generic fallback implements both hooks (compute_PK1_3D and update_state_from_3D!) by calling material_response: given the wrapper's embedded 3D F̄, it forms ε = sym(F̄ - I), evaluates the response, and maps the Cauchy stress to PK1. The package then runs:\n\n# provided generically for any SmallStrain material – you do not write this\nfunction compute_PK1_3D(mp, F̄, dt, state)\n    ε = symmetric(F̄ - one(F̄))\n    σ, _, _ = material_response(mp, ε, state, dt)\n    return det(F̄) * σ ⋅ inv(F̄)'\nend\n\nOnly finite-strain models must implement compute_PK1_3D and update_state_from_3D! themselves as there is no universal F̄ → stress conversion for them (see VEVP_Zhao2021_AD for a worked example).\n\nBoth wrappers differentiate compute_PK1_3D with ForwardDiff, so F̄ carries ForwardDiff.Dual entries while the committed history remains Float64. A model that constructs a state object or scratch cache mixing both types fails with a MethodError. J2Plasticity stays wrappable by using generic number types for its internal state instead of hardcoding Float64.","title":"Wrapper hooks (for PlaneStrain / PlaneStress)"},{"category":"section","location":"developer_guide/#Tangent-strategies","page":"Developer guide","text":"Either hand-derive the tangent to hard-code an analytical tangent or use automatic differentiation via Tensors.gradient:\n\n# Finite-strain: ∂P/∂F\ndP_dF = Tensors.gradient(F_ -> compute_PK1(mat, F_, dt, state), F)\n\n# Small-strain: ∂σ/∂ε\ndσ_dε = Tensors.gradient(ε_ -> compute_stress(mat, ε_, dt, state), ε)","title":"Tangent strategies"},{"category":"section","location":"developer_guide/#Declaring-tangent-symmetry-(small-strain)","page":"Developer guide","text":"Small-strain materials may declare tangent_symmetry (optionally). The default Unsymmetric integrates every entry of ke; MajorSymmetric integrates only the lower triangle and mirrors it, which measures about 1.4× faster on element assembly:\n\ntangent_symmetry(::MyMaterial) = MajorSymmetric()   # only if D[i,j,k,l] == D[k,l,i,j]\n\nBefore declaring it, verify that the tangent D returned by material_response is major-symmetric at several strain states and on every branch (elastic and inelastic) of your model:\n\nD ≈ permutedims(D, (3, 4, 1, 2))   # true ⟹ major-symmetric, safe to opt in\n\nIf the check passes everywhere, declare MajorSymmetric(). If it fails on any branch, keep the default (Unsymmetric()). Note that SymmetricTensor{4} carries only the minor symmetries (D[i,j,k,l] == D[j,i,k,l] == D[i,j,l,k]), which do not imply major symmetry, so the tensor type alone is not sufficient.\n\nIsotropic elasticity and associated plasticity with isotropic hardening are major-symmetric, so the bundled Hooke, Hooke2D, and J2Plasticity opt in. Non-associated flow, nonlinear kinematic hardening, and anisotropic damage are not, and must keep the default. Declaring MajorSymmetric() wrongly discards the non-symmetric part of D: the residual stays correct while the tangent does not, so Newton converges either slowly or not at all. A solution that does converge is still correct, but the K returned by stiffness_matrix is not (matters only if you use it beyond the Newton update). Finite-strain materials are unaffected; ∂P/∂F is never assumed symmetric.","title":"Declaring tangent symmetry (small-strain)"},{"category":"section","location":"developer_guide/#Testing-material-model-implementations","page":"Developer guide","text":"It is recommended to implement three categories of tests:\n\nMaterial-point verification. Evaluate material_response for prescribed strain values to verify the computed stresses and consistent tangent moduli. Finite-difference approximations can be used to validate the analytical or automatically differentiated tangent operator. This test does not require a finite element assembly, making it suitable for comparison against other implementations. Several models have dedicated test files for this purpose, e.g. test/test_ogden.jl or test/test_vepd_detrez2010_tangent.jl.\n# Example\nmat = J2Plasticity(200.0e3, 0.3, 200.0, 10.0e3)\nstate = create_state(mat)\nε = SymmetricTensor{2,3}((i, j) -> i == j == 1 ? 2.0e-3 : 0.0)\nσ, D, new = material_response(mat, ε, state, 0.0)\nset_trial!(state, new)\nupdate_state!(state)\nEquivalence to reference implementations. Compare the element stiffness matrix and residual vector against a standard Ferrite cell loop under simple loading conditions. The script test/test_generic_assembler_equivalence.jl demonstrates this procedure.\nState management validation. The internal state variable handling must be verified by advancing the state and subsequently invoking revert_state!. The final state must be identical to the initial state. The available tests in test/test_state_management.jl can serve as a template.\n\nInstead of creating a dedicated file for each model, tests should generally be appended to the existing categorical test files in test/ and run via test/runtests.jl.","title":"Testing material model implementations"},{"category":"section","location":"developer_guide/#Test-coverage-requirements","page":"Developer guide","text":"Material models integrated into the core package (src/models/) require specific test coverage. The following test integrations are prioritized, with the first two being mandatory.\n\ntest/test_material_execution_paths.jl A single-element assembly test must be conducted to ensure that stiffness_matrix and compute_stresses return finite, non-zero results.\ntest/test_state_management.jl Stateful models require a test scenario that induces a non-trivial state change, such as yielding. The test must subsequently verify the proper functioning of the update_state! and revert_state! procedures.\ntest/test_stress_extraction.jl The compute_stresses function must be deterministic and independent of the time increment for repeated evaluations on a committed state.\ntest/test_dimensionality_wrappers.jl Models providing compute_PK1_3D and update_state_from_3D! methods must be tested within the PlaneStrain and PlaneStress wrappers. The chosen parameters must trigger every code branch (e.g. both the elastic and the plastic path), because a Float64/Dual type mismatch in an unexecuted branch stays undetected.\n\nThe existing tests in the test/ directory provide suitable templates.","title":"Test coverage requirements"},{"category":"section","location":"developer_guide/#Documentation-requirements","page":"Developer guide","text":"Each bundled model requires a documentation page in docs/src/models/ following the structure and style of the existing pages (e.g. ogden.md or vevp_moammm.md).","title":"Documentation requirements"},{"category":"section","location":"developer_guide/#Experimental-models","page":"Developer guide","text":"Experimental models located in src/models/experimental/ are exempt from the strict naming conventions, as well as the testing and documentation requirements. New contributions are welcome here directly: get a working model in first and only later adopt the bundled-model requirements if you decide to promote it.","title":"Experimental models"},{"category":"section","location":"developer_guide/#See-also","page":"Developer guide","text":"src/Interfaces.jl: Documentation of the required interfaces.\nsrc/models/ArrudaBoyce.jl and src/models/MooneyRivlin.jl: Simple stateless hyperelastic models that each support both :AT and :AD tangent options.\nsrc/models/Ogden.jl: A specialized analytical tangent involving spectral decomposition.\nsrc/models/VEVP_Zhao2021_AT.jl and src/models/VEPD_Detrez2010.jl: Stateful viscoelastic-(visco)plastic models with analytical and automatic differentiation tangents, respectively.","title":"See also"},{"category":"section","location":"developer_guide/#References","page":"Developer guide","text":"[1] Ferrite.jl documentation. Plasticity tutorial. url","title":"References"},{"category":"section","location":"#FerriteSolidMechanics.jl","page":"Home","text":"A solid mechanics assembly framework and material model library for Ferrite.jl.\n\nFerriteSolidMechanics.jl provides finite element assembly infrastructure for solid mechanics in Ferrite.jl alongside a library of ready-to-use constitutive models and a quadrature point interface for implementing new ones. The package handles element-level assembly, external load integration, state bookkeeping, thread and MPI parallelism, and provides MPI-distributed solving through MUMPS. Dimensionality wrappers adapt 3D models to 2D plane strain and plane stress analyses; an adaptive time step controller allows step size adjustment after failed material updates.\n\nnote: Note\nContributions are welcome! If you implement a new material model using FerriteSolidMechanics.jl, consider opening a pull request to add it to the package.\n\nIn a nutshell, FerriteSolidMechanics.jl works as follows:\n\nusing Ferrite\nusing FerriteSolidMechanics\n\n# After setting up a grid and initializing Ferrite's DofHandler `dh`,\n# ConstraintHandler `ch`, and displacement vector `u`:\nmaterial = J2Plasticity(100.0, 0.3, 1.0, 10.0)  # assign material model\nassembler = create_assembler(material, dh, ch)  # create material assembler\n\nK, r = stiffness_matrix(assembler, u; dt=0.5)   # get stiffness tangent and internal residual\n# ... solve the global Newton step and update u ...\nupdate_states!(assembler)                       # commit material state\nstresses = compute_stresses(assembler, u)       # postprocessing","title":"FerriteSolidMechanics.jl"},{"category":"section","location":"#Why-this-package","page":"Home","text":"Implementing a finite-strain constitutive model directly in Ferrite.jl requires cell iteration, sub-dof-handler setup, quadrature handling, optional thread/MPI parallelism, and state management for each model. This package factors that out by providing:\n\nA quadrature point constitutive interface (material_response, kinematics, plus the state lifecycle create_state, update_state!, revert_state!): a material author writes only the constitutive update – parameters, state, stress, and tangent – comparable to an Abaqus UMAT. A generic element routine owns the quadrature loop, integration weights, weak form, and trial-state bookkeeping; stateless hyperelastic models can even be defined by a single strain energy function via AbstractHyperelastic. For formulations whose element structure is material-specific, the element-level hooks (_assemble_element!, _compute_stress_qp) remain stable extension points.\nA generic material assembler that handles 2D / 3D, mixed-element grids, sub-dof-handlers, linear preassembly, thread parallelism (OhMyThreads.@tasks), and MPI-parallel constitutive evaluation and element assembly.\nExternal load assembly (LoadHandler) for Neumann boundary conditions: BodyForce, Traction, Pressure and NodalForce are collected with add! and integrated into the external force vector by external_forces!, using the assembler's quadrature order and thickness so that external and internal forces match.\nDimensionality wrappers (PlaneStrain, PlaneStress) that use compatible 3D materials in 2D analyses: PlaneStrain uses 2D-to-3D embedding, while PlaneStress solves the out-of-plane stretch with a local Newton iteration and condenses the tangent.\nBundled material models ranging from linear elasticity to finite-strain hyperelastic and viscoelastic-viscoplastic laws (MooneyRivlin, Ogden, ArrudaBoyce, VEPD_Detrez2010, VEVP_Zhao2021_AD, VEVP_MOAMMM).\n\nTo add a new material, implement the hooks for that material. The Developer guide walks through the smallest possible example using the bundled J2Plasticity model.","title":"Why this package"},{"category":"section","location":"#Where-to-start","page":"Home","text":"If you want to… Read\nGet started by running a simple 2D problem 2D plate with a hole\nRun a 3D viscoelastic problem and plot the results DMA cantilever beam\nRetry failed material updates with smaller steps Adaptive time stepping\nRun large-scale problems on clusters using MPI MPI four-point bending\nPick a constitutive model for your problem Material models\nReduce a 3D model to plane stress / plane strain Wrappers\nApply tractions, pressures, body forces, or nodal forces External loads\nUnderstand the assembler, time stepping, and material states Concepts\nChoose a linear solver, rank and thread layout, and BLAS threads Performance and parallel execution\nAdd your own material model Developer guide\nLook up the signature of a function API reference\nDiagnose a behaviour you don't understand FAQ\nSee relations to further Ferrite-based packages Related packages and acknowledgements","title":"Where to start"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url=\"https://github.com/LukasLaubert/FerriteSolidMechanics.jl\")\n\nFor local development:\n\nusing Pkg\nPkg.develop(path=\".\")\nPkg.test(\"FerriteSolidMechanics\")","title":"Installation"},{"category":"section","location":"#Status","page":"Home","text":"FerriteSolidMechanics.jl requires Julia 1.10 or newer; CI tests Julia 1.10 and the latest stable Julia 1.x. The implementation is tested with Julia 1.11.6 and Ferrite 1.4.1. It is licensed under the MIT license (see LICENSE in the repository root); material model provenance and acknowledgements are documented on the material pages.","title":"Status"},{"category":"section","location":"#Related-packages-and-acknowledgements","page":"Home","text":"FerriteSolidMechanics.jl sits alongside several Julia packages for constitutive modelling and finite element assembly, and follows some conventions established by them.\n\nMaterialModels.jl (K. Auth and contributors) is a material model library without an assembly layer. Its material_response convention returning (stress, tangent, new_state), the AbstractMaterial/AbstractMaterialState pair, and PlaneStrain/PlaneStress as dimensional wrappers are used here under the same names.\nMaterialModelsBase.jl (K. A. Meyer) is an implementation-independent interface package developed from MaterialModels.jl, with MechanicalMaterialModels.jl as the model library built on it. The constitutive interface in FerriteSolidMechanics.jl follows the MaterialModelsBase conventions, and materials written for MaterialModelsBase can be used through the FromMaterialModelsBase wrapper.\nFerriteAssembly.jl (K. A. Meyer) covers similar ground to the assembler here: a generic Ferrite assembly path over pluggable materials, with an element-level hook, update_states! for committing converged state, and per-task scratch buffers. Its LoadHandler, a container populated with add! and applied at a given time, is followed here under the same name. FerriteAssembly also assembles general facet contributions to the tangent and threads load assembly, neither of which FerriteSolidMechanics.jl currently does; assembly of the tangent and internal force in FerriteSolidMechanics.jl is instead MPI-parallel, with the external force vector replicated on every rank.\nFerrite.jl's hyperelasticity and von Mises plasticity tutorials are the source of the Ψ(C)/constitutive_driver convention and of the element integration loops.\nFerriteDistributed.jl runs MPI by partitioning the grid and the DofHandler. FerriteSolidMechanics.jl replicates the global system on every rank and reduces it instead, which leaves the Ferrite DofHandler unchanged at the cost of per-rank memory; see Performance and parallel execution.\nCAPRICCIO - Tool to run concurrent Finite Element-Molecular Dynamics Simulations (Version 3.0.0) (J. Roksvaag, Apache-2.0) couples finite elements to molecular dynamics and is the origin of the assembler interface used here. The entry points create_assembler, stiffness_matrix, compute_forces and compute_stresses, the _assemble_element! element hook, the alpha_value quadrature point scaling hook with its AlphaValues companion, and the replicated MPI assembly keep their names and design from it.\n\nScientific references and provenance for the bundled constitutive models are on the individual material pages.","title":"Related packages and acknowledgements"}]
}
